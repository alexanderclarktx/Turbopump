Linear issue status icons extracted from the public `https://linear.app` page on 2026-06-12.

Source symbols:

- `IssueStatusBacklog`
- `IssueStatusTodo`
- `IssueStatusStarted`
- `IssueStatusReview`
- `IssueStatusDone`
- `IssueStatusTriage`

The standalone SVGs preserve the original 16x16 path data and use `currentColor`.

`in-eng.svg` is a local derivative of `IssueStatusStarted` with a fuller inner fill, used to distinguish the custom `In Eng` workflow state from `Ready for Eng`.
