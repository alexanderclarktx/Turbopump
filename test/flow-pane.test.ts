import { describe, expect, test } from "bun:test";

const html = await Bun.file("public/index.html").text();
const appFiles = ["public/app.js", ...[...new Bun.Glob("public/js/*.js").scanSync()].sort()];
const app = (await Promise.all(appFiles.map((file) => Bun.file(file).text()))).join("\n");
const css = await Bun.file("public/styles.css").text();
const server = await Bun.file("src/server.ts").text();
const packageJson = await Bun.file("package.json").json();
const markdown = await Bun.file("public/linear-markdown.js").text();
const linearInEngStatusIcon = await Bun.file("public/linear-status-icons/in-eng.svg").text();
const linearInQaStatusIcon = await Bun.file("public/linear-status-icons/in-qa.svg").text();
const linearStartedStatusIcon = await Bun.file("public/linear-status-icons/started.svg").text();
const linearDoneStatusIcon = await Bun.file("public/linear-status-icons/done.svg").text();
const { renderLinearMarkdown } = await import("../public/linear-markdown.js");
const legacyFlowName = new RegExp(`${"water"}${"flow"}`, "i");

describe("Turbopump pane markup", () => {
  test("uses Turbopump for product copy and flow for the workflow noun", () => {
    expect(html).toContain("<title>Turbopump</title>");
    expect(html).not.toContain(" title=");
    expect(app).not.toMatch(/\.title\s*=/);
    expect(app).not.toContain("setAttribute(\"title\"");
    expect(markdown).toContain('aria-label="Copy code" title="Copy code"');
    expect(markdown).toContain('aria-label="Resize column" title="Resize column"');
    expect(app).toContain('title="Change Linear status"');
    expect(app).toContain('title="In flow"');
    expect(html).toContain("<h2>Tickets</h2>");
    expect(html).not.toContain("<h2>Linear Tickets</h2>");
    expect(html).toContain('href="/favicon.svg"');
    expect(app).toContain("/api/flows");
    expect(server).toContain("FLOW_RUN_ID");
    expect(app).not.toMatch(legacyFlowName);
    expect(css).not.toMatch(legacyFlowName);
    expect(html).not.toMatch(legacyFlowName);
    expect(server).not.toMatch(legacyFlowName);
  });

  test("uses a simple chevron for the settings collapse button", () => {
    expect(html).toContain('<path d="m15 6-6 6 6 6" />');
    expect(html).not.toContain('<rect x="3" y="4" width="18" height="16" rx="2" />');
    expect(html).not.toContain('<path d="M9 4v16" />');
  });

  test("shows a cogwheel icon for collapsed settings", () => {
    expect(html).toContain('<body class="settings-collapsed app-booting">');
    expect(html).toContain('aria-expanded="false"');
    expect(html).toContain('class="settings-collapsed-icon"');
    expect(css).toContain(".settings-collapsed-icon");
    expect(css).toContain("body.settings-collapsed .settings-collapsed-icon");
    expect(css).toContain("body.settings-collapsed .settings-toggle");
    expect(css).toContain("padding: 14px 0;");
    expect(css).toContain("body.settings-collapsed .settings-header {\n  display: grid;");
    expect(css).toContain("gap: 0;");
    expect(css).toContain("justify-self: center;");
    expect(css).toContain("--settings-pane-size: 26px;");
    expect(css).toContain("width: 26px;");
  });

  test("hides the Linear tickets pane on narrow screens", () => {
    expect(css).toContain("@media (max-width: 980px)");
    expect(css).toContain("  .ticket-drawer {\n    display: none;\n  }");
    expect(css).not.toContain("max-height: 360px;");
  });

  test("can hide the Linear tickets pane from the keyboard", () => {
    expect(app).not.toContain("flow.ticketDrawerHidden");
    expect(app).toContain("ticketDrawerHidden: false");
    expect(html).toContain("app-booting");
    expect(app).toContain("function setTicketDrawerHidden(hidden)");
    expect(app).toContain('document.body.classList.toggle("tickets-collapsed", hidden);');
    expect(app).toContain('els.ticketDrawer.setAttribute("aria-label", hidden ? "Expand tickets" : "Tickets");');
    expect(app).toContain('if (hidden) els.ticketDrawer.setAttribute("tabindex", "0");');
    expect(app).not.toContain("localStorage.setItem(TICKET_DRAWER_HIDDEN_KEY");
    expect(app).toContain('requestAnimationFrame(() => document.body.classList.remove("app-booting"));');
    expect(app).toContain("function toggleTicketDrawerHidden()");
    expect(app).toContain('event.code === "Backquote" || key === "`"');
    expect(css).toContain("--ticket-drawer-size: 320px;");
    expect(css).toContain("--settings-pane-size var(--motion-pane),\n    --ticket-drawer-size var(--motion-pane);");
    expect(css).toContain("body.tickets-collapsed main {\n  --ticket-drawer-size: 12px;\n}");
    expect(css).toContain("body.app-booting main,\nbody.app-booting .ticket-drawer {\n  transition: none;\n}");
    expect(css).toContain("body.tickets-collapsed .ticket-drawer");
    expect(css).not.toContain("body.tickets-collapsed .ticket-drawer {\n  place-items: center;\n  grid-template-rows: minmax(0, 1fr);\n  gap: 0;\n  padding: 0;\n  border-left-width: 0;");
    expect(css).not.toContain("body.tickets-collapsed .ticket-drawer::before");
    expect(css).toContain("body.tickets-collapsed .ticket-drawer > * {\n  display: none;\n}");
    expect(app).toContain('els.ticketDrawer.addEventListener("click"');
    expect(app).toContain('els.ticketDrawer.addEventListener("keydown"');
  });

  test("hides the settings pane entirely on narrow screens", () => {
    const narrowRules = css.slice(css.indexOf("@media (max-width: 980px)"), css.indexOf("@media (prefers-reduced-motion: reduce)"));
    expect(narrowRules).toContain("  .sidebar {\n    display: none;\n  }");
    expect(narrowRules).not.toContain("body.settings-collapsed .sidebar");
    expect(narrowRules).not.toContain("body.settings-collapsed .settings-header");
  });

  test("keeps the flow split height stable on narrow screens", () => {
    const narrowRules = css.slice(css.indexOf("@media (max-width: 980px)"), css.indexOf("@media (prefers-reduced-motion: reduce)"));
    expect(narrowRules).toContain("  main {\n    grid-template-columns: 1fr;\n  }");
    expect(narrowRules).not.toContain("height: auto;");
    expect(narrowRules).not.toContain(".workspace {\n    min-height: 720px;\n  }");
  });

  test("hides shell input and output on narrow screens", () => {
    const narrowRules = css.slice(css.indexOf("@media (max-width: 980px)"), css.indexOf("@media (prefers-reduced-motion: reduce)"));
    expect(narrowRules).toContain(
      '  .message-form {\n    grid-template-columns: minmax(0, 1fr);\n    grid-template-areas:\n      "search"\n      "images"\n      "input"\n      "context";\n  }',
    );
    expect(narrowRules).toContain("  .prompt-input-pane {\n    padding-right: 14px;\n  }");
    expect(narrowRules).toContain(
      '  .terminal-panel.shell-output-visible {\n    grid-template-columns: minmax(0, 1fr);\n    grid-template-areas:\n      "terminal"\n      "form";\n    grid-template-rows: minmax(0, 1fr) auto;\n  }',
    );
    expect(narrowRules).toContain("  .terminal-panel.shell-output-visible .shell-output-resizer,\n  .pane-rail,\n  .shell-command-panel,\n  .shell-output-pane {\n    display: none;\n  }");
    expect(narrowRules).not.toContain('      "context"\n      "shell";');
    expect(narrowRules).not.toContain('      "shell"\n      "form";');
    expect(narrowRules).not.toContain("minmax(140px, 32%)");
  });

  test("keeps the Settings heading larger than section toggles", () => {
    expect(css).toContain(".settings-header h2 {\n  margin: 0;\n  color: var(--ink);\n  font-size: 22px;");
    expect(css).toContain(".settings-section-toggle {\n  display: flex;");
    expect(css).toContain("font-size: 14px;");
    expect(css).not.toContain(".sidebar h2 {\n  margin: 0;\n  font-size: 14px;");
  });

  test("defaults settings closed unless Linear is disconnected", () => {
    expect(app).toContain("settingsCollapsed: true");
    expect(app).not.toContain('localStorage.getItem("flow.settingsCollapsed")');
    expect(app).not.toContain('localStorage.setItem("flow.settingsCollapsed"');
    expect(app).toContain("function setDefaultSettingsState(linear)");
    expect(app).toContain("setSettingsCollapsed(Boolean(linear.signedIn));");
    expect(app).toContain("function toggleSettingsCollapsed()");
    expect(app.indexOf("setDefaultSettingsState(data.linear);")).toBeLessThan(
      app.indexOf("updateLinearState(data.linear);"),
    );
  });

  test("toggles settings with Cmd+Comma", () => {
    expect(app).toContain("function handlePaneVisibilityShortcuts(event)");
    expect(app).toContain('const togglesSettings = event.metaKey && !event.ctrlKey && !event.altKey && (event.code === "Comma" || key === ",");');
    expect(app).toContain("if (!togglesSettings && !togglesTickets && !togglesShell) return false;");
    expect(app).toContain("event.preventDefault();");
    expect(app).toContain("event.stopImmediatePropagation();");
    expect(app).toContain("if (togglesSettings) toggleSettingsCollapsed();");
    expect(app).toContain('document.addEventListener("keydown", handlePaneVisibilityShortcuts, true);');
    expect(app).toContain("toggleSettingsCollapsed();\n});");
  });

  test("keeps the theme toggle visible outside settings expansion", () => {
    expect(html).toContain('class="hotkeys-info"');
    expect(html).toContain('aria-label="Show app hotkeys"');
    expect(html).toContain('<strong>Hotkeys</strong>');
    expect(html).toContain("<kbd>Cmd + ,</kbd>");
    expect(html).toContain("<kbd>Ctrl + `</kbd>");
    expect(html).toContain("<kbd>Cmd + \\</kbd>");
    expect(html).toContain("<kbd>Ctrl + R</kbd>");
    expect(html).toContain("<kbd>Esc</kbd>");
    expect(html).not.toContain("<kbd>Enter</kbd>");
    expect(html).not.toContain("<kbd>Shift + Enter</kbd>");
    expect(html).toContain('id="themeToggle"');
    expect(html).toContain('class="icon-button theme-toggle"');
    expect(html).toContain('class="corner-controls"');
    expect(html).toContain('aria-label="Switch to dark mode"');
    expect(html.indexOf('id="settingsPane"')).toBeLessThan(html.indexOf('id="themeToggle"'));
    expect(html.indexOf('class="hotkeys-info"')).toBeLessThan(html.indexOf('id="themeToggle"'));
    expect(html.indexOf("</main>")).toBeLessThan(html.indexOf('id="themeToggle"'));
    expect(app).toContain('const THEME_KEY = "flow.theme";');
    expect(app).toContain("theme: initialTheme()");
    expect(app).toContain("function setTheme(theme)");
    expect(app).toContain("function toggleTheme()");
    expect(app).toContain('setTheme(state.theme === "dark" ? "light" : "dark");');
    expect(app).toContain('els.themeToggle.addEventListener("click", (event) => {');
    expect(app).toContain("event.stopPropagation();");
    expect(app).toContain("toggleTheme();");
    expect(app).toContain('if (event.target !== els.settingsPane) return;');
    expect(css).toContain(".corner-controls {\n  position: fixed;");
    expect(css).toContain("grid-template-columns: 26px;");
    expect(css).toContain(".hotkeys-info {\n  position: relative;");
    expect(css).toContain("display: grid;\n  place-items: center;");
    expect(css).toContain("width: 26px;\n  height: 32px;");
    expect(css).toContain("height: 32px;\n  min-height: 32px;");
    expect(css).toContain(".hotkeys-info:hover .hotkeys-pane {\n  opacity: 1;");
    expect(css).not.toContain(".hotkeys-info:focus-within .hotkeys-pane");
    expect(css).toContain("left: 0;\n  bottom: 0;");
    expect(css).toContain("width: max-content;");
    expect(css).toContain("width: 26px;\n  min-width: 26px;");
    expect(css).toContain(".corner-controls .icon-button {\n  width: 26px;");
    expect(css).toContain(".hotkeys-info-trigger circle {\n  stroke-width: 1.5;");
    expect(css).toContain("text-align: center;");
    expect(css).toContain("grid-template-columns: max-content max-content;");
    expect(css).toContain("font-size: 11px;\n  white-space: nowrap;");
    expect(css).toContain(".hotkeys-pane dt {\n  min-width: 92px;");
    expect(css).toContain("box-sizing: border-box;");
    expect(css).toContain("text-align: center;\n  white-space: nowrap;");
    expect(css).toContain("border: 0;");
    expect(css).toContain("background: transparent;");
    expect(css).toContain("  .theme-toggle {\n    display: none;\n  }");
    expect(css).toContain("  .hotkeys-info {\n    display: none;\n  }");
    expect(css).toContain("body.settings-collapsed .theme-toggle");
    expect(css).toContain("body.settings-collapsed .hotkeys-info");
    expect(css).toContain("body.theme-dark");
    expect(css).toContain("body.theme-dark .theme-icon-sun");
  });

  test("keeps dark mode headings and user messages readable", () => {
    expect(css).toContain(".ticket-drawer h2 {\n  margin: 0;\n  color: var(--ink);");
    expect(css).toContain("body.theme-dark .ticket-status-separator {\n  color: var(--muted);\n}");
    expect(css).toContain("body.theme-dark .ticket-card .ticket-title {\n  color: var(--ink);\n}");
    expect(css).toContain("body.theme-dark .terminal-entry-user .terminal-entry-body {\n  color: var(--terminal-ink);");
    expect(css).toContain("body.theme-dark .linear-comment strong {\n  color: var(--terminal-ink);\n}");
    expect(css).toContain(".terminal-entry-user .terminal-entry-marker,\n.terminal-entry-user .terminal-entry-label {\n  color: #d97706;\n}");
    expect(css).toContain("body.theme-dark .terminal-entry-user .terminal-entry-marker,\nbody.theme-dark .terminal-entry-user .terminal-entry-label {\n  color: #fbbf24;\n}");
  });

  test("keeps Linear connection state pill from being selected", () => {
    expect(html).toContain('<span id="linearState" class="pill">');
    expect(css).toContain(".pill {");
    expect(css).toContain("user-select: none;");
  });

  test("does not show a Linear disconnect button", () => {
    expect(html).not.toContain("disconnectLinear");
    expect(html).not.toContain("Disconnect Linear");
    expect(app).not.toContain("disconnectLinear");
    expect(css).not.toContain("#disconnectLinear");
  });

  test("marks completed agent turns as browser and Linear card notifications", () => {
    expect(app).toContain('const NOTIFIED_LINEAR_ISSUES_KEY = "flow.notifiedLinearIssueIds";');
    expect(app).toContain("function initialNotifiedLinearIssues()");
    expect(app).toContain("notifiedLinearIssueIds: initialNotifiedLinearIssues()");
    expect(app).toContain("function persistLinearIssueNotifications()");
    expect(app).toContain("function updateBrowserTabNotification()");
    expect(app).toContain("function notifyAgentTurnEnded(flowId)");
    expect(app).toContain("function canAcknowledgeSelectedNotification()");
    expect(app).toContain("function acknowledgeSelectedLinearIssueNotification()");
    expect(app).toContain("function refreshFlowDiffAfterAgentTurn(flowId)");
    expect(app).toContain("function isAgentTurnEndedLog(log)");
    expect(app).toContain('<circle cx="50" cy="54" r="9" fill="#ffff00" />');
    expect(app).toContain("void updateBrowserTabNotification();");
    expect(app).toContain('document.addEventListener("visibilitychange", acknowledgeSelectedLinearIssueNotification);');
    expect(app).toContain('window.addEventListener("focus", acknowledgeSelectedLinearIssueNotification);');
    expect(app).toContain("if (isAgentTurnEndedLog(log)) {\n        refreshFlowDiffAfterAgentTurn(log.flowId);\n        notifyAgentTurnEnded(log.flowId);\n      }");
    expect(app).toContain('card.classList.toggle("agent-turn-notified"');
    expect(app).toContain("clearLinearIssueNotification(ticket.identifier, { render: false });");
    expect(css).toContain("@property --ticket-notification-angle");
    expect(css).toContain(".ticket-card.agent-turn-notified:not(.active)");
    expect(css).toContain(".ticket-card.agent-turn-notified:not(.active) {\n  border-width: 2px;");
    expect(css).toContain("from var(--ticket-notification-angle)");
    expect(css).toContain("#ef4444 0deg");
    expect(css).toContain("#ffff00 120deg");
    expect(css).toContain("#8b5cf6 240deg");
    expect(css).toContain("animation: ticket-notification-border 3.2s linear infinite;");
    expect(css).toContain("@keyframes ticket-notification-border");
    expect(css).toContain("to {\n    --ticket-notification-angle: 360deg;");
  });

  test("hides the Development settings section", () => {
    expect(html).toContain('<section class="settings-section development-settings" hidden>');
    expect(html).toContain("<span>Development</span>");
    expect(css).toContain("[hidden] {\n  display: none !important;");
  });

  test("adds an agent output toolbar that can hide trace messages", () => {
    expect(html).toContain('class="agent-output-toolbar"');
    expect(html).toContain('id="agentTraceToggle"');
    expect(html).toContain('aria-pressed="true"');
    expect(html).toContain('aria-label="Show tool output messages"');
    expect(app).toContain("agentTraceHidden: true");
    expect(app).toContain("agentTraceToggle: document.querySelector(\"#agentTraceToggle\")");
    expect(app).toContain("state.agentTraceHidden = !state.agentTraceHidden;");
    expect(app).toContain('els.agentTraceToggle.setAttribute("aria-pressed", String(state.agentTraceHidden));');
    expect(app).toContain('els.agentTraceToggle.setAttribute("aria-label", state.agentTraceHidden ? "Show tool output messages" : "Hide tool output messages");');
    expect(app).toContain("renderLogs(state.selectedFlowId, { force: true, preserveScrollTop: true });");
    expect(app).toContain("function renderableTerminalGroups(groups, options = {})");
    expect(app).toContain("function isAgentToolOutputGroup(group)");
    expect(app).toContain("function latestAgentToolOutputGroupIndex(groups)");
    expect(app).toContain("function hasLiveAgentReplyGroup(groups)");
    expect(app).toContain("const latestToolIndex = options.agentWorking && !hasLiveAgentReplyGroup(groups) ? latestAgentToolOutputGroupIndex(groups) : -1;");
    expect(app).toContain('block.classList.add("terminal-entry-tool-preview");');
    expect(app).toContain("group.latestVisibleToolOutput = index === latestToolIndex && isAgentToolOutputGroup(group);");
    expect(app).toContain("function traceThoughtGroups(groups)");
    expect(app).toContain("result.push({ ...group, children: traceThoughtGroups(group.children), defaultOpen: false });");
    expect(app).toContain("toggleTerminalTraceGroup(details);");
    expect(app).toContain("if (isAgentToolOutputGroup(group) && index !== latestToolIndex) continue;");
    expect(app).toContain("traceContent: Boolean(options.traceContent)");
    expect(css).toContain(".agent-output-toolbar {\n  grid-area: terminal;");
    expect(css).toContain(".agent-output-button[aria-pressed=\"true\"] .agent-output-slash");
    expect(css).toContain(".terminal-entry-tool-preview .terminal-entry-body-content");
    expect(css).toContain("max-height: calc(1.55em * 3);");
  });

  test("persists environment settings in a dedicated db table", () => {
    expect(html).toContain('<section class="settings-section environment-settings">');
    expect(html).toContain("<span>Environment</span>");
    expect(html).toContain('<div class="env-editor" id="envEditor" aria-label="Environment variables"></div>');
    expect(html).not.toContain('<textarea id="envEditor"');
    expect(css).toContain(".env-row {\n  display: grid;");
    expect(css).toContain("  padding: 0;");
    const envRowCss = css.slice(css.indexOf(".env-row {"), css.indexOf(".env-row input {"));
    expect(envRowCss).not.toContain("border-bottom");
    expect(css).toContain(".env-row + .env-row {\n  padding-top: 12px;");
    expect(css).not.toContain('grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);');
    expect(app).toContain("function parseEnvEditorRows(contents)");
    expect(app).toContain("function updateEnvRowControls()");
    expect(app).toContain("function envEditorGroupsFromRows(rows)");
    expect(app).toContain("function createEnvValueRow(value = \"\", active = false)");
    expect(app).toContain('keyInput.placeholder = "KEYNAME";');
    expect(app).toContain('addValueButton.className = "env-add-value";');
    expect(app).toContain('addValueButton.textContent = "+";');
    expect(app).toContain('valueList.className = "env-values";');
    expect(app).toContain('for (const value of values.length ? values : [{ value: "", active: false }]) {');
    expect(app).toContain('activeInput.className = "env-active-value";');
    expect(app).toContain('activeInput.type = "radio";');
    expect(app).toContain('valueInput.placeholder = "VALUE";');
    expect(app).toContain("function maskedEnvValue(value)");
    expect(app).toContain('valueInput.dataset.envValue = value;');
    expect(app).toContain("valueInput.value = maskedEnvValue(value);");
    expect(app).toContain("function revealEnvValueInput(input)");
    expect(app).toContain("function maskEnvValueInput(input)");
    expect(app).toContain('radio.hidden = !hasMultipleValues;');
    expect(app).toContain("radio.checked = valueRow === activeValueRow;");
    expect(app).toContain("const orderedValues = [");
    expect(app).toContain('els.envEditor.addEventListener("focusin", handleEnvEditorFocusIn);');
    expect(app).toContain('els.envEditor.addEventListener("change", handleEnvEditorChange);');
    expect(app).toContain('els.envEditor.addEventListener("click", handleEnvEditorClick);');
    expect(app).toContain("function envEditorContents()");
    expect(app).toContain("function handleEnvEditorPaste(event)");
    expect(app).toContain("function activateEnvValueRow(valueRow)");
    expect(app).toContain("function handleEnvEditorChange(event)");
    expect(app).toContain("function handleEnvEditorClick(event)");
    expect(app).toContain('if (event.target.closest(".env-active-value")) return;');
    expect(app).toContain("if (valueRow) activateEnvValueRow(valueRow);");
    expect(app).toContain('row.querySelector(".env-values")?.append(nextRow);');
    expect(app).toContain("row.replaceWith(...pastedEnvRows);");
    expect(app).toContain("if (!row || !envRowIsEmpty(row)) return;");
    expect(app).toContain('event.clipboardData?.getData("text")');
    expect(app).toContain("renderEnvEditor(env.contents || \"\");");
    expect(server).toContain("create table if not exists environment_variables");
    expect(server).toContain("const getEnvironmentVariablesStmt = db.query(\"select contents from environment_variables where id = 1\");");
    expect(server).toContain("const setEnvironmentVariablesStmt = db.query(");
    expect(server).toContain("function readEnvContents()");
    expect(server).toContain("return row?.contents ?? \"\";");
    expect(server).toContain("function writeEnvContents(contents: string)");
    expect(server).toContain("writeEnvContents(body.contents ?? \"\");");
    expect(server).not.toContain("sessionEnvContents");
    expect(server).not.toContain("const envPath");
    expect(server).not.toContain("const legacyEnvPath");
    expect(server).not.toContain("writeFileSync(envPath, body.contents ?? \"\", \"utf8\");");
    expect(server).toContain("...parseEnv(readEnvContents()),");
    expect(server).toContain("env[key] = value;");
    expect(server).toContain("env: runtimeEnv(flow),");
    expect(server).toContain("function shellRuntimeEnv(flow: Flow)");
    expect(server).toContain("delete env.NO_COLOR;");
    expect(server).toContain("delete env.NODE_DISABLE_COLORS;");
    expect(server).toContain('FORCE_COLOR: process.env.FORCE_COLOR || "3",');
    expect(server).toContain('CLICOLOR_FORCE: "1",');
    expect(server).toContain("env: shellRuntimeEnv(flow),");
    expect(server).toContain("function stopIdleAgentRuntimesForEnvUpdate()");
    expect(server).toContain("if (runtime.activeTurnId) continue;");
    expect(server).toContain("stopIdleAgentRuntimesForEnvUpdate();");
    expect(app).toContain('window.addEventListener("pagehide", flushEnvSaveOnPageHide);');
    expect(app).toContain("keepalive: true");
    expect(css).toContain(".env-key-field,\n.env-values,\n.env-value-field {\n  display: grid;");
    expect(css).toContain(".env-values {\n  gap: 6px;");
    expect(css).toContain(".env-key-field {\n  position: relative;");
    expect(css).toContain("padding-right: 28px;");
    expect(css).toContain("position: absolute;\n  top: 50%;\n  right: 6px;");
    expect(css).toContain("width: 18px;\n  min-width: 18px;\n  height: 18px;\n  min-height: 18px;");
    expect(css).toContain("padding: 0;");
    expect(css).toContain("transform: translateY(-50%);");
    expect(css).toContain(".env-row.env-has-active-choice .env-value-field");
    expect(css).toContain(".env-active-value");
    expect(css).toContain("accent-color: #d97706;");
    expect(css).toContain(".env-add-value");
    expect(css).toContain(".env-row:hover .env-add-value,\n.env-row:focus-within .env-add-value");
  });

  test("makes settings sections collapsible", () => {
    expect(html).toContain('class="settings-section-toggle"');
    expect(html).toContain('class="settings-section-body"');
    expect(app).toContain('const COLLAPSED_SETTINGS_SECTIONS_KEY = "flow.collapsedSettingsSections";');
    expect(app).toContain("function initialCollapsedSettingsSections()");
    expect(app).toContain('if (raw === null) return new Set(["checkouts"]);');
    expect(app).toContain("collapsedSettingsSections: initialCollapsedSettingsSections()");
    expect(app).toContain("function toggleSettingsSection(section)");
    expect(app).toContain('els.settingsContent.addEventListener("click", (event) => {');
    expect(app).toContain('localStorage.setItem(COLLAPSED_SETTINGS_SECTIONS_KEY, JSON.stringify([...state.collapsedSettingsSections]));');
    expect(css).toContain(".settings-section.collapsed .settings-section-body {\n  display: none;");
    expect(css).toContain(".settings-section.collapsed .settings-section-chevron");
  });

  test("exposes agent developer instructions in Settings", () => {
    expect(html).toContain('<section class="settings-section agent-settings">');
    expect(html).toContain("<span>Agents</span>");
    expect(html).toContain('id="agentDeveloperInstructions"');
    expect(html).toContain('id="resetAgentDeveloperInstructions"');
    expect(html).toContain('aria-label="Reset developer instructions"');
    expect(html).not.toContain('id="agentStartPrompt"');
    expect(app).toContain("agentDeveloperInstructions: document.querySelector");
    expect(app).toContain("resetAgentDeveloperInstructions: document.querySelector");
    expect(app).not.toContain("agentStartPrompt");
    expect(app).toContain("function agentConfigSignature()");
    expect(app).toContain("async function saveAgentConfig()");
    expect(app).toContain('await api("/api/agents",');
    expect(app).toContain("els.agentDeveloperInstructions.value = data.agents.developerInstructions || \"\";");
    expect(app).toContain("state.defaultAgentDeveloperInstructions = data.agents.defaultDeveloperInstructions || \"\";");
    expect(app).toContain('state.defaultAgentProvider = data.agents.defaultProvider === "claude" ? "claude" : "codex";');
    expect(app).toContain("els.agentDeveloperInstructions.addEventListener(\"input\", scheduleAgentConfigSave);");
    expect(app).toContain('els.resetAgentDeveloperInstructions.addEventListener("click", () => {');
    expect(app).toContain("els.agentDeveloperInstructions.value = state.defaultAgentDeveloperInstructions;");
    expect(css).toContain(".agent-settings textarea");
    expect(css).toContain(".setting-label-row");
    expect(server).toContain("const defaultAgentDeveloperInstructions");
    expect(server).not.toContain("defaultAgentStartPrompt");
    expect(server).toContain("function renderAgentTemplate");
    expect(server).toContain("function getStoredSetting(key: string)");
    expect(server).toContain('const stored = getStoredSetting("agentDeveloperInstructions");');
    expect(server).toContain("if (stored !== null) return stored;");
    expect(server).not.toContain("legacyAgentDeveloperInstructions");
    expect(server).toContain('setSetting("agentDeveloperInstructions", defaultAgentDeveloperInstructions);');
    expect(server).toContain('agents: {\n        developerInstructions: getAgentDeveloperInstructionsTemplate(),');
    expect(server).toContain("defaultDeveloperInstructions: defaultAgentDeveloperInstructions,");
    expect(server).toContain('url.pathname === "/api/agents"');
    expect(server).toContain('setSetting("agentDeveloperInstructions"');
    expect(server).not.toContain('setSetting("agentStartPrompt"');
    expect(server).not.toContain("buildAgentPrompt");
    expect(server).toContain("flowMetaApiUrl");
    expect(server).toContain('Example body: {"prUrl":"https://github.com/org/repo/pull/123"}');
    expect(server).toContain("Each field in the body is optional.");
    expect(server).not.toContain('"Current stage: {stage}"');
    expect(server).not.toContain("FLOW_STAGE");
    expect(server).not.toContain("stage: flow.stage,");
    expect(server).toContain("prUrl: flow.prUrl,");
  });

  test("shows worktree cleanup cards in Settings and deletes trace data with the workspace", () => {
    expect(html).toContain('<section class="settings-section checkout-settings">');
    expect(html).toContain("<span>Worktrees</span>");
    expect(html).toContain('id="checkoutList"');
    expect(app).toContain("checkouts: []");
    expect(app).toContain("checkoutsLoaded: false");
    expect(app).toContain("checkoutsLoading: false");
    expect(app).toContain("checkoutList: document.querySelector");
    expect(app).toContain("function renderCheckouts()");
    expect(app).toContain("function renderCheckoutCard(checkout)");
    expect(app).toContain("function ensureCheckoutsLoaded()");
    expect(app).toContain("function scheduleCheckoutsLoaded()");
    expect(app).toContain("if (state.settingsCollapsed) return;");
    expect(app).toContain("renderCheckouts();\n    scheduleCheckoutsLoaded();");
    expect(app).toContain("scheduleCheckoutsLoaded();");
    expect(app).toContain("checkoutLoadFrame = requestAnimationFrame");
    expect(app).toContain("state.checkoutsLoading = true;");
    expect(app).toContain("function renderLinearStatusIcon(status)");
    expect(app).toContain('if (key === "in-qa" || key === "qa") return "in-qa";');
    expect(app).toContain('if (key === "in-eng") return "in-eng";');
    expect(app).toContain('if (typeKey === "unstarted") return "todo";');
    expect(app).toContain('if (typeKey === "started") return "started";');
    expect(app).toContain('<span class="linear-status-icon-glyph" aria-hidden="true"></span>');
    expect(app).toContain('["Linear", renderLinearStatusIcon(checkout.linearStatus)]');
    expect(app).not.toContain('["Phase", checkout.flowPhase || "No flow"]');
    expect(app).toContain("function deleteCheckout(name)");
    expect(app).toContain("function clearFlowClientState(flowId)");
    expect(app).toContain("if (data.flows) setFlows(data.flows);");
    expect(app).toContain("if (data.checkouts) setCheckouts(data.checkouts);");
    expect(app).toContain("Date.parse(a.lastPromptAt || a.createdAt || 0)");
    expect(app).toContain("deletingCheckoutNames: new Set()");
    expect(app).toContain('spinner.className = "checkout-spinner";');
    expect(app).toContain('await api(`/api/checkouts/${encodeURIComponent(name)}`, { method: "DELETE" });');
    expect(app).toContain('if (message.event === "checkouts")');
    expect(css).toContain(".checkout-list");
    expect(css).toContain(".linear-status-icon");
    expect(css).toContain(".linear-status-icon-glyph");
    expect(css).toContain('url("/linear-status-icons/in-eng.svg")');
    expect(css).toContain('url("/linear-status-icons/in-qa.svg")');
    expect(css).toContain('url("/linear-status-icons/started.svg")');
    expect(css).toContain('url("/linear-status-icons/done.svg")');
    expect(linearInEngStatusIcon).toContain("M8 4a4 4 0 1 1-4 4h4z");
    expect(linearInQaStatusIcon).toContain("M8 4a4 4 0 1 1-3.236 1.649L8 8z");
    expect(linearStartedStatusIcon).toContain("M8 1a7 7 0 1 1 0 14");
    expect(linearDoneStatusIcon).toContain("M8 1a7 7 0 1 0 0 14");
    expect(css).toContain(".checkout-card:hover .checkout-delete");
    expect(css).toContain(".checkout-spinner");
    expect(css).toContain("@keyframes checkout-spinner");
    expect(css).toContain("pointer-events: none;");
    expect(server).toContain("function listWorktrees()");
    expect(server).toContain("Date.parse(a.lastPromptAt || a.createdAt || \"\")");
    expect(server).toContain("function latestLinearStatusForFlow(flow: Flow | null)");
    expect(server).toContain("return issue?.state?.name || flow.linearStatus || \"\";");
    expect(server).toContain("linearStatus: latestLinearStatusForFlow(flow),");
    expect(server).not.toContain("function refreshWorktreeLinearStatuses()");
    expect(server).not.toContain("function scheduleWorktreeLinearStatusRefresh()");
    expect(server).not.toContain("worktreeLinearStatusRefreshRunning");
    expect(server).not.toContain('if (url.pathname === "/api/checkouts" && request.method === "GET") {\n    await');
    expect(server).not.toContain('if (url.pathname === "/api/checkouts" && request.method === "GET") {\n    schedule');
    expect(server).toContain("latestPromptTimestamp(flow.id)");
    expect(server).toContain('url.pathname === "/api/checkouts"');
    expect(server).toContain('parts[0] === "api" && parts[1] === "checkouts"');
    expect(server).toContain("function deleteWorktree(name: string)");
    expect(server).toContain("function deleteFlowTraceData(flowId: string)");
    expect(server).toContain("function stopFlowRuntimesForDelete(flowId: string)");
    expect(server).toContain("deleteLogsByFlowIdStmt.run(flowId);");
    expect(server).toContain("deleteShellOutputStateByFlowIdStmt.run(flowId);");
    expect(server).toContain("deleteSettingByKeyStmt.run(codexThreadSettingKey(flowId));");
    expect(server).toContain("deleteFlowByIdStmt.run(flowId);");
    expect(server).toContain('broadcast("flows", listClientFlows());');
    expect(server).toContain("return json({ ok: true, ...result, flows: listClientFlows(), checkouts: listWorktrees() });");
    expect(server).toContain("rmSync(target, { recursive: true, force: true });");
  });

  test("backs off noisy Linear connectivity failures", () => {
    expect(server).toContain("class LinearUnavailableError extends Error");
    expect(server).toContain("const linearUnavailableBackoffMs = 30_000;");
    expect(server).toContain("const linearRequestTimeoutMs = 5_000;");
    expect(server).toContain("signal: AbortSignal.timeout(linearRequestTimeoutMs),");
    expect(server).toContain("function isLinearNetworkError(error: unknown)");
    expect(server).toContain("FailedToOpenSocket|ConnectionRefused|Unable to connect");
    expect(server).toContain("function throwIfLinearUnavailable()");
    expect(server).toContain("markLinearUnavailable();");
    expect(server).toContain('throw new LinearUnavailableError("Linear is unreachable. Check your network connection.");');
    expect(server).toContain("function logLinearUnavailable(error: unknown)");
    expect(server).toContain("if (error instanceof LinearUnavailableError) {\n        logLinearUnavailable(error);");
    expect(server).toContain("{ status: error instanceof LinearUnavailableError ? error.status : 500 }");
    expect(server).toContain("create table if not exists linear_issues");
    expect(server).toContain("const upsertLinearIssueStmt = db.query(`");
    expect(server).toContain("const allLinearIssuesStmt = db.query(\"select issueJson from linear_issues order by updatedAt desc\");");
    expect(server).toContain("upsertLinearIssueStmt.run(identifier, JSON.stringify(issue), issue.updatedAt || now());");
    expect(server).toContain("function cachedLinearIssuesFromDb()");
    expect(server).toContain("function cachedAssignedLinearIssuesPayload()");
    expect(server).toContain("function hasCachedAssignedLinearIssues()");
    expect(server).toContain('if (url.searchParams.get("cached") === "1") return json(cachedAssignedLinearIssuesPayload());');
    expect(server).toContain("if (!apiKey && hasCachedAssignedLinearIssues()) {");
    expect(server).toContain("return cachedAssignedLinearIssuesPayload();");
    expect(server).toContain("if (!assigned.cached) setSetting(\"linearViewerName\", assigned.viewer.name);");
  });

  test("keeps refresh buttons visually quiet", () => {
    expect(html).toContain('id="refreshLinearTickets"');
    expect(html).toContain('id="createLinearTicket"');
    expect(html).toContain('id="searchLinearTickets"');
    expect(html).toContain('id="resetAgentDeveloperInstructions"');
    expect(css).toContain(".ticket-drawer-actions {\n  display: flex;\n  align-items: center;\n  gap: 2px;\n  margin-left: auto;\n  margin-right: -5px;\n}");
    expect(css).toContain(".ticket-drawer-header #refreshLinearTickets,\n.ticket-drawer-header #createLinearTicket,\n.ticket-drawer-header #searchLinearTickets,\n.ticket-search-pane #closeTicketSearch {\n  width: 28px;\n  min-width: 28px;\n  min-height: 28px;\n  flex: 0 0 28px;\n}");
    expect(css).toContain("#refreshLinearTickets,\n#createLinearTicket,\n#searchLinearTickets,\n#closeTicketSearch,\n#resetAgentDeveloperInstructions {\n  border-color: transparent;\n  background: transparent;\n}");
    expect(css).toContain("#refreshLinearTickets,\n#createLinearTicket,\n#searchLinearTickets,\n#closeTicketSearch {\n  color: var(--muted);\n}");
    expect(css).toContain("#refreshLinearTickets:hover,\n#createLinearTicket:hover,\n#searchLinearTickets:hover,\n#closeTicketSearch:hover,\n#resetAgentDeveloperInstructions:hover");
    expect(app).toContain("linearTicketsLoading: false");
    expect(app).toContain("function updateRefreshLinearTicketsButton()");
    expect(app).toContain('els.refreshLinearTickets.classList.toggle("is-loading", state.linearTicketsLoading);');
    expect(app).toContain("if (state.linearTicketsLoading) return;");
    expect(css).toContain("#refreshLinearTickets.is-loading svg {\n  animation: refresh-linear-tickets-spin 900ms linear infinite;");
    expect(css).toContain("@keyframes refresh-linear-tickets-spin");
  });

  test("filters Linear tickets by title from the ticket drawer search pane", () => {
    expect(html.indexOf('id="createLinearTicket"')).toBeLessThan(html.indexOf('id="searchLinearTickets"'));
    expect(html).toContain('id="ticketSearchPane"');
    expect(html).toContain('id="ticketSearchInput"');
    expect(html).toContain('id="closeTicketSearch"');
    expect(app).toContain("ticketSearchOpen: false");
    expect(app).toContain("ticketSearchQuery: \"\"");
    expect(app).toContain("searchLinearTickets: document.querySelector(\"#searchLinearTickets\"),");
    expect(app).toContain("ticketSearchInput: document.querySelector(\"#ticketSearchInput\"),");
    expect(app).toContain("function openTicketSearch()");
    expect(app).toContain("function closeTicketSearch()");
    expect(app).toContain("els.refreshLinearTickets.hidden = state.ticketSearchOpen;");
    expect(app).toContain("els.createLinearTicket.hidden = state.ticketSearchOpen;");
    expect(app).toContain("els.searchLinearTickets.hidden = state.ticketSearchOpen;");
    expect(app).toContain("function ticketMatchesTicketSearch(ticket, query)");
    expect(app).toContain('return String(ticket.title || "").toLowerCase().includes(query);');
    expect(app).toContain("const pinnedTickets = orderedPinnedTickets(tickets).filter((ticket) => !searchQuery || ticketMatchesTicketSearch(ticket, searchQuery));");
    expect(app).toContain("const ticketGroups = searchQuery");
    expect(app).toContain("collapsed: false,");
    expect(app).toContain("tickets: group.tickets.filter((ticket) => ticketMatchesTicketSearch(ticket, searchQuery)),");
    expect(app).toContain("for (const group of ticketGroups) {");
    expect(app).not.toContain("renderTicketSearchResults");
    expect(app).toContain('els.searchLinearTickets.addEventListener("click", openTicketSearch);');
    expect(app).toContain('els.closeTicketSearch.addEventListener("click", closeTicketSearch);');
    expect(app).toContain('els.ticketSearchInput.addEventListener("input", () => {');
    expect(app).toContain('if (event.key !== "Escape") return;');
    expect(css).toMatch(/\.ticket-drawer \{[^}]*display: grid;[^}]*grid-template-rows: auto auto minmax\(0, 1fr\);/s);
    expect(css).toContain(".ticket-search-pane {\n  display: grid;");
    expect(css).toContain(".ticket-search-pane[hidden] {\n  display: none;\n}");
  });

  test("keeps cached Linear tickets rendered when a ticket refresh fails", () => {
    expect(app).toContain("if (!options.refreshDetails) await loadCachedLinearTickets();");
    expect(app).toContain('const data = await api("/api/linear/issues?cached=1");');
    expect(app).toContain("if (data.issues?.length) applyLinearTicketsPayload(data, { loaded: false, prefetchLogs: false });");
    expect(app).toContain("function applyLinearTicketsPayload(data, options = {})");
    expect(app).toContain("state.linearTicketsLoaded = options.loaded !== false;");
    expect(app).toContain('els.ticketState.textContent = data.cached ? "Showing cached tickets." : formatLastUpdated();');
    expect(app).toContain(
      'els.linearState.textContent = data.cached ? "Linear unavailable; using cached tickets" : `Linear connected: ${data.viewer.name}`;',
    );
    expect(app).toContain('els.linearState.classList.toggle("live", !data.cached);');
    expect(app).not.toContain("state.linearTickets = [];");
    expect(app).toContain("els.ticketState.textContent = state.linearTickets.length ? `Showing cached tickets. ${error.message}` : error.message;");
  });

  test("paginates assigned Linear tickets", () => {
    expect(server).toContain("type LinearIssueConnection = {");
    expect(server).toContain("let after: string | null = null;");
    expect(server).toContain("query AssignedToMe($after: String, $includeWorkflowStates: Boolean!)");
    expect(server).toContain("assignedIssues(first: 100, after: $after)");
    expect(server).toContain("workflowStates(first: 250) @include(if: $includeWorkflowStates)");
    expect(server).toContain("{ after, includeWorkflowStates: !after }");
    expect(server).toContain("pageInfo {\n                hasNextPage\n                endCursor\n              }");
    expect(server).toContain("issues.push(...data.viewer.assignedIssues.nodes);");
    expect(server).toContain("after = pageInfo?.hasNextPage ? pageInfo.endCursor ?? null : null;");
    expect(server).toContain("} while (after);");
    expect(server).toContain("return assignedLinearIssuesPayload(viewer, issues, false, workflowStates);");
  });

  test("omits Done and Duplicate Linear tickets unless they already have a Turbopump session", () => {
    expect(server).toContain("function isDoneLinearIssue(issue: LinearIssue)");
    expect(server).toContain('return stateType === "completed" || stateName === "done" || stateName === "completed";');
    expect(server).toContain("function isDuplicateLinearIssue(issue: LinearIssue)");
    expect(server).toContain('return linearWorkflowKey(issue.state?.name ?? "") === "duplicate";');
    expect(server).toContain("if (!flow && (isDoneLinearIssue(issue) || isDuplicateLinearIssue(issue))) return [];");
  });

  test("lets the open Linear tickets pane resize between 280 and 320px", () => {
    expect(html).toContain('class="ticket-drawer-resizer"');
    expect(html).toContain('aria-label="Resize tickets pane"');
    expect(html).toContain('aria-valuemin="280"');
    expect(html).toContain('aria-valuemax="320"');
    expect(app).toContain('const TICKET_DRAWER_MIN_SIZE = 280;');
    expect(app).toContain('const TICKET_DRAWER_MAX_SIZE = 320;');
    expect(app).toContain('const TICKET_DRAWER_SIZE_KEY = "flow.ticketDrawerSize";');
    expect(app).toContain("function clampTicketDrawerSize(value)");
    expect(app).toContain("const applied = state.ticketDrawerHidden ? 12 : clampTicketDrawerSize(state.ticketDrawerSize);");
    expect(app).toContain('els.ticketDrawerResizer.addEventListener("pointerdown", startTicketDrawerResize);');
    expect(app).toContain('els.ticketDrawerResizer.addEventListener("keydown", handleTicketDrawerResizeKeydown);');
    expect(css).toContain(".ticket-drawer-resizer {");
    expect(css).toContain("body.tickets-collapsed .ticket-drawer-resizer {\n  display: none;\n}");
  });

  test("refreshes cached Linear descriptions and comments from the ticket refresh button", () => {
    expect(app).toContain("async function loadLinearTickets(options = {})");
    expect(app).toContain("function reconcileRemovedLinearTickets(previousTickets, nextTickets)");
    expect(app).toContain("function removeLinearIssueFromTurbopump(identifier)");
    expect(app).toContain("state.linearDetails.delete(issueId);");
    expect(app).toContain("state.ticketInputStates.delete(issueId);");
    expect(app).toContain("const removedPin = state.pinnedLinearIssues.delete(issueId);");
    expect(app).toContain("clearSelectedLinearIssue(issueId);");
    expect(app).toContain("reconcileRemovedLinearTickets(previousTickets, nextTickets);");
    expect(app).toContain("if (options.refreshDetails) state.linearDetails.clear();");
    expect(app).toContain(
      'els.refreshLinearTickets.addEventListener("click", () => void loadLinearTickets({ refreshDetails: true }));',
    );
    expect(app).toContain('const data = await api(`/api/linear/issues/${encodeURIComponent(identifier)}`);');
    expect(app).toContain("if (error.status === 404)");
    expect(app).toContain('state.linearDetails.set(identifier, { loading: false, deleted: true, error: "Linear issue not found" });');
    expect(app).toContain("error.status = response.status;");
    expect(server).toContain("comments(first: 50)");
    expect(server).toContain("parent { id }");
    expect(server).toContain("description");
    expect(server).toContain("archivedAt");
    expect(server).toContain("function isDeletedLinearIssue(issue: LinearIssue)");
    expect(server).toContain("if (isDeletedLinearIssue(issue)) return [];");
    expect(server).toContain("function isLinearIssueNotFoundError(error: unknown)");
  });

  test("creates new pinned In Eng Linear tickets from the ticket drawer", () => {
    expect(html.indexOf('id="refreshLinearTickets"')).toBeLessThan(html.indexOf('id="createLinearTicket"'));
    expect(app).toContain("createLinearTicket: document.querySelector(\"#createLinearTicket\"),");
    expect(app).toContain("async function createPinnedLinearTicket()");
    expect(app).toContain('const data = await api("/api/linear/issues", { method: "POST" });');
    expect(app).toContain('setLinearIssuePinned(issue.identifier, true, { position: "top" });');
    expect(app).not.toContain("state.pinnedLinearIssues.add(issue.identifier);");
    expect(app).toContain("state.linearDetails.set(issue.identifier, { loading: false, issue });");
    expect(app).toContain("function focusLinearTicketCard(identifier)");
    expect(app).toContain('const card = [...els.ticketGrid.querySelectorAll(".ticket-card")].find((item) => item.dataset.issue === identifier);');
    expect(app).toContain('card?.focus({ preventScroll: true });');
    expect(app).toContain('card?.scrollIntoView({ block: "nearest" });');
    expect(app).toContain("focusLinearTicketCard(issue.identifier);");
    expect(app).toContain('els.createLinearTicket.addEventListener("click", () => void createPinnedLinearTicket());');
    expect(server).toContain("async function createBlankInEngLinearIssue()");
    expect(server).toContain('linearWorkflowKey(state.name) === "in-eng"');
    expect(server).toContain("issueCreate(input: $input)");
    expect(server).toContain('title: "turbopump placeholder"');
    expect(server).toContain('url.pathname === "/api/linear/issues" && request.method === "POST"');
  });

  test("renders Linear comments oldest to newest", () => {
    expect(app).toContain("function linearCommentTree(comments)");
    expect(app).toContain("const sorted = [...comments].sort(");
    expect(app).toContain("new Date(a.createdAt || 0).getTime() - new Date(b.createdAt || 0).getTime()");
  });

  test("renders nested Linear comments as replies", () => {
    expect(app).toContain("function linearCommentParentId(comment)");
    expect(app).toContain('return comment.parent?.id || comment.parentId || "";');
    expect(app).toContain("if (parent) parent.replies.push(comment);");
    expect(app).toContain('class="linear-comment${nested ? " linear-comment-reply" : ""}"');
    expect(app).toContain('class="linear-comment-replies"');
    expect(css).toContain(".linear-comment-replies {\n  display: grid;");
    expect(css).toContain("border-left: 2px solid var(--line);");
  });

  test("uses higher contrast border tokens for subtle element outlines", () => {
    expect(css).toContain("--line: #c4ccd7;");
    expect(css).toContain("--line-strong: #9aa6b7;");
    expect(css).toContain("--line: #3d4754;");
    expect(css).toContain("--line-strong: #5a6675;");
    expect(css).toContain(".linear-markdown code,\n.terminal-entry-body code {\n  border: 1px solid var(--line);");
    expect(css).toContain(".linear-markdown .markdown-code-block,\n.terminal-entry-body .markdown-code-block {\n  margin: 6px 0;\n  border: 1px solid var(--line);");
    expect(css).not.toContain("border: 1px solid #d7dce3;");
    expect(css).not.toContain("border-color: #343c47;");
  });

  test("hides scrollbar UI globally", () => {
    expect(css).toContain("scrollbar-width: none;");
    expect(css).toContain("*::-webkit-scrollbar");
    expect(css).toContain("display: none;");
  });

  test("prevents horizontal scrolling in Linear panes", () => {
    expect(css).toContain(".ticket-grid {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n  contain: layout paint style;\n  overflow-x: hidden;\n  overflow-y: auto;");
    expect(css).toContain(".linear-detail {\n  display: grid;\n  align-content: start;\n  gap: 12px;\n  padding: 14px;\n  contain: layout paint style;\n  overflow-x: hidden;\n  overflow-y: auto;");
  });

  test("prevents horizontal scrolling in the Settings pane", () => {
    expect(css).toContain(".settings-content {\n  display: grid;\n  align-content: start;\n  box-sizing: border-box;\n  gap: 18px;\n  min-height: 0;\n  min-width: 0;\n  width: 100%;\n  max-width: 100%;\n  overflow-x: hidden;\n  overflow-y: auto;\n  overscroll-behavior-x: none;");
    expect(css).toContain(".sidebar button,\n.sidebar input,\n.sidebar .pill,\n.sidebar textarea {\n  box-sizing: border-box;\n  min-width: 0;\n  max-width: 100%;");
    expect(css).toContain(".settings-section-toggle > span:first-child {\n  min-width: 0;\n  overflow-wrap: anywhere;");
    expect(app).toContain("function resetSettingsHorizontalScroll()");
    expect(app).toContain('els.settingsContent.addEventListener("scroll", resetSettingsHorizontalScroll);');
    expect(app).toContain('els.settingsPane.addEventListener(\n  "wheel"');
    expect(app).toContain('if (state.settingsCollapsed || event.target.closest(".settings-content")) return;');
    expect(app).toContain("els.settingsContent.scrollTop += event.deltaY;");
    expect(app).toContain("{ passive: false }");
  });

  test("avoids layout animations when opening and closing Settings", () => {
    expect(css).not.toContain("transition: grid-template-columns");
    expect(css).not.toContain("grid-template-rows 180ms ease");
    expect(css).not.toContain("padding 180ms ease");
    expect(css).toContain("@property --settings-pane-size {\n  syntax: \"<length>\";");
    expect(css).toContain("--settings-pane-size var(--motion-pane)");
    expect(css).toContain(".sidebar {\n  position: relative;\n  box-sizing: border-box;");
    expect(css).toContain("contain: layout style;");
    expect(css).toContain("contain: layout paint style;");
  });

  test("uses a vertical split flow pane instead of tabs", () => {
    expect(html).not.toContain("flow-titlebar");
    expect(html).not.toContain("window-toolbar");
    expect(html).not.toContain('class="tabs browser-tabs"');
    expect(html).not.toContain('class="tab-panel');
    expect(html).not.toContain('data-tab=');
    expect(html.indexOf('class="linear-panel"')).toBeLessThan(html.indexOf('class="flow-resizer"'));
    expect(html.indexOf('class="flow-resizer"')).toBeLessThan(html.indexOf('class="agent-panel terminal-panel"'));
  });

  test("keeps the Linear and Agent panes flush to the workspace", () => {
    expect(css).toContain(
      ".workspace {\n  min-width: 0;\n  overflow: hidden;\n  border-top: 1px solid var(--line);\n  background: var(--bg);\n  padding: 0;\n}",
    );
    expect(css).toContain(".flow-pane {\n  height: 100%;");
    expect(css).toContain("border-radius: 0;");
    expect(css).toContain("border-top: 0;");
    expect(css).toContain("border-bottom: 0;");
    expect(css).toContain("box-shadow: none;");
  });

  test("keeps controls scoped to their panes", () => {
    expect(html).toContain('class="linear-panel"');
    expect(html).not.toContain("sync-linear");
    expect(html).not.toContain("Codex app-server command");
    expect(html).not.toContain('id="agentCommand"');
    expect(html).toContain('class="agent-panel terminal-panel"');
    expect(html).not.toContain('class="wf-stage"');
    expect(html).not.toContain('class="wf-agent"');
    expect(html).not.toContain('class="agent-actions"');
    expect(html).not.toContain('class="diff-panel"');
    expect(html).not.toContain('class="refresh-diff"');
    expect(html).not.toContain('class="diff-summary"');
  });

  test("keeps input panes editable and blocks submission at submit time", () => {
    expect(html).toContain('<div class="agent-disabled-message">Configure the repo URL to use the agent.</div>');
    expect(app).toContain("function repoUrlConfigured()");
    expect(app).toContain("const agentEnabled = repoUrlConfigured();");
    expect(app).toContain('agentPanel.classList.toggle("disabled", !agentEnabled);');
    expect(app).toContain("function canSubmitPromptMessage()");
    expect(app).toContain("function canSubmitShellCommand()");
    expect(app).toContain("function flashBlockedInput(input)");
    expect(app).toContain("queuedPrompt: null");
    expect(app).toContain("async function queuePromptMessage(input)");
    expect(app).toContain("function queuedPromptForFlow(flow)");
    expect(app).toContain("async function submitQueuedPromptSteer()");
    expect(app).toContain("function handleQueuedPromptKeydown(event)");
    expect(app).toContain('["a", "c", "r"].includes(event.key.toLowerCase())');
    expect(app.indexOf('["a", "c", "r"].includes(event.key.toLowerCase())')).toBeLessThan(
      app.indexOf('if (event.key === "Tab" && handleInputPaneTabKeydown(event)) return true;'),
    );
    expect(app).toContain('if (event.key === "Tab" && handleInputPaneTabKeydown(event)) return true;');
    expect(app).toContain("function promptQueuedCanSteer(flow = selectedFlow())");
    expect(app).toContain("const compactQueued = queued && flowAgentCompacting(flow);");
    expect(app).toContain("queuedHint.hidden = !queued || compactQueued;");
    expect(app).toContain('queuedHint.textContent = queuedCanSteer ? \'message queued — press "s" to steer\' : "message queued";');
    expect(app).toContain("function queuedPromptIsCompact(queued)");
    expect(app).toContain("promptQueuedCanSteer() &&");
    expect(app).toContain("!queuedPromptIsCompact(queued)");
    expect(app).toContain('event.key.toLowerCase() === "s" &&');
    expect(app).toContain("if (!event.repeat) void submitQueuedPromptSteer();");
    expect(app).toContain('if (event.key === "$" && !event.metaKey && !event.ctrlKey && !event.altKey && !event.isComposing)');
    expect(app).toContain('if (event.key === "Escape") {\n    if (!event.repeat) clearQueuedPrompt();\n    return true;\n  }');
    expect(app).toContain('if (event.key === "Backspace") {\n    clearQueuedPrompt();\n    return true;\n  }');
    expect(app).not.toContain('if (event.key === "Backspace" || event.key === "Escape")');
    expect(app).toContain("function handleQueuedPromptBeforeInput(event)");
    expect(app).toContain("function flushQueuedPrompt()");
    expect(app).toContain("for (const { queued, flow } of queuedPromptEntries())");
    expect(app).toContain("await submitQueuedPromptMessage(queued, flow);");
    expect(app).toContain("function queuedPromptDraftMatches(value, message = \"\")");
    expect(app).toContain("return value === message || value.trim() === message.trim();");
    expect(app).toContain("function clearPromptDraftForIssue(issueId, message = \"\")");
    expect(app).toContain("if (queuedPromptDraftMatches(inputState.promptValue, message)) inputState.promptValue = \"\";");
    expect(app).toContain("if (input && queuedPromptDraftMatches(input.value, message)) {");
    expect(app).toContain("function clearQueuedPromptDraftState(flow, message = \"\")");
    expect(app).toContain("clearPromptDraftForIssue(issueId, message);");
    expect(app).toContain("if (!options.preserveQueuedPromptDraft) syncClearedQueuedPromptDrafts(state.flows, nextFlows);");
    expect(app).toContain("function syncClearedQueuedPromptDrafts(previousFlows, nextFlows)");
    expect(app).toContain("if (queuedPromptMessage(nextFlow)) continue;");
    expect(app).toContain("clearPromptDraftForIssue(previousFlow.linearIssueId || linearIssueIdForFlowId(previousFlow.id), message);");
    expect(app).toContain("clearQueuedPromptDraftState(flow, queued.message);");
    expect(app).toContain("function updateFlowQueuedPrompt(flowId, queuedPrompt, options = {})");
    expect(app).toContain("if (queued?.flowId) updateFlowQueuedPrompt(queued.flowId, null, { preserveQueuedPromptDraft: true });");
    expect(app).toContain("if (flowId) updateFlowQueuedPrompt(flowId, null);");
    expect(app.indexOf("clearQueuedPromptDraftState(flow, queued.message);")).toBeLessThan(
      app.indexOf("renderFlowPane();\n    renderLogs(flow.id, { force: true, scrollToLatest: true });"),
    );
    expect(app).toContain('/api/flows/${encodeURIComponent(flow.id)}/queued-prompt');
    expect(app).toContain('/api/flows/${encodeURIComponent(flow.id)}/queued-prompt/steer');
    expect(app).toContain('/api/flows/${encodeURIComponent(flow.id)}/queued-prompt/flush');
    expect(app).toContain('const QUEUED_PROMPT_PREFIX_HTML = \'<span class="queued-prompt-spinner" aria-hidden="true"></span>\';');
    expect(app).toContain('if (queued && !prefix.querySelector(".queued-prompt-spinner")) {');
    expect(app).toContain("queuedHint.hidden = !queued || compactQueued;");
    expect(app).toContain("messageInput.disabled = false;");
    expect(app).toContain("commandInput.disabled = false;");
    expect(app).toContain('messageForm.setAttribute("aria-disabled", "false");');
    expect(app).toContain("renderFlowPane();\n});");
    expect(css).toContain(".agent-disabled-message");
    expect(css).toContain(".agent-panel.disabled .agent-disabled-message {\n  display: grid;");
    expect(css).toContain(
      ".agent-panel.disabled .terminal,\n.agent-panel.disabled .agent-output-toolbar,\n.agent-panel.disabled .shell-output-resizer,\n.agent-panel.disabled .shell-output-pane,\n.agent-panel.disabled .message-form {\n  display: none;",
    );
    expect(css).toContain(".message-input.input-submit-blocked");
    expect(css).toContain(".queued-prompt-hint");
    expect(css).toContain(".prompt-input-pane.prompt-queued .prompt-input-prefix");
    expect(css).toContain(".queued-prompt-spinner");
    expect(css).toContain("box-sizing: border-box;");
    expect(css).toContain("border-right-color: transparent;");
    expect(css).toContain("animation: queued-prompt-spinner 1600ms linear infinite;");
    expect(css).toContain("@keyframes queued-prompt-spinner");
    expect(css).toContain("@keyframes input-submit-blocked");
    expect(css).not.toContain(".message-form.input-disabled .message-input");
  });

  test("does not expose flow stages", () => {
    expect(server).not.toContain("type Stage");
    expect(server).not.toContain("const stages");
    expect(server).toContain('tryMigration("alter table flows drop column stage");');
    expect(server).not.toContain("flow.stage");
    expect(server).not.toContain('parts[3] === "stage"');
    expect(server).not.toContain("stageApiUrl");
    expect(app).not.toContain("flowStage");
    expect(app).not.toContain("state.stages");
  });

  test("uses the Linear ticket id as the default flow branch name", () => {
    expect(server).toContain('let branch = `turbo/${safeSlug(issueId)}`;');
    expect(server).not.toContain('const branch = `flow/${safeSlug(issueId)}`;');
    expect(server).not.toContain('const branch = `flow/${safeSlug(issueId)}-${flowId.slice(0, 8)}`;');
  });

  test("uses git worktrees instead of copied checkouts for flow workspaces", () => {
    expect(server).toContain('const worktreeDir = join(dataDir, "worktrees");');
    expect(server).not.toContain("const checkoutDir = worktreeDir;");
    expect(server).not.toContain('join(dataDir, "checkouts")');
    expect(server).not.toContain('join(dataDir, "threadtrees")');
    expect(server).not.toContain("function checkoutRoots()");
    expect(server).toContain("const warmedRepoPullIntervalMs = 60000;");
    expect(server).toContain("let warmedRepoPulling = false;");
    expect(server).toContain("function pullWarmedRepo()");
    expect(server).toContain("if (warmedRepoPulling) return;");
    expect(server).toContain("if (!serverHasActiveWork()) return;");
    expect(server).toContain('runGit(["pull", "--ff-only"], repoCheckoutDir);');
    expect(server).toContain("setInterval(pullWarmedRepo, warmedRepoPullIntervalMs);");
    expect(server).not.toContain("type TimingEntry = {");
    expect(server).not.toContain("function timeStep");
    expect(server).not.toContain("function insertTimingLogs");
    expect(server).toContain("function createWorktree(flowId: string, issueId: string)");
    expect(server).toContain("id, linearIssueId, linearIssueUrl, title, linearStatus, agentServiceTier, agentProvider,");
    expect(server).toContain(") values (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)");
    expect(server).toContain('body.linearStatus?.trim() || body.state?.name?.trim() || linearIssue?.state?.name || "",\n      "",');
    expect(server).toContain('const repoCheckoutDir = join(dataDir, "repo");');
    expect(server).toContain("function ensureRepoCheckout(repoUrl: string)");
    expect(server).toContain('runGit(["clone", repoUrl, repoCheckoutDir])');
    expect(server).toContain('runGit(["remote", "get-url", "origin"], repoCheckoutDir)');
    expect(server).toContain("rmSync(repoCheckoutDir, { recursive: true, force: true })");
    expect(server).toContain('runGit(["worktree", "prune"], repoCheckoutDir)');
    expect(server).toContain("ensureRepoCheckout(repoUrl)");
    expect(server).toContain('runGit(["rev-parse", "HEAD"], repoCheckoutDir)');
    expect(server).toContain('runGit(["worktree", "add", "-b", branch, target, baseSha], repoCheckoutDir)');
    expect(server).toContain('branch = `${branch}-${flowId.slice(0, 8)}`;');
    expect(server).toContain('runGit(["worktree", "remove", "--force", target], repoCheckoutDir);');
    expect(server).toContain('insertLog(id, "flow", `Created worktree ${branch}\\n`);');
    expect(server).not.toContain("flow:timing");
    expect(server).toContain("const linearIssueCache = new Map<string, LinearIssue>();");
    expect(server).toContain("function cachedLinearIssue(identifier: string)");
    expect(app).toContain("issue: ticket.identifier,");
    expect(app).toContain("url: ticket.url,");
    expect(app).toContain("linearStatus: linearStatusName(ticket),");
    expect(server).toContain("const linearIssue = cachedLinearIssue(parsed.identifier);");
    expect(server).toContain("const { target, branch, baseSha } = createWorktree(id, parsed.identifier);");
    expect(server).not.toContain("cpSync(");
    expect(server).not.toContain('runGit(["clone", repoUrl, target]);');
    expect(server).not.toContain('runGit(["checkout", "-b", branch], target);');
  });

  test("allocates missing worktrees lazily for prompts and shell commands", () => {
    expect(server).toContain("function flowHasWorktree(flow: Flow)");
    expect(server).toContain("function ensureFlowWorktree(flow: Flow)");
    expect(server).toContain("flow = ensureFlowWorktree(flow);");
    const startAgentBody = server.slice(server.indexOf("async function startAgent("), server.indexOf("function startShellCommand("));
    expect(startAgentBody).toContain("flow = ensureFlowWorktree(flow);");
    const shellBody = server.slice(server.indexOf("function startShellCommand("), server.indexOf("function interruptShellCommand"));
    expect(shellBody).toContain("flow = ensureFlowWorktree(flow);");
    expect(server).toContain("updateFlow(flow.id, { checkoutPath: target, branchName: branch, baseSha });");
    expect(server).toContain('throw new Error(`Worktree does not exist: ${flow.checkoutPath}`);');
    expect(server).not.toContain("repaired worktree path");
  });

  test("shows visible error toasts for git clone failures", () => {
    expect(html).toContain('id="toastStack"');
    expect(css).toContain(".toast-stack");
    expect(css).toContain(".toast-error");
    expect(app).toContain("function toast(message, options = {})");
    expect(app).toContain("function isGitCloneError(error)");
    expect(app).toContain('toast(error.message, { kind: "error" });');
    expect(server).toContain("function gitCommandLabel(args: string[])");
    expect(server).toContain('`${gitCommandLabel(args)} failed: ${output}`');
  });

  test("trusts flow worktrees before starting the Codex app server", () => {
    expect(server).toContain("function ensureCodexProjectTrusted(projectPath: string)");
    expect(server).toContain('const codexHome = codexEnv.CODEX_HOME || process.env.CODEX_HOME || join(homedir(), ".codex");');
    expect(server).toContain('const header = `[projects.${tomlBasicString(resolve(projectPath))}]`;');
    expect(server).toContain('appendFileSync(configPath, `${prefix}\\n${header}\\ntrust_level = "trusted"\\n`, "utf8");');
    expect(server).toContain('Bun.spawn(["/bin/zsh", "-lc", command], {');
    expect(server).toContain("function assertFlowWorktree(flow: Flow)");
    expect(server).toContain("const activeFlow = assertFlowWorktree(flow);");
    expect(server).toContain("ensureCodexProjectTrusted(activeFlow.checkoutPath);");
    const codexStartup = server.slice(server.indexOf("async function startCodexAppServer"));
    expect(codexStartup.indexOf("ensureCodexProjectTrusted(activeFlow.checkoutPath);")).toBeLessThan(
      codexStartup.indexOf('Bun.spawn(["/bin/zsh", "-lc", command], {'),
    );
    expect(codexStartup).toContain("env,");
  });

  test("does not render the Linear status inside the Linear pane detail", () => {
    expect(app).not.toContain('class="linear-state"');
    expect(app).not.toContain("context.linearStatus");
    expect(css).not.toContain(".linear-state");
  });

  test("renders Linear pane priority as an editable pill with the same bar icon used in tickets", () => {
    expect(app).toContain("const priorityControl = renderLinearPriorityControl(issue);");
    expect(app).toContain("${priorityControl}");
    expect(app).toContain("function renderLinearPriorityControl(issue)");
    expect(app).toContain("if (container._linearDetailHtml === html)");
    expect(app).toContain('data-linear-priority-pill="true"');
    expect(app).toContain('data-linear-priority-option="true"');
    expect(app).toContain('data-priority="${option.priority}" style="--linear-priority-option-index: ${index};"');
    expect(app).not.toContain('aria-label="${escapeAttribute(option.name)}"');
    expect(app).not.toContain('aria-label="${escapeAttribute(currentName)}"');
    expect(app).toContain("renderLinearPriorityIcon(option.priority, { empty: true })");
    expect(app).not.toContain('<span>${escapeHtml(option.name)}</span>');
    expect(app).toContain("void moveTicketToLinearPriority(issueId, priority);");
    expect(app).toContain("async function moveTicketToLinearPriority(issueId, priority)");
    expect(server).toContain("async function updateLinearIssuePriority(identifier: string, issueId: string, priority: number)");
    expect(server).toContain("issueUpdate(id: $id, input: { priority: $priority })");
    expect(server).toContain('parts[4] === "priority" && request.method === "POST"');
    expect(app).not.toContain('issue.priority ? `P${issue.priority}` : ""');
    expect(app).toContain('els.flowPane.addEventListener("mouseover", handleLinearOptionsOpen);');
    expect(app).toContain('els.flowPane.addEventListener("focusout", handleLinearOptionsClose);');
    expect(css).toContain(".linear-priority-pill,\n.linear-priority-option");
    expect(css).not.toContain(".linear-priority-pill .ticket-priority,\n.linear-priority-option .ticket-priority");
    expect(css).toContain(".linear-meta > span:not(.linear-status-control):not(.linear-priority-control),\n.linear-status-pill,\n.linear-status-option,\n.linear-priority-pill,\n.linear-priority-option,\n.github-ci-pill,\n.linear-comments > header span");
    expect(css).toContain("display: inline-flex;\n  align-items: center;");
    expect(css).toContain(".linear-meta > span:not(.linear-status-control):not(.linear-priority-control),\n.linear-status-pill,\n.linear-status-option,\n.linear-priority-pill,\n.linear-priority-option,\n.github-ci-pill {\n  user-select: none;\n  -webkit-user-select: none;\n}");
    expect(css).not.toContain(".linear-meta span,\n.linear-comments > header span");
  });

  test("edits the Linear pane title inline and saves it to Linear", () => {
    expect(app).toContain("editingLinearTitleIssueId: \"\"");
    expect(app).toContain("data-linear-title-edit=\"true\"");
    expect(app).toContain("data-linear-title-form=\"true\"");
    expect(app).toContain("handleLinearTitleSubmit");
    expect(app).toContain("handleLinearTitleKeydown");
    expect(app).toContain("handleLinearTitleOutsidePointerDown");
    expect(app).toContain('event.key !== "Escape"');
    expect(app).toContain("async function updateLinearIssueTitle(issueId, title)");
    expect(app).toContain('api(`/api/linear/issues/${encodeURIComponent(issueId)}/title`');
    expect(server).toContain("async function updateLinearIssueTitle(identifier: string, issueId: string, title: string)");
    expect(server).toContain("issueUpdate(id: $id, input: { title: $title })");
    expect(server).toContain('parts[4] === "title" && request.method === "POST"');
    expect(css).toContain(".linear-title-edit");
    expect(css).toContain("field-sizing: content;");
    expect(css).toContain("border: 0;\n  background: transparent;\n  color: var(--ink);");
  });

  test("does not render the Linear team key as a redundant metadata pill", () => {
    expect(app).not.toContain("issue.team?.key || issue.team?.name");
  });

  test("does not render empty Linear descriptions", () => {
    expect(app).toContain('const issueDescription = issue.description || "";');
    expect(app).toContain("const descriptionHtml = issueDescription.trim()");
    expect(app).toContain('renderLinearMarkdown(issueDescription, "")');
    expect(app).toContain("${descriptionHtml}");
    expect(app).not.toContain('renderLinearMarkdown(issue.description, "")');
    expect(app).not.toContain("No description.");
    expect(app).not.toContain("Loading issue details.");
  });

  test("labels user agent messages with the Linear viewer name", () => {
    expect(app).toContain('linearViewerName: "",');
    expect(app).toContain('state.linearViewerName = linear.viewerName || state.linearViewer?.name || "";');
    expect(app).toContain("state.linearViewerName = data.viewer?.name || state.linearViewerName;");
    expect(app).toContain('const userLabel = state.linearViewer?.name || state.linearViewerName || "user";');
    expect(app).toContain("function agentSenderLabel()");
    expect(app).toContain('return model.replace(/^claude-/, "");');
    expect(app).toContain('user: { label: userLabel, marker: ">", tone: "user" }');
    expect(app).not.toContain('user: { label: "user", marker: ">", tone: "user" }');
  });

  test("lets ticket selection animate without rebuilding unchanged cards", () => {
    expect(app).toContain("dataset.ticketSignature");
    expect(app).toContain("const active = card.dataset.issue === state.selectedLinearIssueId;");
    expect(app).toContain('card.classList.toggle("active", active);');
    expect(css).toContain(".ticket-card::before");
    expect(css).toContain(".ticket-card:hover");
    expect(css).toContain("transform var(--motion-fast)");
    expect(css).toContain("background-color var(--motion-fast)");
  });

  test("shows ticket id on cards and GitHub before Linear metadata in the Flow pane metadata row", () => {
    expect(app).toContain('class="ticket-meta"');
    expect(app).toContain('class="ticket-id"');
    expect(app).toContain('<span class="ticket-id">${escapeHtml(ticket.identifier)}</span>');
    expect(app).toContain("const statusName = issue.state?.name || context.ticket?.state?.name || \"\";");
    expect(app).toContain("const statusControl = renderLinearStatusControl(issue, statusName);");
    expect(app).toContain("const githubCiPill = renderGithubCiPill(context.flow);");
    expect(app).toContain('${githubCiPill}\n        ${meta.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}\n        ${statusControl}\n        ${priorityControl}');
    expect(app).not.toContain('const labels = issue.labels?.nodes || [];');
    expect(app).not.toContain('issue.assignee?.name ? `Assignee: ${issue.assignee.name}` : "",');
    expect(app).not.toContain('${labels.map((label) => `<span>${escapeHtml(label.name)}</span>`).join("")}');
    expect(css).not.toContain(".ticket-status {");
    expect(app).not.toContain("ticket-linear-status");
    expect(css).not.toContain("ticket-linear-status");
    expect(css).not.toContain(".linear-meta > span,\n.linear-status-pill,\n.linear-status-option,\n.linear-comments > header span");
    expect(css).toContain("--ticket-card-title-y: 8px;");
    expect(css).toContain("padding: var(--ticket-card-title-y) 10px 7px;");
    expect(css).toContain("grid-template-columns: minmax(0, 1fr) auto;");
    expect(css).toContain("grid-template-rows: auto minmax(0, 1fr) auto;");
    expect(css).toContain(".ticket-drawer h2 {\n  margin: 0;\n  color: var(--ink);\n  font-size: 15px;\n  font-weight: 700;");
    expect(css).toContain(".ticket-flow-corner {\n  position: absolute;\n  right: 8px;\n  top: var(--ticket-card-title-y);");
    expect(css).toContain(".ticket-id {\n  position: absolute;\n  right: 10px;\n  bottom: 7px;");
    expect(css).toContain(".ticket-card .ticket-title {\n  grid-column: 1 / -1;\n  grid-row: 1;");
    expect(css).toContain("color: var(--ink);\n  font-size: 12px;\n  line-height: 1.25;\n  font-weight: 650;");
    expect(css).toContain(".ticket-card.in-flow .ticket-title {\n  padding-right: 32px;\n}");
    expect(css).toContain(".ticket-meta {\n  grid-column: 1 / -1;\n  grid-row: 3;");
    expect(css).toContain(".ticket-card > .ticket-meta {\n  align-self: stretch;\n  padding-right: 88px;\n}");
  });

  test("lets the Linear pane status pill edit issue status", () => {
    expect(app).toContain('return ticket?.state?.name || "No status";');
    expect(app).toContain('return ticket?.state?.id || "";');
    expect(app).toContain('return ticket?.state?.type || "";');
    expect(app).toContain('return ticket?.team?.id || "";');
    expect(app).toContain("function linearStatusOptions(issue = null, fallbackStatus = null)");
    expect(app).toContain("linearWorkflowStates: []");
    expect(app).toContain("if (Array.isArray(data.workflowStates)) state.linearWorkflowStates = data.workflowStates;");
    expect(app).toContain('linearStatusKey(status.name) === "in-review" && (!teamId || status.team?.id === teamId)');
    expect(app).toContain("addStatus(status.id, status.name, status.type)");
    expect(app).toContain("function renderLinearStatusControl(issue, statusName)");
    expect(app).toContain("function renderLinearStatusPillContent(status, type = \"\")");
    expect(app).toContain('<span>${escapeHtml(label)}</span><span class="linear-status-icon linear-status-icon-${kind}" aria-hidden="true">');
    expect(app).toContain("${renderLinearStatusPillContent(option.status, option.type)}</button>");
    expect(app).toContain("${renderLinearStatusPillContent(statusName, currentStatusType)}</button>");
    expect(app).toContain('data-linear-status-pill="true"');
    expect(app).toContain('data-linear-status-option="true"');
    expect(app).toContain('class="linear-status-options"');
    expect(app).toContain("void moveTicketToLinearStatus(issueId, { stateId, status });");
    expect(app).toContain('els.flowPane.addEventListener("click", handleLinearDetailClick);');
    expect(app).toContain("focusLinearTicketCard(issueId);");
    expect(app).toContain("function showFloatingLinearOptions(control)");
    expect(app).toContain('let floatingLinearOptionsKey = "";');
    expect(app).toContain('const key = `${options.className}:${options.innerHTML}`;');
    expect(app).toContain("if (floatingLinearOptions && floatingLinearOptionsKey === key)");
    expect(app).toContain('menu?.classList.remove("is-open");');
    expect(app).toContain('requestAnimationFrame(() => floatingLinearOptions?.classList.add("is-open"));');
    expect(app).toContain('options.classList.contains("linear-status-options") ? "linear-floating-status-options" : "linear-floating-priority-options"');
    expect(app).toContain('els.flowPane.addEventListener("mouseout", handleLinearOptionsClose);');
    expect(app).toContain('els.flowPane.addEventListener("focusin", handleLinearOptionsOpen);');
    expect(app).toContain('floatingLinearOptions.addEventListener("click", handleLinearDetailClick);');
    expect(app).toContain("const width = Math.min(menu.getBoundingClientRect().width || 240, window.innerWidth - 32);");
    expect(css).toContain(".linear-floating-options {\n  position: fixed;\n  z-index: 1001;");
    expect(css).toContain(".linear-floating-options.is-open {\n  clip-path: inset(0);\n  opacity: 1;\n  transform: translateY(0);\n}");
    expect(css).toContain("clip-path: inset(0 0 100% 0);");
    expect(css).toContain("clip-path: inset(0);");
    expect(css).toContain(".linear-floating-status-options {\n  width: max-content;\n}");
    expect(css).toContain(".linear-floating-status-options .linear-status-option {\n  box-sizing: border-box;\n  width: 100%;\n  justify-content: flex-start;\n}");
    expect(css).toContain(".linear-floating-status-options .linear-status-option .linear-status-icon {\n  order: -1;\n}");
    expect(css).toContain("background: var(--panel);\n  box-shadow: 0 12px 28px rgb(15 23 42 / 18%);");
    expect(css).toContain(".linear-status-control {\n  margin: -6px -10px;\n  padding: 6px 10px;\n}");
    expect(css).toContain(".linear-status-pill,\n.linear-status-option,\n.linear-priority-pill,\n.linear-priority-option,\n.github-ci-pill {");
    expect(css).toContain(".linear-meta > span:not(.linear-status-control):not(.linear-priority-control)");
    expect(css).toContain("padding: 2px 7px;");
    expect(css).toContain("body.theme-dark .linear-status-pill,\nbody.theme-dark .linear-status-option,\nbody.theme-dark .linear-priority-pill,\nbody.theme-dark .linear-priority-option,\nbody.theme-dark .github-ci-pill {\n  background: #151a20;\n  color: var(--muted);\n}");
    expect(css).toContain("font-weight: 400;");
    expect(css).toContain(".linear-status-pill .linear-status-icon,\n.linear-status-option .linear-status-icon");
  });

  test("shows Linear ticket status and priority icons before the project at the bottom left", () => {
    expect(app).toContain("function renderLinearPriorityIcon(priority, options = {})");
    expect(app).toContain("function linearPriorityBarCount(priority)");
    expect(app).toContain("if (value === 1) return 3;");
    expect(app).toContain("if (value === 2) return 3;");
    expect(app).toContain("const urgent = Number(priority) === 1;");
    expect(app).toContain('<span class="ticket-priority${urgent ? " urgent" : ""}">');
    expect(app).not.toContain('<span class="ticket-priority${urgent ? " urgent" : ""}" aria-label=');
    expect(app).toContain('<svg viewBox="0 0 16 14" aria-hidden="true" focusable="false">');
    expect(app).toContain("${renderLinearStatusIcon(ticket)}\n      ${renderLinearPriorityIcon(ticket.priority)}");
    expect(app).toContain("${renderLinearPriorityIcon(ticket.priority)}");
    expect(app).not.toContain('const priorityName = ticket.priority ? `P${ticket.priority}` : "";');
    expect(app).not.toContain('<span class="ticket-priority">${escapeHtml(priorityName)}</span>');
    expect(app.indexOf("${renderLinearStatusIcon(ticket)}")).toBeLessThan(app.indexOf("${renderLinearPriorityIcon(ticket.priority)}"));
    expect(app.indexOf("${renderLinearPriorityIcon(ticket.priority)}")).toBeLessThan(app.indexOf('class="ticket-project"'));
    expect(css).toContain(".ticket-meta {\n  grid-column: 1 / -1;\n  grid-row: 3;\n  display: flex;");
    expect(css).toContain("justify-content: flex-start;");
    expect(css).toContain(".ticket-meta .linear-status-icon {\n  width: 13px;");
    expect(css).toContain(".ticket-meta .linear-status-icon-glyph {\n  width: 13px;");
    expect(css).toContain(".ticket-priority {\n  display: inline-grid;");
    expect(css).toContain(".ticket-priority.urgent {\n  color: var(--danger);\n}");
    expect(css).toContain(".ticket-priority svg");
    expect(css).toContain("fill: currentColor;\n  stroke: none;\n  stroke-width: 0;");
  });

  test("supports client-side pinned Linear tickets at the top of the drawer", () => {
    expect(app).toContain('const PINNED_LINEAR_ISSUES_KEY = "flow.pinnedLinearIssues";');
    expect(app).toContain("function initialPinnedLinearIssues()");
    expect(app).toContain("pinnedLinearIssues: initialPinnedLinearIssues(),");
    expect(app).toContain("const pinnedTickets = orderedPinnedTickets(tickets).filter((ticket) => !searchQuery || ticketMatchesTicketSearch(ticket, searchQuery));");
    expect(app).toContain("const nodes = [renderPinnedTicketGroup(pinnedTickets)];");
    expect(app).toContain('section.className = "ticket-status-group pinned-ticket-group";');
    expect(app).toContain("section.addEventListener(\"drop\", handlePinnedTicketDrop);");
    expect(app).toContain("function setLinearIssuePinned(identifier, pinned, options = {})");
    expect(app).toContain('if (pinned && options.position === "top") {');
    expect(app).toContain("state.pinnedLinearIssues = new Set([identifier, ...[...state.pinnedLinearIssues].filter((issueId) => issueId !== identifier)]);");
    expect(app).toContain("function renderLinearPinButton(issue)");
    expect(app).toContain('data-linear-pin-toggle="true"');
    expect(app).toContain('const pinned = !isLinearIssuePinned(issueId);');
    expect(app).toContain('setLinearIssuePinned(issueId, pinned, pinned ? { position: "top" } : {});');
    expect(app).toContain("updateLinearPinButtons(issueId);");
    expect(app).toContain("function ticketCanMoveToPinned(issueId)");
    expect(app).toContain("if (isLinearIssuePinned(issueId)) {");
    expect(app).not.toContain('class="ticket-pin"');
    expect(css).toContain(".pinned-ticket-separator");
    expect(css).toContain(".pinned-ticket-group .ticket-status-group-items");
    expect(css).toContain(".ticket-card.pinned");
    expect(css).toContain(".linear-issue-kicker");
    expect(css).toContain(".linear-pin-toggle");
    expect(css).toContain("min-height: 18px;\n  padding: 0;\n  border: 0;\n  border-radius: 0;\n  background: transparent;");
    expect(css).toContain("body.theme-dark .linear-pin-toggle,\nbody.theme-dark .linear-pin-toggle:hover,\nbody.theme-dark .linear-pin-toggle:focus-visible,\nbody.theme-dark .linear-pin-toggle.active {\n  background: transparent;\n}");
    expect(css).toContain(".linear-pin-icon path {\n  fill: none;");
    expect(css).toContain(".linear-pin-toggle.active .linear-pin-icon path {\n  fill: currentColor;\n}");
    expect(css).not.toContain(".ticket-pin {");
  });

  test("lets pinned Linear tickets be manually sorted", () => {
    expect(app).toContain("function orderedPinnedTickets(tickets)");
    expect(app).toContain(".concat(\"\\u001d\", [...state.pinnedLinearIssues].join(\"\\u001f\"))");
    expect(app).toContain(".concat(\"\\u001d\", state.ticketSearchOpen ? \"search-open\" : \"search-closed\", \"\\u001f\", searchQuery)");
    expect(app).toContain("function setPinnedLinearIssueOrder(identifiers)");
    expect(app).toContain("function moveLinearIssueToPinnedPosition(identifier, index)");
    expect(app).toContain("function pinnedTicketDropIndex(event)");
    expect(app).toContain('event.currentTarget.querySelectorAll(".ticket-card.pinned:not(.dragging)")');
    expect(app).toContain("function updatePinnedTicketDropTarget(event)");
    expect(app).toContain("updatePinnedTicketDropTarget(event);");
    expect(app).toContain("moveLinearIssueToPinnedPosition(issueId, index);");
    expect(css).toContain(".ticket-card.drop-before");
    expect(css).toContain(".ticket-card.drop-after");
  });

  test("marks tickets with flows using the favicon instead of a green card tint", () => {
    expect(app).toContain('const DEFAULT_FAVICON_HREF = "/favicon.svg";');
    expect(app).toContain('class="ticket-flow-mark"');
    expect(app).toContain('class="ticket-flow-corner"');
    expect(app).toContain('src="${DEFAULT_FAVICON_HREF}" alt="In flow"');
    expect(app).not.toContain("ticket-stage");
    expect(css).toContain(".ticket-flow-mark");
    expect(css).toContain(".ticket-flow-corner");
    expect(css).not.toContain(".ticket-stage");
    expect(css).not.toContain(".ticket-card.in-flow {\n  border-color:");
    expect(css).not.toContain(".ticket-card.in-flow:hover");
  });

  test("shows a hover start-agent button for tickets without sessions", () => {
    expect(app).toContain('card.classList.toggle("can-start-agent", !ticket.flowId && !creatingFlow);');
    expect(app).toContain(": renderTicketStartAgentButton(ticket)");
    expect(app).toContain("function renderTicketStartAgentButton(ticket)");
    expect(app).toContain("const TICKET_START_AGENT_FLAME_PATH =");
    expect(app).toContain("const TICKET_START_AGENT_FLAME_ANIMATION_VALUES = [");
    expect(app).toContain('class="ticket-flow-corner ticket-flow-start"');
    expect(app).toContain('data-ticket-start-agent="true"');
    expect(app).toContain('aria-label="Start agent session"');
    expect(app).not.toContain('title="Start agent session"');
    expect(app).toContain('class="ticket-flow-start-icon"');
    expect(app).toContain('d="${TICKET_START_AGENT_FLAME_PATH}"');
    expect(app).toContain('class="ticket-flow-start-shape"');
    expect(app).toContain('values="${TICKET_START_AGENT_FLAME_ANIMATION_VALUES}" dur="1.8s" repeatCount="indefinite" begin="indefinite"');
    expect(app).toContain('startAgentButton?.addEventListener("mouseenter", () => updateTicketStartAgentAnimation(startAgentButton));');
    expect(app).toContain('startAgentButton?.addEventListener("mouseleave", () => updateTicketStartAgentAnimation(startAgentButton));');
    expect(app).toContain('startAgentButton?.addEventListener("focus", () => updateTicketStartAgentAnimation(startAgentButton));');
    expect(app).toContain('startAgentButton?.addEventListener("blur", () => updateTicketStartAgentAnimation(startAgentButton));');
    expect(app).toContain("function updateTicketStartAgentAnimation(button)");
    expect(app).toContain('const active = button.matches(":hover, :focus-visible");');
    expect(app).toContain("animation?.beginElement?.();");
    expect(app).toContain("animation?.endElement?.();");
    expect(app).toContain('path?.setAttribute("d", TICKET_START_AGENT_FLAME_PATH);');
    expect(app).toContain('startAgentButton?.addEventListener("click"');
    expect(app).toContain("event.stopPropagation();");
    expect(app).toContain("async function startTicketAgentSession(ticket)");
    expect(app).toContain('setLinearIssuePinned(ticket.identifier, true, { position: "top" });');
    expect(app).toContain("await openTicketInFlowPane(ticket);");
    expect(app).toContain('input.value = "get started on this. open a pr if you have a changeset";');
    expect(app).toContain("await submitPromptMessage();");
    expect(css).toContain(".ticket-card.can-start-agent .ticket-title {\n  padding-right: 32px;\n}");
    expect(css).toContain(".ticket-flow-start {");
    expect(css).toContain("opacity: 0;");
    expect(css).toContain("width: 18px;\n  height: 18px;");
    expect(css).toContain("min-width: 18px;\n  min-height: 18px;");
    expect(css).toContain("border: 0;\n  background: transparent;");
    expect(css).toContain("body.theme-dark .ticket-flow-start,\nbody.theme-dark .ticket-flow-start:hover,\nbody.theme-dark .ticket-flow-start:focus-visible {\n  background: transparent;\n}");
    expect(css).toContain("fill: none;");
    expect(css).toContain("transition: stroke-width var(--motion-fast);");
    expect(css).toContain(".ticket-flow-start:hover .ticket-flow-start-icon path,\n.ticket-flow-start:focus-visible .ticket-flow-start-icon path {\n  stroke-width: 5.5;\n}");
    expect(css).toContain(".ticket-card.can-start-agent:hover .ticket-flow-start,\n.ticket-card.can-start-agent:focus-within .ticket-flow-start");
    expect(css).toContain("pointer-events: auto;");
    expect(css).not.toContain("ticket-flow-start-wiggle");
    expect(css).not.toContain("ticket-flow-start-colors");
    expect(css).not.toContain("stroke-dasharray");
    expect(css).not.toContain("ticket-flow-start-dash");
    expect(css).toContain(".ticket-flow-start:active {\n  transform: scale(0.88);\n}");
  });

  test("shows a spinner while creating a new flow worktree", () => {
    expect(app).toContain("creatingFlowIssueIds: new Set(),");
    expect(app).toContain("function ticketFlowCreating(ticket)");
    expect(app).toContain("state.creatingFlowIssueIds.has(ticket.identifier)");
    expect(app).toContain('card.classList.toggle("creating-flow", ticketFlowCreating(ticket));');
    expect(app).toContain('card.classList.toggle("can-start-agent", !ticket.flowId && !creatingFlow);');
    expect(app).toContain("? renderTicketCreatingFlowIndicator(ticket)");
    expect(app).toContain("function renderTicketCreatingFlowIndicator(ticket)");
    expect(app).toContain('class="ticket-flow-corner ticket-flow-creating"');
    expect(app).toContain('class="flow-create-spinner"');
    expect(app).toContain("state.creatingFlowIssueIds.add(issueId);");
    expect(app).toContain("state.creatingFlowIssueIds.delete(issueId);");
    expect(app).toContain('terminal.innerHTML = \'<div class="flow-creating-message" role="status"><span class="flow-create-spinner" aria-hidden="true"></span><span>Creating worktree.</span></div>\';');
    expect(css).toContain(".ticket-flow-creating");
    expect(css).toContain(".ticket-card.creating-flow .ticket-flow-creating");
    expect(css).toContain(".flow-creating-message");
    expect(css).toContain(".flow-create-spinner");
    expect(css).toContain("animation: flow-create-spinner 900ms linear infinite;");
    expect(css).toContain("@keyframes flow-create-spinner");
  });

  test("grows the ticket favicon smoothly while the agent is in a turn", () => {
    expect(app).toContain("function ticketAgentWorking(ticket)");
    const ticketAgentWorking = app.slice(
      app.indexOf("function ticketAgentWorking(ticket)"),
      app.indexOf("function ticketShellRunning(ticket)"),
    );
    expect(ticketAgentWorking).toContain("flowAgentRunning(flow)");
    expect(ticketAgentWorking).toContain("state.messageSubmittingFlowId === flow.id");
    expect(ticketAgentWorking).not.toContain("flowRuntimeActive(flow)");
    expect(ticketAgentWorking).not.toContain("state.shellSubmitting");
    expect(ticketAgentWorking).not.toContain("state.interruptSubmitting");
    expect(app).toContain('card.classList.toggle("agent-turn-active", ticketAgentWorking(ticket));');
    expect(app).toContain("function updateTicketCardState(card)");
    expect(app).toContain("renderTickets();\n    renderFlowPane();");
    expect(css).toContain("width 180ms ease");
    expect(css).toContain("height 180ms ease");
    expect(css).toContain(".ticket-card.agent-turn-active .ticket-flow-mark");
    expect(css).toContain("width: 25.2px;");
    expect(css).toContain("height: 25.2px;");
  });

  test("shows a shell running dot on ticket favicons", () => {
    expect(app).toContain("function ticketShellRunning(ticket)");
    expect(app).toContain("return flowShellLive(flowForTicket(ticket));");
    expect(app).toContain('card.classList.toggle("shell-command-active", ticketShellRunning(ticket));');
    expect(app).toContain('section.classList.toggle("shell-command-active", group.tickets.some(ticketShellRunning));');
    expect(app).toContain('section.classList.toggle("shell-command-active", tickets.some(ticketShellRunning));');
    expect(app).toContain("function updateTicketStatusGroupShellStates()");
    expect(app).not.toContain("SHELL_FAVICON_HREF");
    expect(css).toContain(".ticket-card.shell-command-active .ticket-flow-corner::after");
    expect(css).toContain('.ticket-status-group[data-collapsed="true"].shell-command-active .ticket-status-count::after');
    expect(css).toContain("right: 2px;");
    expect(css).toContain("bottom: 1px;");
    expect(css).toContain("right: -1px;");
    expect(css).toContain("bottom: -1px;");
    expect(css).toContain("background: #22c55e;");
  });

  test("shows last updated copy instead of assigned ticket count", () => {
    expect(app).toContain("formatLastUpdated");
    expect(app).toContain("last updated:");
    expect(app).not.toContain("tickets assigned to");
  });

  test("keeps Linear attachment comments compact", () => {
    expect(css).toContain(".linear-markdown:has(.linear-image)");
    expect(css).toContain("white-space: normal;");
    expect(css).toContain("width: fit-content;");
    expect(css).toContain("width: max-content;");
    expect(css).toContain(".linear-image a");
    expect(css).toContain("display: contents;");
    expect(css).toContain("max-width: min(100%, 760px);");
    expect(css).toContain(".linear-comment header");
    expect(css).toContain("justify-content: flex-start;");
    expect(css).toContain("align-items: baseline;");
    expect(css).not.toContain(".linear-comment {\n  display: grid;\n  justify-items: start;\n  gap: 7px;\n  border-top:");
  });

  test("opens Markdown images in an in-app preview modal", () => {
    expect(html).toContain('class="image-preview-backdrop" id="imagePreviewModal" hidden');
    expect(html).toContain('class="image-preview-modal" role="dialog" aria-modal="true" aria-labelledby="imagePreviewTitle"');
    expect(html).not.toContain("image-preview-close");
    expect(markdown).toContain("data-image-preview");
    expect(markdown).not.toContain('target="_blank" rel="noreferrer"><img src="${escapeAttribute(imageSrc)}"');
    expect(app).toContain("imagePreviewModal: document.querySelector");
    expect(app).toContain("const MODAL_HIDE_DELAY_MS = 220;");
    expect(app).toContain("function showModalElement(element)");
    expect(app).toContain("void element.offsetWidth;");
    expect(app).toContain("function openImagePreview(src, alt = \"\")");
    expect(app).toContain("function handleImagePreviewWheel(event)");
    expect(app).toContain("const nextScale = Math.min(5, Math.max(1, imagePreviewScale + (event.deltaY < 0 ? 0.15 : -0.15)));");
    expect(app).toContain("const scaleRatio = nextScale / oldScale;");
    expect(app).toContain("imagePreviewOffsetX += (event.clientX - baseCenterX - imagePreviewOffsetX) * (1 - scaleRatio);");
    expect(app).toContain("function handleImagePreviewPointerDown(event)");
    expect(app).toContain("function handleImagePreviewPointerMove(event)");
    expect(app).toContain("function endImagePreviewDrag(event)");
    expect(app).toContain('addEventListener("wheel", handleImagePreviewWheel, { passive: false })');
    expect(app).toContain('addEventListener("pointerdown", handleImagePreviewPointerDown)');
    expect(app).toContain('const target = event.target.closest?.("[data-image-preview]");');
    expect(app).toContain('els.flowPane.addEventListener("click", handleImagePreviewClick);');
    expect(app).not.toContain(".image-preview-close");
    expect(app).toContain('if (event.key === "Escape" && els.imagePreviewModal && !els.imagePreviewModal.hidden)');
    expect(css).toContain(".image-preview-backdrop");
    expect(css).toContain(".image-preview-backdrop.is-open");
    expect(css).toContain(".image-preview-backdrop.is-closing");
    expect(css).toContain("padding: 8px 14px;");
    expect(css).toContain(".image-preview-frame img");
    expect(css).toContain("transform: translate(var(--image-preview-x, 0), var(--image-preview-y, 0)) scale(var(--image-preview-scale, 1));");
    expect(css).toContain("transition: transform 140ms ease-out;");
    expect(css).toContain(".image-preview-backdrop.is-dragging .image-preview-frame img");
  });

  test("renders Markdown headings, inline formatting, and code blocks in Linear and Agent panes", () => {
    expect(app).toContain('import { renderInlineMarkdown, renderLinearMarkdown } from "../linear-markdown.js";');
    expect(app).toContain("function usesTerminalBlockMarkdown(source)");
    expect(app).toContain('["user", "user:queued", "agent", "agent:message", "agent:thinking", "agent:reasoning"].includes(source)');
    expect(app).toContain('document.createElement(usesTerminalBlockMarkdown(group.source) ? "div" : "pre")');
    expect(app).toContain("function usesTerminalMarkdownToggle(source)");
    expect(app).toContain('["agent", "agent:message"].includes(source)');
    expect(app).toContain("function renderTerminalMarkdownOutput(message, options = {})");
    expect(app).toContain('markdownToggle.className = "terminal-markdown-toggle";');
    expect(app).toContain('markdownToggle.setAttribute("aria-pressed", "false");');
    expect(app).toContain('markdownToggle.textContent = "raw";');
    expect(app).toContain("function terminalContentGroups(groups, result = [])");
    expect(app).toContain("function markPendingTerminalTurnGroups(groups, flow)");
    expect(app).toContain("if (isLiveAgentTextSource(contentGroups[index].source)) contentGroups[index].turnPending = true;");
    expect(app).toContain("markPendingTerminalTurnGroups(visibleGroups, flow);");
    expect(app).toContain("markLiveTerminalGroup(visibleGroups, flow);");
    expect(app).toContain('return `<div class="terminal-markdown-content" data-raw-markdown="${escapeAttribute(message)}">${renderTerminalMarkdownContent(message, options)}</div>`;');
    expect(app).toContain("function renderTerminalStreamingTextOutput(message)");
    expect(app).toContain('class="terminal-streaming-markdown"');
    expect(app).toContain('return `<div class="terminal-streaming-markdown">${renderLinearMarkdown(message, "", {');
    expect(app).toContain("compactBlankLines: true");
    expect(app).toContain("copyCode: false");
    expect(app).toContain("body.innerHTML = renderTerminalStreamingTextOutput(message);\n      highlightCodeBlocks(body);");
    expect(app).not.toContain("parts.push(renderTextWithSentenceBreaks(line));");
    expect(app).toContain("preserveTrailingNewlines: Boolean(group.liveStreaming)");
    expect(app).toContain("renderTerminalMarkdownOutput(message, { copyCode: !group.turnPending })");
    expect(app).toContain("if (!group.liveStreaming && !group.turnPending && usesTerminalMarkdownToggle(group.source))");
    expect(app).toContain("header.replaceChildren(marker, label, ...(markdownToggle ? [markdownToggle] : []), ...(time ? [time] : []));");
    expect(app).toContain("function toggleTerminalMarkdownOutput(button)");
    expect(app).toContain('event.target.closest(".terminal-markdown-toggle")');
    expect(app).toContain("if (event.detail > 0) toggle.blur();");
    expect(app).toContain('button.setAttribute("aria-pressed", String(showingRaw));');
    expect(app).toContain('button.textContent = "raw";');
    expect(app).not.toContain('button.textContent = showingRaw ? "Rendered" : "Raw";');
    expect(app).toContain("function highlightCodeBlocks(root)");
    expect(app).toContain("window.Prism?.highlightAllUnder?.(root);");
    expect(app).toContain('event.target.closest("[data-code-copy]")');
    expect(app).toContain("void copyMarkdownCodeBlock(copyButton);");
    expect(app).toContain("async function copyMarkdownCodeBlock(button)");
    expect(app).toContain('button.closest(".markdown-code-block")?.querySelector("code")?.textContent || ""');
    expect(app).not.toContain('toast("Code copied");');
    expect(app).toContain('toast("Could not copy code", { kind: "error" });');
    expect(app).toContain("const MARKDOWN_CODE_COPY_OFFSET = 6;");
    expect(app).toContain("const MARKDOWN_CODE_COPY_SIZE = 24;");
    expect(app).toContain("function updateMarkdownCodeCopyPositions(root = els.flowPane)");
    expect(app).toContain('for (const block of root.querySelectorAll?.(".markdown-code-block") || [])');
    expect(app).toContain("const clipTop = markdownCodeBlockClipTop(block);");
    expect(app).toContain("button.style.setProperty(\"--markdown-code-copy-top\", `${top}px`);");
    expect(app).toContain("function markdownCodeBlockClipTop(block)");
    expect(app).toContain("function scheduleMarkdownCodeCopyPositionUpdate(root = els.flowPane)");
    expect(app).toContain("function shouldHideFloatingLinearOptionsForScroll(event)");
    expect(app).toContain('Boolean(target?.closest(".linear-panel, .linear-detail"))');
    expect(app).toContain("if (shouldHideFloatingLinearOptionsForScroll(event)) hideFloatingLinearOptions();");
    expect(app).toContain("function splitTerminalAttachedImages(message)");
    expect(app).toContain('const marker = "\\n\\nAttached images:\\n";');
    expect(app).toContain("function renderTerminalAttachedImages(images)");
    expect(app).toContain('class="terminal-attached-images"');
    expect(app).toContain("function agentImagePreviewSource(image)");
    expect(app).toContain('data-image-preview data-image-preview-src="${escapeAttribute(previewSrc)}" data-image-preview-alt="${escapeAttribute(label)}"');
    expect(app).toContain('/context-images/preview?path=${encodeURIComponent(path)}');
    expect(app).toContain('<span class="agent-image-chip"${previewAttrs}>');
    expect(css).toContain(".agent-image-chip[data-image-preview]");
    expect(server).toContain("async function serveFlowContextImage(flow: Flow, rawPath: string)");
    expect(server).toContain('parts[3] === "context-images" && parts[4] === "preview" && request.method === "GET"');
    expect(css).toContain("cursor: pointer;");
    expect(app).toContain("function renderTerminalMarkdownContent(message, options = {})");
    expect(app).toContain("renderTerminalMarkdownContent(raw)");
    expect(app).toContain("copyCode: options.copyCode !== false");
    expect(app).toContain("const MARKDOWN_TABLE_MIN_COLUMN_WIDTH = 72;");
    expect(app).toContain("function startMarkdownTableColumnResize(event)");
    expect(app).toContain('els.flowPane.addEventListener("pointerdown", startMarkdownTableColumnResize);');
    expect(app).not.toContain("body.textContent = formatTerminalMessage(group.source, group.message);");
    expect(html).toContain('<script src="/vendor/prismjs/prism.js" data-manual></script>');
    expect(html).toContain('<script src="/vendor/prismjs/components/prism-typescript.min.js"></script>');
    expect(server).toContain('path.startsWith("/vendor/prismjs/")');
    expect(packageJson.dependencies.prismjs).toBeDefined();
    expect(css).toContain(".linear-markdown h1,");
    expect(css).toContain("margin: 6px 0 2px;");
    expect(css).toContain(".linear-markdown a,\n.terminal-entry-body a");
    expect(css).toContain("color: #3b82f6;\n  font-weight: 700;\n  overflow-wrap: anywhere;\n  text-decoration: none;");
    expect(css).toContain("body.theme-dark .linear-markdown a,\nbody.theme-dark .terminal-entry-body a {\n  color: #93c5fd;\n}");
    expect(css).toContain(".linear-markdown table,\n.terminal-entry-body table {\n  display: block;");
    expect(css).toContain("  border: 1px solid var(--line);\n  border-collapse: separate;\n  border-spacing: 0;\n  border-radius: 4px;");
    expect(css).toContain(".terminal-entry-body table {\n  margin-bottom: 2px;\n}");
    expect(css).toContain(".terminal-entry-body table + br {\n  display: none;\n}");
    expect(css).toContain(".linear-markdown th,\n.linear-markdown td,\n.terminal-entry-body th,\n.terminal-entry-body td");
    expect(css).toContain("  min-width: 0;\n  border-right: 1px solid var(--line);\n  border-bottom: 1px solid var(--line);\n  padding: 4px 8px;");
    expect(css).toContain(".linear-markdown .markdown-resizable-table th,\n.terminal-entry-body .markdown-resizable-table th");
    expect(css).toContain("overflow-wrap: normal;\n  word-break: normal;");
    expect(css).toContain(".markdown-table-column-resizer");
    expect(css).toContain("cursor: col-resize;");
    expect(css).toContain("body.theme-dark .markdown-table-column-resizer,\nbody.theme-dark .markdown-table-column-resizer:hover,\nbody.theme-dark .markdown-table-column-resizer:focus-visible");
    expect(css).toContain("body.markdown-table-resizing");
    expect(css).toContain("body.theme-dark .linear-markdown th,\nbody.theme-dark .terminal-entry-body th");
    expect(css).toContain(".terminal-entry-body td {\n  font-size: 11px;\n}");
    expect(css).toContain(".linear-markdown ul,\n.linear-markdown ol {\n  margin: 4px 0 4px 18px;");
    expect(css).toContain(".terminal-entry-body ul,\n.terminal-entry-body ol {\n  margin: 4px 0;\n  padding-left: 3ch;");
    expect(css).toContain(".terminal-entry-body ul {\n  padding-left: 2ch;");
    expect(css).toContain(".terminal-entry-body li > ul,\n.terminal-entry-body li > ol {\n  margin: 2px 0;\n  padding-left: 4ch;");
    expect(css).toContain(".linear-markdown li,\n.terminal-entry-body li");
    expect(css).toContain(".linear-markdown code,\n.terminal-entry-body code");
    expect(css).toContain("body.theme-dark .linear-markdown .markdown-code-block code,\nbody.theme-dark .terminal-entry-body .markdown-code-block code");
    expect(css).toContain("background: transparent;");
    expect(css).toContain(".linear-markdown .markdown-code-copy,\n.terminal-entry-body .markdown-code-copy");
    expect(css).toContain("position: absolute;\n  top: var(--markdown-code-copy-top, 6px);\n  right: 6px;");
    expect(css).toContain("margin: 0;");
    expect(css).toContain("min-width: 24px;\n  min-height: 24px;");
    expect(css).toContain("padding: 0;\n  aspect-ratio: 1;");
    expect(css).toContain("stroke-width: 1.4;");
    expect(css).toContain("overflow: visible;\n  white-space: normal;");
    expect(css).toContain("display: block;\n  overflow-x: auto;");
    expect(css).not.toContain(".terminal-entry-body-content:has(.markdown-code-copy)");
    expect(css).toContain("opacity: 0;\n  transition:");
    expect(css).toContain(".linear-markdown .markdown-code-block:hover .markdown-code-copy");
    expect(css).toContain(".terminal-entry-body .markdown-code-copy:focus-visible");
    expect(css).not.toContain(".terminal-markdown-output {\n  position: relative;");
    expect(css).toContain(".terminal-markdown-content {\n  display: contents;");
    expect(css).toContain(".terminal-entry-body h1,\n.terminal-entry-body h2,");
    expect(css).toContain(".terminal-streaming-markdown {\n  white-space: normal;");
    expect(css).toContain("--terminal-message-max-lines: 50;");
    expect(css).toContain("--terminal-message-max-height: 77.5em;");
    expect(css).toContain("max-height: var(--terminal-message-max-height);");
    expect(css).toContain(".shell-output-pane .terminal-entry-body-content {\n  max-height: none;\n  overflow: visible;\n}");
    expect(css).toContain(".shell-output-pane .terminal-entry-overflow-marker {\n  display: none;\n}");
    expect(css).not.toContain(".terminal-entry-assistant .terminal-entry-body-content");
    expect(css).not.toContain(".terminal-entry-assistant .terminal-entry-overflow-marker");
    expect(css).toContain(".terminal-entry-body-content");
    expect(css).toContain(".terminal-entry-overflow-marker");
    expect(app).toContain("function applyTerminalMessageClamps(root)");
    expect(app).toContain("function terminalMessageContent(body)");
    expect(app).toContain('if (root.closest?.(".shell-output-pane")) return;');
    expect(app).toContain('body.closest(".shell-output-pane")');
    expect(app).toContain('entry.classList.contains("terminal-entry-assistant") || entry.classList.contains("terminal-entry-output")');
    expect(app).toContain('!entry.closest(".terminal-trace-body")');
    expect(app).toContain('marker.textContent = ". . .";');
    expect(app).toContain("body.appendChild(marker);");
    expect(app).toContain("applyTerminalMessageClamps(terminal);");
    expect(app).not.toContain("applyTerminalMessageClamps(pane);");
    expect(css).toContain(".terminal-markdown-toggle {\n  flex: 0 0 auto;");
    expect(css).toContain("  min-height: 18px;\n  padding: 0 6px;");
    expect(css).toContain(".terminal-entry:hover .terminal-markdown-toggle");
    expect(css).toContain('.terminal-markdown-toggle[aria-pressed="true"] {\n  border-color: var(--accent);\n  background: rgba(59, 130, 246, 0.12);\n  color: var(--accent);\n}');
    expect(css).toContain(".terminal-raw-markdown {\n  margin: 0;");
    expect(css).toContain(".terminal-entry-body .markdown-blank-line {\n  display: block;\n  height: 4px;\n}");
    expect(css).toContain(".linear-markdown .token.keyword,");
    expect(css).toContain("body.theme-dark .linear-markdown .token.keyword,");
    expect(markdown).toContain("<strong>");
    expect(markdown).toContain('data-code-copy="true"');
    expect(markdown).toContain('aria-label="Copy code"');
    expect(renderLinearMarkdown("```ts\nconst value = 1;\n```")).toContain('class="language-typescript"');
    expect(renderLinearMarkdown("[managedAgentFileLoader.ts](/Users/alex/project/file.ts:201)", "", { links: true })).toContain(
      "<code>managedAgentFileLoader.ts</code>",
    );
    expect(renderLinearMarkdown("[My File](</Users/alex/My Project/file.ts:201>)", "", { links: true })).toContain(
      "<code>My File</code>",
    );
    expect(css).toContain(".terminal-entry-body .markdown-code-block");
  });

  test("groups Linear tickets behind collapsible status separators", () => {
    expect(app).toContain("collapsedLinearStatuses");
    expect(app).toContain('DEFAULT_COLLAPSED_LINEAR_STATUSES = ["backlog", "canceled"]');
    expect(app).toContain('LINEAR_STATUS_ORDER = ["in-review", "in-eng", "ready-for-eng", "triage", "backlog", "canceled"]');
    expect(app).toContain("linearStatusRank");
    expect(app).toContain("groupedTicketsByLinearStatus");
    expect(app).toContain("renderTicketStatusGroup");
    expect(app).toContain("renderTicketStatusSeparator");
    expect(app).toContain('separator.setAttribute("aria-expanded"');
    expect(app).toContain("group.dataset.collapsed");
    expect(app).toContain("section.dataset.selectedTicketVisible = String(selectedTicketVisible);");
    expect(app).toContain("const selectedTicketVisible = group.collapsed && group.tickets.some((ticket) => ticket.identifier === state.selectedLinearIssueId);");
    expect(app).toContain("const visibleTickets = selectedTicketVisible");
    expect(app).toContain("group.tickets.filter((ticket) => ticket.identifier === state.selectedLinearIssueId)");
    expect(app).toContain("function ticketInCollapsedStatusGroup(identifier)");
    expect(app).toContain("function updateTicketSelectionCards(previousIssueId, nextIssueId)");
    expect(app).toContain("if (ticketInCollapsedStatusGroup(previousIssueId) || ticketInCollapsedStatusGroup(nextIssueId))");
    expect(app).toContain("updateTicketSelectionCards(previousIssueId, flow.linearIssueId);");
    expect(app).toContain("updateTicketSelectionCards(previousIssueId, ticket.identifier);");
    expect(app).toContain('.concat("\\u001d", state.selectedLinearIssueId);');
    expect(css).toContain(".ticket-status-separator");
    expect(css).toContain(".ticket-status-separator::after");
    expect(css).toContain(".ticket-status-group-body");
    expect(css).toContain("grid-template-rows var(--motion-fast)");
    expect(css).toContain('[data-collapsed="true"] .ticket-status-group-body');
    expect(css).toContain('[data-collapsed="true"][data-selected-ticket-visible="true"] .ticket-status-group-body');
    expect(css).toContain('[data-collapsed="true"][data-selected-ticket-visible="true"] {\n  gap: 6px;\n}');
  });

  test("sorts Linear tickets within each status by agent session and priority", () => {
    expect(app).toContain("function ticketHasAgentSession(ticket)");
    expect(app).toContain("function linearPrioritySortRank(priority)");
    expect(app).toContain("function compareLinearTickets(a, b)");
    expect(app).toContain("Number(ticketHasAgentSession(b)) - Number(ticketHasAgentSession(a))");
    expect(app).toContain("linearPrioritySortRank(a.priority) - linearPrioritySortRank(b.priority)");
    expect(app).toContain("return [...tickets].sort(compareLinearTickets);");
  });

  test("lets ticket cards move between Linear status groups with drag and drop", () => {
    expect(app).toContain("draggingLinearIssueId");
    expect(app).toContain("function linearStatusId(ticket)");
    expect(app).toContain("section.dataset.stateId = group.stateId;");
    expect(app).toContain("handleTicketStatusDrop");
    expect(app).toContain("async function moveTicketToLinearStatus(issueId, group)");
    expect(app).toContain("function mergeLinearIssue(existing, incoming)");
    expect(app).toContain('api(`/api/linear/issues/${encodeURIComponent(issueId)}/status`');
    expect(app).toContain("card.draggable = true;");
    expect(app).toContain('event.dataTransfer.setData("text/plain", ticket.identifier);');
    expect(app).toContain("const TICKET_DRAG_SCROLL_EDGE_PX = 72;");
    expect(app).toContain("function updateTicketDragAutoScroll(event)");
    expect(app).toContain('els.ticketGrid.addEventListener("dragover", handleTicketGridDragOver);');
    expect(app).toContain("els.ticketGrid.scrollTop += state.ticketDragScrollStep;");
    expect(app).toContain("function ticketWouldHighlightStatusGroup(issueId, group)");
    expect(app).toContain("linearStatusId(ticket) !== group.stateId");
    expect(app).toContain("function ticketWouldHighlightPinnedGroup(issueId)");
    expect(app).toContain("ticketCanMoveToPinned(issueId) && !isLinearIssuePinned(issueId)");
    expect(app).toContain("setTicketStatusGroupDragOver(event.currentTarget, ticketWouldHighlightStatusGroup(state.draggingLinearIssueId, group));");
    expect(app).toContain("setTicketStatusGroupDragOver(event.currentTarget, ticketWouldHighlightPinnedGroup(state.draggingLinearIssueId));");
    expect(css).toContain("user-select: none;");
    expect(css).toContain(".ticket-status-group.drag-over .ticket-status-separator");
    expect(css).toContain(".ticket-status-group.drag-over .ticket-status-separator::after");
    expect(css).toContain(".ticket-status-group.drag-over .ticket-status-count");
    expect(server).toContain("async function updateLinearIssueStatus");
    expect(server).toContain("issueUpdate(id: $id, input: { stateId: $stateId })");
    expect(server).toContain('parts[4] === "status"');
    expect(server).toContain("return json({ ok: true, ...result, flow: result.flow ? clientFlow(result.flow) : null });");
    expect(server).toContain("state { id name color type }");
    expect(server).toContain("workflowStates(first: 250) @include(if: $includeWorkflowStates)");
    expect(server).toContain("return assignedLinearIssuesPayload(viewer, issues, false, workflowStates);");
    expect(server).toContain("workflowStates,");
    expect(server).toContain("team { id key name }");
  });

  test("keeps ticket flow status synced when flows update", () => {
    expect(app).toContain("function setFlows(flows, options = {})");
    expect(app).toContain("const nextIds = new Set(nextFlows.map((flow) => flow.id));");
    expect(app).toContain("if (!nextIds.has(flow.id)) clearFlowClientState(flow.id);");
    expect(app).toContain('localStorage.removeItem("flow.selectedFlowId");');
    expect(app).toContain("function runtimeOnlyFlowChanges(previousFlows, nextFlows)");
    expect(app).toContain('"agentRuntimeStatus"');
    expect(app).toContain('"shellRuntimeStatus"');
    expect(app).toContain("if (runtimeOnlyFlowChanges(previousFlows, state.flows)) {\n        renderTickets();\n        const flow = selectedFlow();\n        if (flow) scheduleLogRender(flow.id, { force: true });\n        return;\n      }");
    expect(app).toContain("function syncLinearTicketsWithFlows()");
    expect(app).toContain("function flowUpdatedAtMs(flow)");
    expect(app).toContain("if (index !== -1 && flowUpdatedAtMs(flow) < flowUpdatedAtMs(next[index])) return;");
    expect(app).toContain("const flowsByIssue = new Map");
    expect(app).toContain("return { ...ticket, flowId };");
    expect(app).toContain("setFlows(message.payload);");
    expect(app).toContain("upsertFlow(data.flow);");
    expect(app).toContain("if (state.githubCiSelectedFlowId === flowId) state.githubCiSelectedFlowId = \"\";");
  });

  test("keeps the selected ticket attached to its flow while Linear tickets load", () => {
    expect(app).toContain("linearTicketsLoaded: false");
    expect(app).toContain("state.linearTicketsLoaded = true;");
    expect(app).toContain("function flowForLinearIssue(identifier)");
    expect(app).toContain("function flowForTicket(ticket)");
    expect(app).toContain("const flow = flowForTicket(ticket);");
    expect(app).toContain("flowForLinearIssue(state.selectedLinearIssueId)");
    expect(app).toContain("state.selectedFlowId = flow.id;");
    expect(app).toContain("!flow && !ticket && state.selectedLinearIssueId && state.linearTicketsLoaded");
    expect(app).toContain("renderTickets();\n    renderFlowPane();");
  });

  test("does not write flow status updates back to Linear comments", () => {
    expect(server).not.toContain("commentCreate");
    expect(server).not.toContain("Turbopump moved this flow");
    expect(server).not.toContain("commentLinearIssue");
  });

  test("shows context availability, model, reasoning effort, fast mode, and branch values on the right side of the Agent input", () => {
    expect(html).toContain('class="agent-context" aria-label="Current flow context" hidden');
    expect(html).toContain('class="agent-context-window"');
    expect(html).toContain('class="agent-context-model"');
    expect(html).toContain('class="agent-context-diff" type="button" hidden');
    expect(html).toContain('<a class="agent-context-branch"></a>');
    expect(html).toContain('class="diff-modal-backdrop" id="diffModal" hidden');
    expect(html).not.toContain('class="agent-context-phase"');
    expect(app).toContain("function agentModelLabel(flow)");
    expect(app).toContain("function agentContextWindowLabel(flow)");
    expect(app).toContain("function loadFlowDiff(flowId, options = {})");
    expect(app).toContain('const diffButton = context.querySelector(".agent-context-diff");');
    expect(app).toContain('diffButton.hidden = !flow || !diffHasChanges(diff);');
    expect(app).toContain('diffButton.onclick = flow ? () => openDiffViewer(flow.id) : null;');
    expect(app).toContain('if (flow?.id && !diff && !state.flowDiffLoadingIds.has(diffLoadingKey(flow.id))) void loadFlowDiff(flow.id);');
    expect(app).toContain('await api(`/api/flows/${encodeURIComponent(flowId)}/diff${options.patch ? "?patch=1" : ""}`)');
    expect(app).toContain("if (!options.patch && !options.force && latest?.patch) diff.patch = latest.patch;");
    expect(app).toContain("function diffLoadingKey(flowId, options = {})");
    expect(app).toContain('state.diffModalDiff = flowId && state.flowDiffs.get(flowId) ? { ...state.flowDiffs.get(flowId) } : null;');
    expect(app).toContain('if (flowId) void loadFlowDiff(flowId, { patch: true, modal: true, force: true });');
    expect(app).toContain("const diff = state.diffModalDiff;");
    expect(app).toContain("const loading = state.diffModalLoadingFlowId === flowId && !diff?.patch;");
    expect(app).toContain("if (Number.isFinite(diff?.count) && diff.count > 0) return diff.count;");
    expect(app).toContain("additions: Number(data?.additions || 0)");
    expect(app).toContain("deletions: Number(data?.deletions || 0)");
    expect(app).toContain("files: Array.isArray(data?.files) ? data.files : []");
    expect(app).toContain('count.className = `${className} diff-count`;');
    expect(app).toContain('count.setAttribute("aria-label", `${prefix}${nextText}`);');
    expect(app).toContain("digits.textContent = nextText;");
    expect(app).not.toContain("diff-count-digit-stack");
    expect(app).toContain("function renderDiffIndicator(button, flowId, diff)");
    expect(app).toContain('if (button.dataset.flowId !== (flowId || ""))');
    expect(app).toContain('renderDiffIndicator(diffButton, flow?.id || "", diff);');
    expect(app).toContain("function diffCountLabel(value, prefix)");
    expect(app).toContain("function showDiffModal()");
    expect(app).toContain("function hideDiffModal()");
    expect(app).toContain("showModalElement(els.diffModal);");
    expect(app).toContain("function renderDiffCode(code, text, selectedPath = \"\")");
    expect(app).toContain("function diffLineClass(line)");
    expect(app).toContain("function shouldHideDiffLine(line)");
    expect(app).toContain("function diffFilePathFromHeader(line)");
    expect(app).toContain("function diffPatchFilePaths(text)");
    expect(app).toContain("function orderedDiffFiles(diff)");
    expect(app).toContain("function diffHunkLineState(line)");
    expect(app).toContain("function appendDiffLine(fragment, line, lineState)");
    expect(app).toContain("function setSelectedDiffFile(path, options = {})");
    expect(app).toContain("function scrollDiffFileIntoView(path)");
    expect(app).toContain("function selectedDiffFileFromScroll()");
    expect(app).toContain("function syncSelectedDiffFileFromScroll()");
    expect(app).toContain("function scheduleSelectedDiffFileSync()");
    expect(app).toContain("function renderDiffSummary(summary, diff, loading, selectedPath = \"\")");
    expect(app).toContain("const files = orderedDiffFiles(diff);");
    expect(app).toContain('row.className = "diff-summary-file";');
    expect(app).toContain('row.type = "button";');
    expect(app).toContain("row.dataset.diffPath = file.path;");
    expect(app).toContain('row.classList.toggle("is-selected", file.path === selectedPath);');
    expect(app).toContain('path.className = "diff-summary-path";');
    expect(app).toContain('counts.className = "diff-summary-counts";');
    expect(app).toContain("row.append(counts, path);");
    expect(app).toContain('const lines = String(text).split("\\n");');
    expect(app).toContain('anchor.className = "diff-file-anchor";');
    expect(app).toContain("anchor.dataset.diffPath = filePath;");
    expect(app).toContain('divider.className = `diff-file-divider${fileDividerCount ? "" : " is-first"}`;');
    expect(app).toContain("divider.dataset.diffPath = filePath;");
    expect(app).toContain('divider.classList.toggle("is-selected", filePath === selectedPath);');
    expect(app).toContain('row.className = "diff-code-line";');
    expect(app).toContain('oldNumber.className = "diff-line-number";');
    expect(app).toContain('newNumber.className = "diff-line-number";');
    expect(app).toContain('text.className = `diff-line-text${className ? ` ${className}` : ""}`;');
    expect(app).toContain("lineState.oldLine += 1;");
    expect(app).toContain("lineState.newLine += 1;");
    expect(app).toContain('els.diffModal.querySelector(".diff-modal")?.setAttribute("aria-label", `${flow?.linearIssueId || "Flow"} diff`);');
    expect(app).toContain("function renderDiffModal()");
    expect(app).toContain("diffModalSelectedPath: \"\"");
    expect(app).toContain("let diffModalScrollFrame = 0;");
    expect(app).toContain("state.diffModalSelectedPath = \"\";");
    expect(app).toContain('if (options.revealSummary && selectedRow) selectedRow.scrollIntoView({ block: "nearest" });');
    expect(app).toContain('const target = [...scroller.querySelectorAll(".diff-file-anchor")].find((anchor) => anchor.dataset.diffPath === path);');
    expect(app).toContain("scroller.scrollTop = Math.max(0, target.offsetTop - paddingTop - 8);");
    expect(app).toContain('els.diffModal?.querySelector(".diff-modal-code")?.addEventListener("scroll", scheduleSelectedDiffFileSync, { passive: true });');
    expect(app).toContain("const selectedPath = files.some((file) => file.path === state.diffModalSelectedPath)");
    expect(app).toContain("const fileButton = event.target.closest?.(\".diff-summary-file\");");
    expect(app).toContain('setSelectedDiffFile(fileButton.dataset.diffPath || "", { scroll: true });');
    expect(app).toContain('return "--";');
    expect(app).toContain("return `${Math.round((available / total) * 100)}%`;");
    expect(app).not.toContain("ctx ");
    expect(app).toContain('const defaultCodexReasoningEffort = "medium";');
    expect(app).toContain('provider === "codex" ? flow.agentReasoningEffort || defaultCodexReasoningEffort : ""');
    expect(app).toContain("function wouldBeAgentContext(ticket)");
    expect(app).toContain('agentModel: provider === "claude" ? defaultClaudeModel : defaultCodexModel,');
    expect(app).toContain('branchName: issueSlug ? `turbo/${issueSlug}` : "",');
    expect(app).toContain('flow.agentServiceTier === "fast" ? "fast" : ""');
    expect(app).toContain("function renderAgentContext(flow)");
    expect(app).toContain('context.querySelector(".agent-context-window").textContent = agentContextWindowLabel(flow);');
    expect(app).toContain('context.querySelector(".agent-context-model").textContent = agentModelLabel(flow);');
    expect(app).toContain('const branch = context.querySelector(".agent-context-branch");');
    expect(app).toContain('const branchName = flow?.branchName || "";');
    expect(app).toContain("branch.textContent = branchName;");
    expect(app).toContain("if (flow?.prUrl) {");
    expect(app).toContain("branch.href = flow.prUrl;");
    expect(app).toContain("branch.onclick = null;");
    expect(app).toContain('branch.removeAttribute("href");');
    expect(app).toContain('branch.setAttribute("role", "button");');
    expect(app).toContain('branch.setAttribute("aria-label", "Copy branch name");');
    expect(app).toContain("branch.onclick = () => copyAgentBranchName(branchName);");
    expect(app).toContain("async function copyTextToClipboard(text)");
    expect(app).toContain("await navigator.clipboard.writeText(text);");
    expect(app).toContain('document.execCommand("copy");');
    expect(app).toContain('toast("Branch copied");');
    expect(app).not.toContain('context.querySelector(".agent-context-phase")');
    expect(app).not.toContain("const stageName = ticket.flowStage ? escapeHtml(ticket.flowStage) : \"\";");
    expect(app).not.toContain("titleCase");
    expect(app).toContain("renderAgentContext(flow || wouldBeAgentContext(ticket));");
    expect(css).toContain("grid-template-columns: minmax(0, 1fr) var(--shell-resizer-size) var(--shell-pane-size, 28%);");
    expect(css).toContain("padding: 4px 0;");
    expect(css).not.toContain(".agent-context-window::before");
    expect(css).not.toContain('content: "context ";');
    expect(css).not.toContain(".agent-context-model::before");
    expect(css).not.toContain('content: "model ";');
    expect(css).not.toContain(".agent-context-branch::before");
    expect(css).not.toContain('content: "branch ";');
    expect(css).toContain(".agent-context-branch:not([href]) {\n  cursor: pointer;\n}");
    expect(css).toContain("padding-block: 2px;");
    expect(css).toContain(".agent-context-diff {\n  display: inline-flex;");
    expect(css).toContain("min-height: 0;\n  height: 1.2em;");
    expect(css).toContain("font-variant-numeric: tabular-nums;");
    expect(css).toContain(".agent-context-diff:hover,\n.agent-context-diff:focus-visible {\n  text-decoration: none;\n}");
    expect(css).toContain("body.theme-dark .agent-context-diff,\nbody.theme-dark .agent-context-diff:hover");
    expect(css).toContain(".agent-context-diff .diff-additions");
    expect(css).toContain(".agent-context-diff .diff-deletions");
    expect(css).not.toContain("diff-count-slide");
    expect(css).not.toContain("diff-count-digit-stack");
    expect(css).toContain(".diff-modal-backdrop");
    expect(css).toContain(".diff-modal-backdrop.is-open");
    expect(css).toContain("display: flex;\n  flex-direction: column;");
    expect(css).toContain("gap: 1px;");
    expect(css).toContain("line-height: 1.2;");
    expect(css).toContain(".diff-summary-file");
    expect(css).toContain(".diff-summary-file.is-selected .diff-summary-path");
    expect(css).toContain(".diff-summary-path");
    expect(css).toContain(".diff-summary-counts");
    expect(css).toContain(".diff-modal-summary .diff-additions");
    expect(css).toContain(".diff-modal-summary .diff-deletions");
    expect(css).toContain(".diff-modal-code");
    expect(css).toContain(".diff-modal-code {\n  position: relative;");
    expect(css).toContain("scrollbar-color: var(--line-strong) transparent;");
    expect(css).toContain("scrollbar-width: thin;");
    expect(css).toContain(".diff-modal-code::-webkit-scrollbar");
    expect(css).not.toContain(".diff-modal-summary::-webkit-scrollbar");
    expect(css).toContain(".diff-modal-code code");
    expect(html).not.toContain("diff-modal-close");
    expect(app).not.toContain("diff-modal-close");
    expect(css).not.toContain(".diff-modal-close");
    expect(css).toContain(".diff-modal-code .diff-code-line");
    expect(css).toContain(".diff-modal-code .diff-line-number");
    expect(css).toContain(".diff-modal-code .diff-line-text");
    expect(css).toContain(".diff-modal-code .diff-file-anchor");
    expect(css).toContain(".diff-modal-code .diff-file-divider");
    expect(css).toContain(".diff-modal-code .diff-file-divider.is-selected");
    expect(css).toContain("position: sticky;\n  top: 0;");
    expect(css).toContain("z-index: 2;");
    expect(css).toContain("0 -18px 0 18px var(--terminal),\n    0 8px 0 8px var(--terminal);");
    expect(css).toContain(".diff-modal-code .diff-file-divider.is-first");
    expect(css).toContain(".diff-modal-code .diff-line-inserted");
    expect(css).toContain(".diff-modal-code .diff-line-deleted");
    expect(css).toContain("body.theme-dark .diff-modal-code .diff-line-inserted");
    expect(css).toContain("overscroll-behavior: contain;");
    expect(css).toContain("white-space: pre-wrap;\n  overflow-wrap: anywhere;");
    expect(css).toContain("min-width: 0;\n  white-space: inherit;");
    expect(css).not.toContain(".agent-context-phase");
    expect(css).not.toContain('content: "phase ";');
    expect(css).toContain("--link: #2563eb;");
    expect(css).toContain("--link: #60a5fa;");
    expect(css).toContain(".agent-context a[href] {\n  color: var(--link);\n}");
    expect(css).not.toContain(".agent-context a[href]:hover");
    expect(css).toContain("border-left: 1px solid var(--line-strong);");
    expect(server).toContain('if (parts[3] === "diff" && request.method === "GET")');
    expect(server).toContain('await getDiff(flow, { patch: url.searchParams.get("patch") === "1" })');
    expect(server).toContain('run(["symbolic-ref", "--quiet", "--short", "refs/remotes/origin/HEAD"])');
    expect(server).toContain('"origin/main"');
    expect(server).toContain('run(["merge-base", "HEAD", candidate])');
    expect(server).toContain('run(["diff", "--name-only", "-z", baseRef]');
    expect(server).toContain('run(["diff", "--find-renames", "--numstat", baseRef]');
    expect(server).toContain("const files: DiffFile[] = [];");
    expect(server).toContain("files.push({");
    expect(server).toContain("additions += addedCount;");
    expect(server).toContain("deletions += deletedCount;");
    expect(server).toContain('count: names.split("\\0").filter(Boolean).length');
    expect(server).toContain('run(["diff", "--find-renames", "--stat", "--patch", baseRef]');
    expect(server).toContain("if (!options.patch) return { ...diff, stat: \"\", patch: \"\" };");
    expect(server).toContain("agentModel text not null default ''");
    expect(server).toContain("agentReasoningEffort text not null default ''");
    expect(server).toContain("agentServiceTier text not null default ''");
    expect(server).toContain("agentSandbox text not null default 'danger-full-access'");
    expect(server).toContain("agentContextTokensUsed integer not null default 0");
    expect(server).toContain("agentContextWindow integer not null default 0");
    expect(server).toContain("prUrl text not null default ''");
    expect(server).toContain("githubCiStatus text not null default 'unknown'");
    expect(server).toContain("githubCiCheckedAt text not null default ''");
    expect(server).toContain("githubCiTargetUrl text not null default ''");
    expect(server).toContain("githubCiDescription text not null default ''");
    expect(server).toContain("tryMigration(\"alter table flows add column agentModel text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column agentReasoningEffort text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column agentServiceTier text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column agentSandbox text not null default 'danger-full-access'\");");
    expect(server).toContain("tryMigration(\"alter table flows add column agentContextTokensUsed integer not null default 0\");");
    expect(server).toContain("tryMigration(\"alter table flows add column agentContextWindow integer not null default 0\");");
    expect(server).toContain("tryMigration(\"alter table flows add column prUrl text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column githubCiStatus text not null default 'unknown'\");");
    expect(server).toContain("tryMigration(\"alter table flows add column githubCiCheckedAt text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column githubCiTargetUrl text not null default ''\");");
    expect(server).toContain("tryMigration(\"alter table flows add column githubCiDescription text not null default ''\");");
    expect(server).toContain("tryMigration(\"update flows set agentContextTokensUsed = 0 where agentContextWindow > 0 and agentContextTokensUsed > agentContextWindow\");");
    expect(server).toContain("function codexThreadMetadata(payload:");
    expect(server).toContain("function codexTokenUsageMetadata(params: Record<string, unknown>): Partial<Flow>");
    expect(server).toContain("agentContextTokensUsed: optionalInteger(last?.inputTokens)");
    expect(server).toContain("agentReasoningEffort: payload.reasoningEffort ?? \"\"");
    expect(server).toContain("agentServiceTier: payload.serviceTier ?? \"\"");
    expect(server).toContain("updateFlow(activeFlow.id, { ...codexThreadMetadata(threadPayload), ...configuredAgentMetadata(activeFlow) });");
    expect(server).toContain("agentContextTokensUsed: 0");
    expect(server).toContain('if (method === "model/rerouted")');
    expect(server).toContain('if (method === "thread/tokenUsage/updated")');
    expect(server).toContain("updateFlow(runtime.flowId, { agentModel: toModel });");
  });

  test("refreshes the displayed worktree branch after each agent turn", () => {
    expect(server).toContain("function worktreeBranchUpdate(flowId: string): Partial<Flow>");
    expect(server).toContain('runGit(["rev-parse", "--abbrev-ref", "HEAD"], flow.checkoutPath);');
    expect(server).toContain("branchName && branchName !== flow.branchName ? { branchName } : {}");
    expect(server).toContain('insertLog(flowId, "agent:status", `could not read worktree branch: ${String(error)}`);');
    expect(server).toContain("...worktreeBranchUpdate(runtime.flowId),");
    expect(server).toContain('agentStatus: turn?.status === "failed" ? "failed" : "idle",');
    expect(app).toContain("branch.textContent = branchName;");
  });

  test("lets agents update flow metadata including PR URL", () => {
    expect(server).toContain('if (parts[3] === "meta" && request.method === "POST")');
    expect(server).toContain("function flowMetaUpdate(body: Record<string, unknown>): Partial<Flow>");
    expect(server).toContain('fields.prUrl = normalizePrUrl(body.prUrl);');
    expect(server).toContain('fields.githubCiStatus = "unknown";');
    expect(server).toContain('fields.githubCiCheckedAt = "";');
    expect(server).toContain('return json({ error: "No supported flow metadata fields provided." }, { status: 400 });');
    expect(server).toContain('fields.prUrl ? `PR set to ${fields.prUrl}\\n` : "PR cleared\\n"');
    expect(server).toContain('if (selectedGithubCiFlowId === id) void pollSelectedGithubCiStatus();');
  });

  test("polls selected flow GitHub CI on the server and displays persisted status", () => {
    expect(server).toContain('let selectedGithubCiFlowId = "";');
    expect(server).toContain("let githubCiPolling = false;");
    expect(server).toContain('if (request.method === "selected-github-flow")');
    expect(server).toContain("setSelectedGithubCiFlow(flowId);");
    expect(server).toContain("async function runGh(args: string[], flow?: Flow)");
    expect(server).toContain('cmd: ["gh", ...args]');
    expect(server).toContain("const [stdoutText, stderrText, exitCode] = await Promise.all");
    expect(server).toContain('JSON.parse(await runGh(["pr", "view", flow.prUrl, "--json", "statusCheckRollup,headRefOid,state,url"], flow))');
    expect(server).toContain('if (String(pr.state || "").toLowerCase() === "merged")');
    expect(server).toContain('state: "merged" as GithubCiState,');
    expect(server).toContain("async function pollSelectedGithubCiStatus()");
    expect(server).toContain("setInterval(() => void pollSelectedGithubCiStatus(), 5000);");
    expect(server).toContain("githubCiStatus: ci.state,");
    expect(server).toContain("githubCiCheckedAt: ci.checkedAt || now(),");
    expect(app).toContain("function syncSelectedGithubCiFlow(flow)");
    expect(app).toContain('void wsRequest("selected-github-flow", { flowId }).catch(() => {});');
    expect(app).toContain("syncSelectedGithubCiFlow(flow);");
    expect(app).toContain("function renderGithubCiPill(flow)");
    expect(app).toContain("if (!flow?.prUrl) return \"\";");
    expect(app).toContain("const GITHUB_CI_RECENT_MS = 30 * 60 * 1000;");
    expect(app).toContain("function githubCiStatusRecent(flow)");
    expect(app).toContain('if (status === "success" || status === "pending" || status === "failure" || status === "merged") return status;');
    expect(app).toContain('if (status === "merged") return "GitHub PR merged";');
    expect(app).toContain('const knownRecent = status !== "unknown" && githubCiStatusRecent(flow);');
    expect(app).toContain('${knownRecent ? \'<span class="github-ci-dot" aria-hidden="true"></span>\' : ""}');
    expect(app).toContain('href="${escapeAttribute(flow.prUrl)}"');
    expect(app).toContain('class="github-ci-pill${knownRecent ? ` github-ci-pill-${status}` : ""}"');
    expect(app).toContain('aria-label="${escapeAttribute(label)}" title="${escapeAttribute(title)}"');
    expect(app).toContain("function githubCiOnlyFlowChanges(previousFlows, nextFlows)");
    expect(app).toContain("if (githubCiOnlyFlowChanges(previousFlows, state.flows)) {\n        renderFlowPane();\n        return;\n      }");
    expect(app).not.toContain("setInterval(() => void loadGithub");
    expect(css).toContain(".github-ci-pill-success .github-ci-dot");
    expect(css).toContain(".github-ci-pill-merged .github-ci-dot {\n  background: #8b5cf6;\n}");
    expect(css).toContain(".github-ci-pill-pending .github-ci-dot");
    expect(css).toContain(".github-ci-pill-unknown .github-ci-dot {\n  background: #fbbf24;\n}");
    expect(css).toContain("body.theme-dark .github-ci-pill-pending .github-ci-dot,\nbody.theme-dark .github-ci-pill-unknown .github-ci-dot {\n  background: #fbbf24;\n}");
    expect(css).toContain(".github-ci-pill-failure .github-ci-dot {\n  background: #ef4444;\n}");
  });

  test("interrupts active agent turns instead of killing the app server", () => {
    expect(html).not.toContain('class="send-message"');
    expect(html).not.toContain('class="start-agent"');
    expect(html).not.toContain('class="agent-start icon-button"');
    expect(html).not.toContain('class="agent-interrupt icon-button"');
    expect(html).not.toContain('aria-label="Pause agent"');
    expect(html).toContain('class="agent-image-context" aria-label="Attached image context" hidden');
    expect(html).not.toContain('class="agent-image-drop-overlay"');
    expect(html).toContain('class="history-search-indicator" aria-live="polite" aria-hidden="true"');
    expect(html).toContain('class="slash-menu" role="listbox" hidden');
    expect(html).not.toContain('class="agent-working" aria-live="polite" hidden');
    expect(html).not.toContain("<span>working</span>");
    expect(html.indexOf('class="queued-prompt-hint"')).toBeLessThan(html.indexOf('<div class="prompt-input-pane">'));
    expect(html).toContain('<div class="prompt-input-pane">');
    expect(html).toContain("message queued");
    expect(html).toContain('<span class="input-pane-prefix prompt-input-prefix" aria-hidden="true">&gt;</span>');
    expect(html).toContain('<textarea class="message-input" rows="1"></textarea>');
    expect(html).toContain('class="shell-output-resizer"');
    expect(html).toContain('aria-label="Resize agent and shell output panes"');
    expect(html).toContain('aria-orientation="vertical"');
    expect(html).toContain('class="shell-output-pane" role="log" aria-live="polite" hidden');
    expect(html).toContain('<div class="shell-input-pane">');
    expect(html).toContain('<span class="input-pane-prefix shell-input-prefix" aria-hidden="true">$</span>');
    expect(html).toContain('<input class="shell-input" autocomplete="off" />');
    expect(html).not.toContain('placeholder="Prompt the agent"');
    expect(html).not.toContain('placeholder="$ shell"');
    expect(html).not.toContain('<path d="M9 6v12" />');
    expect(html).not.toContain('<path d="M15 6v12" />');
    expect(html.indexOf('class="terminal"')).toBeLessThan(html.indexOf('class="message-form"'));
    expect(app).not.toContain("agentStart.addEventListener");
    expect(app).not.toContain("agentInterrupt.disabled");
    expect(app).toContain("function agentWorkingForFlow(flow)");
    expect(app).not.toContain("const AGENT_WORKING_POLL_INTERVAL_MS = 2500;");
    expect(app).toContain("function syncAgentWorkingPushState(flowId, agentWorking)");
    expect(app).toContain("function requestFlowSnapshot(flowId)");
    expect(app).toContain('void wsRequest("flow", { flowId }).then((data) => {');
    expect(app).not.toContain("/agent/status");
    expect(app).not.toContain("if (state.messageSubmitting || state.interruptSubmitting)");
    expect(app).toContain("terminal-entry-working-${runtimeKind}");
    expect(app).toContain('body.className = "terminal-entry-body agent-working agent-turn-working";');
    expect(app).toContain('dots.setAttribute("aria-label", "Agent working");');
    expect(app).toContain("syncAgentWorkingPushState(id, agentWorking);");
    expect(app).toContain("!options.scrollToLatest && !agentWorking && (state.terminalFollowPaused || !terminalAtLatest(terminal))");
    expect(app).not.toContain("const runtimeKind = flowRuntimeKind(flow);");
    expect(app).toContain('const agentWorkingKind = agentWorking ? "agent" : "idle";');
    expect(app).toContain("if (agentWorking) appendTerminalWorkingBlock(fragment);");
    expect(app).toContain("renderLogs(flow.id, { force: true, scrollToLatest: true });");
    expect(app).not.toContain('els.flowPane.querySelector(".agent-working").hidden = !agentWorking;');
    expect(app).toContain("shellOutputClearAfterLogId: new Map()");
    expect(app).toContain("function latestShellGroups(logs, clearAfterLogId = 0)");
    expect(app).toContain("filter((log) => Number(log.id) > clearAfterLogId)");
    expect(app).toContain("function isShellOutputLog(log)");
    expect(app).toContain('if (!isShellOutputLog(log)) continue;');
    expect(app).toContain('return log.source === "shell:output" || log.source === "shell:stderr" || log.source === "shell:status";');
    expect(app).toContain("function isShellOwnedLog(log)");
    expect(app).toContain('String(log.source || "").startsWith("shell:") && log.source !== "shell:command"');
    expect(app).toContain("function isLegacyShellOutputLog(log)");
    expect(app).toContain("function isShellTraceGroupLog(log)");
    expect(app).toContain("function shellOutputPane()");
    expect(app).toContain("function shellOutputGroups(logs, flow)");
    expect(app).toContain("function syncShellOutputClearState(flows)");
    expect(app).toContain("Number(flow.shellOutputClearAfterLogId || 0)");
    expect(app).toContain("const flowId = flow?.id || state.selectedFlowId;");
    expect(app).toContain("state.shellOutputClearAfterLogId.get(flowId) || 0");
    expect(app).toContain("Number(flow?.shellOutputClearAfterLogId || 0)");
    expect(app).toContain("return latestShellGroups(logs, clearAfterLogId);");
    expect(app).toContain("async function clearShellOutputPane()");
    expect(app).toContain("state.shellOutputClearAfterLogId.set(flowId, state.lastLogId.get(flowId) || 0);");
    expect(app).toContain('await api(`/api/flows/${encodeURIComponent(flowId)}/shell-output/clear`, { method: "POST" });');
    expect(app).toContain('if (message.event === "shell-output-cleared")');
    expect(server).toContain("create table if not exists shell_output_state");
    expect(server).toContain("function shellOutputClearAfterLogId(flowId: string)");
    expect(server).toContain("function setShellOutputClearAfterLogId(flowId: string, clearAfterLogId: number)");
    expect(server).toContain('broadcast("shell-output-cleared", { flowId, clearAfterLogId: normalized });');
    expect(server).toContain("function shellOutputClearAfterLogIds()");
    expect(server).toContain("const rows = allShellOutputStateStmt.all()");
    expect(server).toContain("function clientFlow(flow: Flow, clearAfterLogId = shellOutputClearAfterLogId(flow.id))");
    expect(server).toContain("shellOutputClearAfterLogId: clearAfterLogId");
    expect(server).toContain("const clearAfterLogIds = shellOutputClearAfterLogIds();");
    expect(server).toContain('parts[3] === "shell-output" && parts[4] === "clear" && request.method === "POST"');
    expect(server).toContain("const clearAfterLogId = setShellOutputClearAfterLogId(id, latestLogId(id));");
    expect(app).toContain("function hasVisibleTerminalOutput(message)");
    expect(app).toContain("!hasVisibleTerminalOutput(log.message)");
    expect(app).toContain("if (!isRoutineShellExitLog(log)) appendTerminalGroup(groups, log);");
    expect(app).toContain("const showPane = !state.shellPaneHidden;");
    expect(app).toContain("pane.hidden = !showPane;");
    expect(app).toContain("const keepShellGrid = showPane || state.shellPaneHidden;");
    expect(app).toContain('agentPanel?.classList.toggle("shell-output-visible", keepShellGrid);');
    expect(app).toContain("if (showPane) applyShellOutputSplitSize();");
    expect(app).toContain("async function interruptSelectedFlow()");
    expect(app).toContain("function flowRuntimeActive(flow)");
    expect(app).toContain("function flowRuntimeKind(flow)");
    expect(app).toContain("function flowShellLive(flow)");
    expect(app).toContain("function hasSplitRuntimeStatus(flow)");
    expect(app).toContain("function runtimeStatusActive(status)");
    expect(app).toContain("shellInterruptingFlowIds: new Set()");
    expect(app).toContain("state.shellSubmitting &&");
    expect(app).toContain("!runtimeStatusActive(flow.agentStatus)");
    expect(app).toContain('flow?.agentRuntimeKind === "shell" ? "shell" : "agent"');
    expect(app).toContain("function flowAgentCompacting(flow)");
    expect(app).toContain("return flowAgentRunning(flow) && Boolean(flow?.agentCompacting);");
    expect(app).toContain("function flowAgentQueuedMessage(flow)");
    expect(app).toContain("return flowAgentCompacting(flow) && Boolean(flow?.agentQueuedMessage);");
    expect(app).toContain("if (state.interruptSubmitting) return false;");
    expect(app).toContain("!flow.agentRuntimeStatus && !flow.shellRuntimeStatus && runtimeStatusActive(flow.agentStatus)");
    expect(app).toContain("!flow.agentRuntimeStatus &&\n        !runtimeStatusActive(flow.shellRuntimeStatus)");
    expect(app).toContain("state.interruptSubmitting = true;");
    expect(app).toContain('const data = await api(`/api/flows/${selected.id}/agent/interrupt`, { method: "POST" });');
    expect(app).toContain("if (data.flow) upsertFlow(data.flow);");
    expect(app).toContain("async function interruptSelectedShellCommand()");
    expect(app).toContain('const data = await api(`/api/flows/${selected.id}/command/interrupt`, { method: "POST" });');
    expect(app).toContain("state.shellInterruptingFlowIds.add(selected.id);");
    expect(app).toContain("renderShellOutputPane(selected.id);");
    expect(app).toContain('!state.shellInterruptingFlowIds.has(flow.id)');
    expect(app).toContain("state.shellInterruptingFlowIds.delete(flow.id);");
    expect(app).toContain("function handleShellInterruptKeydown(event)");
    expect(app).toContain('if (!event.ctrlKey || event.metaKey || event.altKey || event.key.toLowerCase() !== "c") return false;');
    expect(app).toContain("if (!flowShellRunning(selectedFlow())) return false;");
    expect(app).toContain("if (handleShellInterruptKeydown(event)) return;");
    const shellInterruptKeydown = app.slice(
      app.indexOf("function handleShellInterruptKeydown(event)"),
      app.indexOf("function handleAgentInterruptKeydown(event)"),
    );
    expect(shellInterruptKeydown).not.toContain("interruptSelectedFlow");
    expect(app).toContain("function handleAgentInterruptKeydown(event)");
    expect(app).toContain('if (event.key !== "Escape" || event.metaKey || event.ctrlKey || event.altKey) return false;');
    expect(app).toContain('if (focusedInputPaneKind() !== "prompt") return false;');
    expect(app).toContain("if (!flowAgentRunning(selectedFlow())) return false;");
    expect(app).toContain("if (!event.repeat) void interruptSelectedFlow();");
    expect(app).toContain("if (handleAgentInterruptKeydown(event)) return;");
    const serverInterruptAgent = server.slice(
      server.indexOf("async function interruptAgent"),
      server.indexOf("function stopIdleAgentRuntimesForEnvUpdate"),
    );
    expect(serverInterruptAgent).not.toContain("interruptShellCommand(flowId)");
    expect(serverInterruptAgent).toContain("const runtime = agentProcesses.get(flowId) ?? (flow ? await recoverAgentRuntime(flow) : null);");
    expect(server).toContain('db.exec("pragma busy_timeout = 5000");');
    expect(server).toContain("function isNoActiveTurnInterruptError(error: unknown)");
    expect(serverInterruptAgent).toContain("if (!isNoActiveTurnInterruptError(error)) throw error;");
    expect(serverInterruptAgent).toContain('insertLog(flowId, "agent:status", "interrupt ignored: no active turn");');
    expect(serverInterruptAgent).toContain('updateFlow(flowId, { agentStatus: "idle" });');
    expect(server).toContain("function isNoActiveTurnSteerError(error: unknown)");
    expect(server).toContain("if (!isNoActiveTurnSteerError(error)) throw error;");
    expect(server).toContain('insertLog(runtime.flowId, "agent:status", "stale active turn cleared before starting a new turn");');
    expect(app).not.toContain('focusedInputPaneKind() === "shell" &&\n    flowShellRunning(selectedFlow())');
    expect(app).not.toContain('els.flowPane.querySelector(".agent-interrupt").addEventListener("click"');
    expect(app).toContain("function canSubmitPromptMessage()");
    expect(app).toContain("!flowAgentRunning(flow) &&\n      !promptQueuedForFlow(flow)");
    expect(app).toContain("function canSubmitShellCommand()");
    expect(app).toContain("function flashBlockedInput(input)");
    expect(app).toContain("async function uploadAgentImages(files)");
    expect(app).toContain("function eventHasDraggedFiles(event)");
    expect(app).toContain("function pastedImageFiles(event)");
    expect(app).toContain('event.clipboardData?.files');
    expect(app).toContain('event.clipboardData?.items');
    expect(app).toContain("function eventTargetsPromptInputPane(event)");
    expect(app).toContain('event.target?.closest?.(".prompt-input-pane")');
    expect(app).toContain("function focusPromptInputForImageDrag(event)");
    expect(app).toContain('if (eventTargetsPromptInputPane(event)) focusInputPane("prompt");');
    expect(app).toContain("function setAgentImageDragActive(active)");
    expect(app).toContain('await api(`/api/flows/${encodeURIComponent(flow.id)}/context-images`,');
    expect(app).toContain("function agentMessageWithImages(message, images = state.pendingAgentImages)");
    expect(app).toContain('const name = image.name ? `${image.name}: ` : "";');
    expect(app).toContain("const queuedImages = [...state.pendingAgentImages];");
    expect(app).toContain(
      'const queuedMessage = agentMessageWithImages(\n    input.value.trim() || (queuedImages.length ? "Use the attached image context." : ""),\n    queuedImages,\n  );',
    );
    expect(app).toContain("message: queuedMessage,");
    expect(app).toContain("const submittedImages = [...state.pendingAgentImages];");
    expect(app).toContain('const agentMessage = agentMessageWithImages(message || "Use the attached image context.", submittedImages);');
    expect(app).toContain("state.pendingAgentImages = [];");
    expect(app).toContain("state.pendingAgentImages = [...queuedImages, ...state.pendingAgentImages];");
    expect(app).toContain("state.pendingAgentImages = [...submittedImages, ...state.pendingAgentImages];");
    expect(app).toContain('promptInput().addEventListener("paste"');
    expect(app).toContain("const files = pastedImageFiles(event);");
    expect(app).toContain("if (!files.length) return;\n  event.preventDefault();\n  void uploadAgentImages(files);");
    expect(app).toContain('document.addEventListener("drop"');
    expect(app).toContain("focusPromptInputForImageDrag(event);\n  setAgentImageDragActive(true);");
    expect(css).not.toContain(".agent-image-drop-overlay");
    expect(css).toContain(".terminal-attached-images");
    expect(css).toContain("  display: inline-grid;\n  place-items: center;\n  width: 20px;");
    expect(css).toContain("  margin-left: 2px;\n  padding: 0;\n  border: 0;\n  border-radius: 3px;");
    expect(server).toContain("async function saveFlowContextImages(flow: Flow, formData: FormData)");
    expect(server).toContain('parts[3] === "context-images" && request.method === "POST"');
    expect(app).toContain("if (!canSubmitPromptMessage()) {\n    flashBlockedInput(input);\n    return;\n  }");
    expect(app).toContain("state.messageSubmitting = true;");
    expect(app).toContain('if (command === "clear") {');
    expect(app).toContain("await clearShellOutputPane();");
    expect(app).toContain("state.shellSubmitting = true;");
    expect(app).toContain("const flow = await ensureSelectedFlow();");
    expect(app).toContain("submittedFlowId = flow.id;");
    expect(app).toContain('renderShellOutputPane(selectedFlow()?.id || "");');
    expect(app).toContain("renderShellOutputPane(flow.id);");
    expect(app).toContain("renderShellOutputPane(submittedFlowId);");
    expect(app).toContain("const shellLive = showPane && flowShellLive(flow);");
    expect(app).toContain("if (!hasGroups && !shellLive) {");
    expect(app).toContain("shell-live:${shellLive}");
    expect(app).toContain('if (shellLive) appendTerminalWorkingBlock(fragment, "shell");');
    expect(app).not.toContain('pane.classList.toggle("live", shellLive);');
    expect(app).toContain('const data = await api(`/api/flows/${flow.id}/command`, {');
    expect(app).toContain("if (data.flow) upsertFlow(data.flow);");
    expect(app).not.toContain("scheduleAgentWorkingPoll(flow.id);");
    expect(app).toContain("requestFlowSnapshot(flow.id);");
    expect(app).toContain("if (submittedFlowId) await loadLogs(submittedFlowId, { shellOnly: true });");
    expect(app).toContain("if (submittedFlowId) await loadLogs(submittedFlowId, { scrollToLatest: true });");
    expect(app).toContain('await api(`/api/flows/${flow.id}/message`, {');
    expect(app).toContain("/agent/interrupt");
    expect(server).toContain("const agentHeartbeatSweepIntervalMs = 5000;");
    expect(server).toContain("async function reconcileAgentHeartbeat(flow: Flow");
    expect(server).toContain("const agentRuntimeRecoveries = new Map<string, Promise<RuntimeProcess | null>>();");
    expect(server).toContain("function codexActiveTurnSettingKey(flowId: string)");
    expect(server).toContain("function persistedActiveTurnId(flowId: string)");
    expect(server).toContain("function setActiveTurnId(flowId: string, turnId = \"\")");
    expect(server).toContain("async function recoverAgentRuntime(flow: Flow)");
    expect(server).toContain("const runtime = await startCodexAppServer(flow);");
    expect(server).toContain("const shellRuntime = shellProcesses.get(flow.id);");
    expect(server).toContain('const shellRuntimeStatus = shellRuntime ? (shellRuntime.stopping ? "interrupting" : "running") : "";');
    expect(server).toContain("const agentRuntimeStatus = agentRuntimeActive");
    expect(server).toContain('agentRuntimeKind: shellRuntimeStatus && !agentRuntimeStatus ? "shell" : "agent"');
    expect(server).toContain("agentRuntime?.activeTurnId || agentRuntime?.compacting");
    expect(server).toContain("agentCompacting: Boolean(agentRuntime?.compacting)");
    expect(server).toContain("agentQueuedMessage: Boolean(agentRuntime?.queuedAgentMessages?.length)");
    expect(server).toContain('flow.agentStatus !== "running" && flow.agentStatus !== "interrupting"');
    expect(server).toContain("const recovered = await recoverAgentRuntime(flow);");
    expect(server).toContain("const compactingStartedAt = runtime.compactingStartedAt ?? runtime.lastSeenAt ?? nowMs;");
    expect(server).toContain("context compaction timed out after");
    expect(server).toContain("void startNextQueuedAgentMessage(runtime);");
    expect(server).not.toContain("if (runtime.compacting) return flow;");
    expect(server).toContain('insertLog(flow.id, "agent:error", "agent runtime disappeared while status was running\\n");');
    expect(server).toContain("setInterval(() => void sweepAgentHeartbeats(), agentHeartbeatSweepIntervalMs);");
    expect(server).toContain("const logsBeforePageStmt = db.query");
    expect(server).toContain('if (params.before !== undefined) {');
    expect(server).toContain("const logs = logsBeforePageStmt.all(flowId, normalizedBefore, limit);");
    expect(server).toContain('if (request.method === "flow")');
    expect(server).not.toContain('parts[3] === "agent" && parts[4] === "status" && request.method === "GET"');
    expect(server).toContain("const reconciled = await reconcileAgentHeartbeat(getFlow(flowId) as Flow);");
    expect(server).toContain("turnRunning: Boolean(agentProcesses.get(flowId)?.activeTurnId)");
    expect(server).toContain("return json({ ok: true, flow: clientFlow(getFlow(id) ?? flow) });");
    expect(server).toContain("function runtimeAdjustedFlow(flow: Flow)");
    expect(server).toContain("function listClientFlows()");
    expect(app).not.toContain("agentActionIcon");
    expect(css).toContain("[hidden] {\n  display: none !important;");
    expect(css).toContain("grid-template-columns: minmax(0, 1fr) var(--shell-resizer-size) var(--shell-pane-size, 28%);");
    expect(css).toContain("grid-template-rows: minmax(0, 1fr) auto;");
    expect(css).toContain("border-top: 1px solid var(--line);");
    expect(css).toContain(".agent-working");
    expect(css).toContain(".terminal > .terminal-entry-assistant:last-child {\n  padding-bottom: 8px;\n}");
    expect(css).toContain(".terminal-entry-working:last-child {\n  padding-bottom: 8px;\n}");
    expect(css).toContain(".terminal-entry-working-agent .agent-working");
    expect(css).toContain("color: #6d28d9;");
    expect(css).toContain("body.theme-dark .terminal-entry-working-agent .agent-working");
    expect(css).not.toContain(".agent-working.shell-working");
    expect(css).toContain(".terminal-panel.shell-output-visible");
    expect(css).toContain(
      "grid-template-columns: minmax(0, 1fr) var(--shell-resizer-size) var(--shell-pane-size, 28%);",
    );
    expect(css).toContain('grid-template-areas:\n    "terminal shell-resizer shell"\n    "form form form";');
    expect(css).toContain('"input . shell"');
    expect(css).toContain('"context . shell"');
    expect(css).toContain(".shell-output-resizer");
    expect(css).toContain("cursor: col-resize;");
    expect(css).toContain(".shell-output-pane");
    expect(css).not.toContain(".shell-output-pane.live");
    expect(css).toContain(".terminal-entry-working-shell .agent-working");
    expect(css).toContain("color: #16a34a;");
    expect(css).toContain("body.theme-dark .terminal-entry-working-shell .agent-working");
    expect(css).toContain(".shell-output-pane[hidden]");
    expect(css).not.toContain(".shell-output-pane .terminal-entry {\n  justify-items: end;");
    expect(css).not.toContain(".shell-output-pane .terminal-entry-body {\n  max-width: 100%;\n  padding-left: 0;\n  text-align: right;");
    expect(css).toContain(
      ".shell-output-pane .terminal-entry-output .terminal-entry-body,\n.shell-output-pane .terminal-entry-error .terminal-entry-body {\n  overflow-x: visible;\n  overflow-wrap: anywhere;\n  white-space: pre-wrap;\n}",
    );
    expect(css).not.toContain("body.theme-dark .agent-working.shell-working");
    expect(css).not.toContain("padding: 0 14px 6px;");
    expect(css).toContain("@keyframes agent-working-dot");
    expect(css).toContain(".agent-image-context {\n  grid-area: images;");
    expect(css).toContain("  padding-left: 14px;\n  transform: translateY(4px);");
    expect(css).toContain("  border: 1px solid #d97706;");
    expect(css).toContain(".prompt-input-pane {\n  grid-area: input;");
    expect(css).toContain(
      "  padding-left: 14px;\n}\n\nbody.shell-pane-hidden .prompt-input-pane {\n  padding-right: 14px;\n}",
    );
    expect(css).toContain(".history-search-indicator,\n.queued-prompt-hint {\n  grid-area: search;");
    expect(css).toContain(".queued-prompt-hint:not([hidden]) {\n  max-height: 22px;\n  margin-bottom: 8px;");
    expect(css).toContain(".queued-prompt-hint {\n  color: #b45309;");
    expect(css).toContain("body.theme-dark .queued-prompt-hint");
    expect(css).toContain(".agent-context {\n  grid-area: context;\n  padding-left: 16px;");
    expect(css).toContain(".shell-command-panel {\n  position: relative;\n  grid-area: shell;\n  min-width: 0;\n  align-self: start;\n  padding-right: 14px;");
    expect(css).toContain(".input-pane-prefix");
    expect(css).toContain("pointer-events: none;\n  color: var(--muted);");
    expect(css).toContain("transition: color var(--motion-fast);");
    expect(css).toContain(".prompt-input-prefix {\n  left: 24px;\n  top: 18px;");
    expect(css).toContain("place-items: center;");
    expect(css).toContain("transform: translateY(-50%);");
    expect(css).toContain(".prompt-input-pane:not(.prompt-queued):focus-within .prompt-input-prefix {\n  color: #6d28d9;\n}");
    expect(css).toContain(".prompt-input-pane.prompt-queued .prompt-input-prefix {\n  color: #d97706;\n}");
    expect(css).toContain(".shell-input-prefix {\n  top: 50%;\n  transform: translateY(-50%);");
    expect(css).toContain(".shell-input-pane:focus-within .shell-input-prefix {\n  color: #22c55e;\n}");
    expect(css).toContain(".message-input {\n  min-width: 0;\n  min-height: 36px;");
    expect(css).toContain("padding: 10px 10px 6px 30px;");
    expect(css).toContain(".message-input:focus {\n  border-color: #d97706;");
    expect(css).toContain(".prompt-input-pane:not(.prompt-queued) .message-input:focus {\n  border-color: #6d28d9;");
    expect(css).toContain("caret-color: var(--text);");
    expect(css).toContain("body.theme-dark .prompt-input-pane:not(.prompt-queued):focus-within .prompt-input-prefix {\n  color: #c084fc;\n}");
    expect(css).toContain("body.theme-dark .prompt-input-pane:not(.prompt-queued) .message-input:focus {\n  border-color: #c084fc;");
    expect(css).toContain("caret-color: var(--text);");
    expect(css).toContain(".shell-input {\n  width: 100%;\n  min-width: 0;\n  min-height: 36px;\n  padding-left: 30px;\n  border-color: var(--line);");
    expect(css).toContain(".shell-input:focus {\n  border-color: #22c55e;");
    expect(css).toContain("max-height: min(220px, 30vh);");
    expect(css).toContain("resize: none;");
    expect(css).toContain("overflow-y: auto;");
    expect(css).toContain("transition: height 120ms ease;");
    expect(css).toContain(".history-search-indicator");
    expect(css).toContain("  margin-bottom: 0;\n  padding-left: 14px;\n  opacity: 0;");
    expect(css).not.toContain(".history-search-indicator strong");
    expect(css).toContain(".message-form.history-searching");
    expect(css).toContain("max-height: 22px;\n  margin-bottom: 8px;\n  opacity: 1;\n  transform: translateY(4px);");
    expect(css).toContain("max-height 120ms ease");
    expect(css).toContain(".message-form.history-searching .history-search-indicator");
    expect(css).toContain("bottom: calc(100% - 4px);");
    expect(css).toContain(".slash-menu");
    expect(css).toContain("width: max-content;");
    expect(css).toContain(
      "max-width: min(720px, calc(100% - var(--shell-pane-size, 28%) - 18px), calc(100vw - 40px));",
    );
    expect(css).toContain("grid-template-columns: minmax(210px, max-content) minmax(0, auto);");
    expect(css).toContain(".slash-command-name {\n  display: inline-flex;");
    expect(css).toContain(".slash-command-icon {\n  width: 14px;");
    expect(css).toContain(".slash-command-description {\n  min-width: 0;");
    expect(css).toContain("overflow: visible;");
    const slashDescriptionRule = css.slice(css.indexOf(".slash-command-description {"));
    expect(slashDescriptionRule.slice(0, slashDescriptionRule.indexOf("}"))).not.toContain("text-align: right;");
    expect(css).toContain("white-space: nowrap;");
    expect(css).not.toContain(".agent-interrupt");
    expect(css).not.toContain(".agent-start svg");
    expect(server).toContain('"turn/interrupt"');
    expect(server).toContain("async function interruptAgent");
    expect(server).toContain("const shellProcesses = new Map<string, RuntimeProcess>();");
    expect(server).toContain('kind: "agent" | "serve" | "shell";');
    expect(server).toContain('if (shellProcesses.has(flow.id)) throw new Error("A shell command is already running.");');
    expect(server).toContain("type RuntimeSignal = \"SIGINT\" | \"SIGTERM\" | \"SIGKILL\";");
    expect(server).toContain("function signalRuntimeProcess(runtime: RuntimeProcess, signal: RuntimeSignal)");
    expect(server).toContain("function signalProcessGroup(pid: number, signal: RuntimeSignal)");
    expect(server).toContain("process.kill(-pid, signal);");
    expect(server).toContain("let signaled = signalProcessGroup(runtime.proc.pid, signal);");
    expect(server).toContain("function scheduleShellInterruptEscalation(flowId: string, runtime: RuntimeProcess)");
    expect(server).toContain('insertLog(flowId, "shell:status", "shell interrupt escalated");');
    expect(server).toContain('insertLog(flowId, "shell:status", "shell interrupt forced cleanup");');
    expect(server).toContain('detached: true,');
    expect(server).toContain("function startShellCommand(flow: Flow, userCommand: string)");
    expect(server).not.toContain("async function runShellCommand");
    expect(server).toContain('shellProcesses.set(flow.id, runtime);');
    expect(server).toContain('const commandLogId = insertLog(flow.id, "shell:command", command);');
    expect(server).toContain('streamProcessOutput(flow.id, "shell:output", proc.stdout)');
    expect(server).toContain('streamProcessOutput(flow.id, "shell:stderr", proc.stderr)');
    expect(server).toContain("void (async () => {");
    expect(server).toContain("if (shellProcesses.get(flow.id)?.proc === proc) shellProcesses.delete(flow.id);\n      await Promise.all([stdoutDone, stderrDone]);");
    expect(server).toContain("await Promise.all([stdoutDone, stderrDone]);");
    expect(server).toContain("const resultLogId = insertLog(\n        flow.id,\n        \"shell:result\",");
    expect(server).toContain('createTraceGroupBetweenLogs(flow.id, commandLogId, resultLogId + 1, "shell");');
    expect(server).toContain('runtime.proc?.stdin?.write("\\x03");');
    expect(server).toContain('signalRuntimeProcess(runtime, "SIGINT");');
    expect(server).toContain("scheduleShellInterruptEscalation(flowId, runtime);");
    expect(server).toContain("function interruptShellCommand(flowId: string)");
    expect(server).not.toContain("if (interruptShellCommand(flowId)) return;");
    expect(server).toContain('parts[3] === "command" && parts[4] === "interrupt" && request.method === "POST"');
    expect(server).toContain("interruptShellCommand(id);");
    expect(server).toContain("startShellCommand(flow, body.command);");
    expect(server).toContain("return json({ ok: true, flow: clientFlow(getFlow(id) ?? flow) });");
    expect(server).toContain('runtime.stopping ? "interrupted" : "failed"');
    expect(server).toContain('agentStatus: code === 0 || runtime.stopping ? "idle" : "failed"');
    expect(server).not.toContain("agent stopped by Turbopump");
    expect(server).toContain('const serveProcessPidSettingKey = "serveProcessPid";');
    expect(server).toContain("function rememberServeProcess(runtime: RuntimeProcess)");
    expect(server).toContain("function cleanupPersistedServeProcess()");
    expect(server).toContain('insertLog(flowId, "serve", "\\n[stale serve process stopped by Turbopump]\\n");');
    expect(server).toContain("cleanupPersistedServeProcess();");
    expect(server).toContain("rememberServeProcess(serveProcess);");
    expect(server).toContain("function serverHasActiveWork()");
  });

  test("cleans up spawned runtimes when server startup or first turn fails", () => {
    expect(server).toContain("function cleanupFailedRuntimeProcess(runtime: RuntimeProcess, reason: string)");
    expect(server).toContain("rejectCodexPending(runtime, new Error(reason));");
    expect(server).toContain("forgetRuntimeProcess(runtime);");
    expect(server).toContain('signalRuntimeProcess(runtime, "SIGTERM");');
    expect(server).toContain('signalRuntimeProcess(runtime, "SIGKILL");');
    const codexStartup = server.slice(server.indexOf("async function startCodexAppServer"));
    expect(codexStartup).toContain("detached: true,");
    expect(codexStartup).toContain('insertLog(activeFlow.id, "agent:status", `cleaning up failed app-server startup: ${String(error)}`);');
    expect(codexStartup).toContain("cleanupFailedRuntimeProcess(runtime, `codex app-server startup failed: ${String(error)}`);");
    const startAgentTurnAfterUserLogBody = server.slice(server.indexOf("async function startAgentTurnAfterUserLog"));
    expect(startAgentTurnAfterUserLogBody).toContain("let createdRuntime = false;");
    expect(startAgentTurnAfterUserLogBody).toContain("createdRuntime = true;");
    expect(startAgentTurnAfterUserLogBody).toContain("if (createdRuntime && runtime) cleanupFailedRuntimeProcess(runtime, `agent turn failed: ${String(error)}`);");
    const startAgentBody = server.slice(server.indexOf("async function startAgent("));
    expect(startAgentBody).toContain("let createdRuntime = false;");
    expect(startAgentBody).toContain("createdRuntime = true;");
    expect(startAgentBody).toContain("if (createdRuntime && runtime) cleanupFailedRuntimeProcess(runtime, `agent turn failed: ${String(error)}`);");
  });

  test("lets the Agent and Linear panes be resized with the separator", () => {
    expect(html).toContain('class="pane-rail linear-pane-rail"');
    expect(html).toContain('aria-label="Expand Linear pane"');
    expect(html).toContain('class="flow-resizer"');
    expect(html).toContain('role="separator"');
    expect(html).toContain('aria-orientation="horizontal"');
    expect(html).toContain('aria-valuemin="0"');
    expect(html).toContain('aria-valuemax="100"');
    expect(css).toContain('grid-template-rows: var(--top-pane-size, 50%) 8px minmax(0, 1fr);');
    expect(css).toContain("body.linear-pane-hidden .flow-content");
    expect(css).toContain("body.linear-pane-hidden .linear-pane-rail");
    expect(css).toContain("width: 12px;\n  min-width: 12px;");
    expect(css).not.toContain(".pane-rail::before");
    expect(app).toContain('const LINEAR_PANE_HIDDEN_KEY = "flow.linearPaneHidden";');
    expect(app).toContain("linearPaneHidden: initialBooleanSetting(LINEAR_PANE_HIDDEN_KEY)");
    expect(app).toContain("function setLinearPaneHidden(hidden)");
    expect(app).toContain('document.body.classList.toggle("linear-pane-hidden", hidden);');
    expect(app).toContain('const linearRail = els.flowPane.querySelector(".linear-pane-rail");');
    expect(app).toContain('if (linearRail) linearRail.setAttribute("aria-expanded", String(!hidden));');
    expect(app).toContain("function toggleLinearPaneHidden()");
    expect(app).toContain("if (!state.linearPaneHidden && next <= 1) {\n    setLinearPaneHidden(true);\n    return;\n  }");
    expect(app).toContain("localStorage.getItem(FLOW_SPLIT_SIZE_KEY) || 50");
    expect(css).toContain(".flow-resizer");
    expect(css).toContain("cursor: row-resize;");
    expect(css).toContain("body.flow-resizing");
    expect(css).toContain(".flow-resizer:hover,\n.flow-resizer:focus-visible,\nbody.flow-resizing .flow-resizer {\n  background: #e2e7ee;\n}");
    expect(css).toContain("body.flow-resizing .flow-content {\n  transition: none;\n}");
    expect(css).toContain(
      "body.theme-dark .flow-resizer:hover,\nbody.theme-dark .flow-resizer:focus-visible,\nbody.theme-dark.flow-resizing .flow-resizer,",
    );
    expect(app).toContain('const FLOW_SPLIT_SIZE_KEY = "flow.topPaneSize";');
    expect(app).toContain("function flowSplitBounds(content, rect)");
    expect(app).toContain("const minTopPx = 0;");
    expect(app).not.toContain('linearDetail?.querySelector(".linear-meta")');
    expect(app).toContain('content.querySelector(".message-form")');
    expect(app).toContain("function setFlowSplitSize(value)");
    expect(app).toContain('content.style.setProperty("--top-pane-size"');
    expect(css).toContain(".agent-panel {\n  --terminal-message-max-lines: 50;\n  --terminal-message-max-height: 77.5em;\n  position: relative;\n  overflow: hidden;\n}");
    expect(css).toContain(".terminal {\n  grid-area: terminal;\n  display: grid;\n  align-content: start;\n  gap: 10px;\n  min-height: 0;");
    expect(css).toContain("padding: 14px 14px 42px;");
    expect(css).toContain("background: var(--panel);\n  z-index: 1;");
    expect(app).toContain('els.flowPane.querySelector(".flow-resizer").addEventListener("pointerdown"');
    expect(app).toContain('els.flowPane.querySelector(".flow-resizer").addEventListener("dblclick", toggleLinearPaneHidden);');
    expect(app).toContain('els.flowPane.querySelector(".flow-resizer").addEventListener("keydown"');
    expect(app).toContain('els.flowPane.querySelector(".linear-pane-rail").addEventListener("click", () => setLinearPaneHidden(false));');
    expect(app).toContain('const SHELL_OUTPUT_SPLIT_SIZE_KEY = "flow.shellOutputPaneSize";');
    expect(app).toContain("function shellOutputSplitBounds(panel, rect)");
    expect(app).toContain("function setShellOutputSplitSize(value)");
    expect(app).toContain("function setShellOutputSplitSizeKeepingTerminalBottom(value)");
    expect(app).toContain("const restoreTerminalBottom = terminalBottomRestorer();");
    expect(app).toContain("setShellOutputSplitSize(value);\n  restoreTerminalBottom();");
    expect(app).toContain("function shellOutputSplitResizeContext()");
    expect(app).toContain("function resizeShellOutputSplitFromPointerWithContext(clientX, context)");
    expect(app).toContain("requestAnimationFrame(flushResize)");
    expect(app).toContain("localStorage.setItem(SHELL_OUTPUT_SPLIT_SIZE_KEY, String(state.shellOutputSplitSize));");
    expect(app).toContain('panel.style.setProperty("--shell-pane-size"');
    expect(app).toContain("function startShellOutputSplitResize(event)");
    expect(app).toContain('els.flowPane.querySelector(".shell-output-resizer").addEventListener("pointerdown"');
    expect(app).toContain('els.flowPane.querySelector(".shell-output-resizer").addEventListener("keydown"');
    expect(css).toContain(
      ".shell-output-resizer:hover,\n.shell-output-resizer:focus-visible,\nbody.shell-output-resizing .shell-output-resizer {\n  background: #e2e7ee;\n}",
    );
    expect(css).toContain(
      "body.theme-dark .shell-output-resizer:hover,\nbody.theme-dark .shell-output-resizer:focus-visible,\nbody.theme-dark.shell-output-resizing .shell-output-resizer,",
    );
    expect(css).toContain("body.shell-output-resizing .terminal-panel {\n  transition: none;\n}");
  });

  test("hides routine turn status logs from the terminal", () => {
    expect(app).toContain("function isHiddenTerminalLog(log)");
    expect(app).toContain('log.source === "agent:status"');
    expect(app).toContain("/^turn started\\b/");
    expect(app).toContain("/^turn completed\\b/");
    expect(app).toContain("/^interrupt requested\\b/");
    expect(app).toContain("/^[$]\\s*codex app-server --listen stdio:\\/\\/$/i");
    expect(app).toContain("/^Codex thread \\S+ ready$/i");
    expect(app).toContain('log.source === "agent:tool-result" && message === "completed"');
    expect(app).toContain('log.source === "agent:tool-result" && message === "completed exit 0"');
    expect(app).toContain('log.source === "agent:tool-result" && message === "failed exit 7"');
    expect(app).toContain("function plainTerminalText(message)");
    expect(app).toContain(".replace(/\\x1b\\[[0-?]*[ -/]*[@-~]/g, \"\")");
    expect(app).toContain("function isNoisyAgentStderr(message)");
    expect(app).toContain("plainTerminalText(message)");
    expect(app).toContain("/ERROR\\s+rmcp::transport::worker:\\s+worker quit with fatal:\\s+Transport channel closed\\b/");
    expect(app).toContain('log.source === "agent:stderr" && isNoisyAgentStderr(message)');
    expect(app).toContain("function isRoutineShellExitLog(log)");
    expect(app).toContain('String(log.message || "").trim() === "completed exit 0"');
    expect(app).toContain("if (isHiddenTerminalLog(log)) continue;");
  });

  test("renders command execution logs as a single shell prompt line", () => {
    expect(app).toContain('group.source === "agent:tool" || group.source === "agent:tool-result" || group.source === "shell:command"');
    expect(app).toContain('block.classList.add("terminal-entry-command");');
    expect(app).toContain('row.className = "terminal-command-line";');
    expect(app).toContain("marker.textContent = meta.marker;");
    expect(app).toContain("row.replaceChildren(marker, body);");
    expect(app).toContain("block.replaceChildren(row);");
    expect(app).toContain("renderInlineMarkdown(formatTerminalMessage(group.source, group.message)");
    expect(app).not.toContain('time.className = "terminal-entry-time";\n    block.classList.add("terminal-entry-command");');
    expect(app).toContain('if (meta.tone !== "output") {');
    expect(app).toContain('...(meta.tone === "output" ? [renderOutputDeleteButton(group)] : []),');
    expect(app).toContain("function renderOutputDeleteButton(group)");
    expect(app).toContain("function deleteOutputLogGroup(flowId, ids)");
    expect(app).toContain('await api(`/api/flows/${encodeURIComponent(flowId)}/logs`, {');
    expect(app).toContain('if (message.event === "logs-deleted")');
    expect(server).toContain('const allowedSources = new Set(["agent:output", "agent:cmd", "shell:output"]);');
    expect(server).toContain('broadcast("logs-deleted", { flowId, ids: uniqueIds });');
    expect(server).toContain('if (parts[3] === "logs" && request.method === "DELETE")');
    expect(app).not.toContain('renderInlineMarkdown(`$ ${formatTerminalMessage(group.source, group.message)}`');
    expect(css).toContain(".terminal-command-line {\n  display: flex;");
    expect(css).toContain(".terminal-output-delete");
    expect(css).toContain("position: sticky;\n  top: 8px;");
    expect(css).toContain("justify-self: end;\n  margin-bottom: -22px;");
    expect(css).toContain(".terminal-entry-output:hover > .terminal-output-delete");
    expect(css).toContain(".terminal-command-line {\n  display: flex;\n  align-items: baseline;\n  gap: 7px;\n  min-width: 0;\n  font-size: 10px;");
    expect(css).toContain(".terminal-entry-command .terminal-entry-body {\n  flex: 1 1 auto;\n  padding-left: 0;");
    expect(css).toContain(".terminal-entry-shell .terminal-entry-marker {\n  color: #22c55e;\n}");
    expect(css).toContain("body.theme-dark .terminal-entry-shell .terminal-entry-marker {\n  color: #22c55e;\n}");
    expect(css).toContain(".terminal-entry-shell.terminal-entry-command .terminal-command-line,\n.terminal-entry-shell.terminal-entry-command .terminal-entry-body {\n  font-size: 12px;\n}");
    expect(css).toContain(".terminal-entry-command .terminal-entry-body {\n  flex: 1 1 auto;\n  padding-left: 0;\n  font-size: 10px;");
    expect(css).toContain(".terminal-trace-body .terminal-entry-assistant,\n.terminal-trace-body .terminal-entry-assistant .terminal-entry-time,\n.terminal-entry-output,\n.terminal-entry-error {\n  font-size: 10px;");
  });

  test("renders terminal output with ANSI styling instead of leaking escape codes", () => {
    expect(app).toContain("function renderAnsiText(root, message)");
    expect(app).toContain("function ansiParams(value)");
    expect(app).toContain('const pattern = /\\x1b\\[([0-?]*)([ -/]*)([@-~])|\\[((?:\\d{1,3}|[;:])+)m/g;');
    expect(app).toContain('span.classList.add("ansi-bold");');
    expect(app).toContain("function ansi256ColorValue(value)");
    expect(app).toContain("function ansiTrueColorValue(red, green, blue)");
    expect(app).toContain("span.style.color = inlineColor;");
    expect(app).toContain("span.style.backgroundColor = backgroundColor;");
    expect(app).toContain('renderAnsiText(body, message);');
    expect(app).toContain('meta.tone === "output" || meta.tone === "error"');
    expect(css).toContain(".terminal-entry-output .terminal-entry-body,\n.terminal-entry-error .terminal-entry-body {\n  overflow: visible;\n  overflow-wrap: normal;\n  white-space: pre;\n}");
    expect(css).toContain(".terminal-entry-body .ansi-fg-bright-black");
    expect(css).toContain(".terminal-entry-body .ansi-fg-green");
    expect(css).toContain("body.theme-dark .terminal-entry-body .ansi-fg-cyan");
    expect(css).toContain("body.theme-dark .terminal-entry-body .ansi-fg-yellow");
    expect(css).toContain(".terminal-entry-body .ansi-dim");
    expect(css).toContain(".terminal-entry-body .ansi-underline");
  });

  test("loads recent agent turns first and fetches older logs from the load more button", () => {
    expect(app).toContain("if (flow) {\n    await loadLogs(flow.id);");
    expect(app).not.toContain("loadAllLogs");
    expect(app).toContain("function appendLogEntry(log)");
    expect(app).toContain("logIds: new Map()");
    expect(app).toContain("firstLogId: new Map()");
    expect(app).toContain("logBackfilledFlowIds: new Set()");
    expect(app).toContain("logOlderCompleteFlowIds: new Set()");
    expect(app).toContain("logOlderLoadingFlowIds: new Set()");
    expect(app).toContain("terminalVisibleTurnCounts: new Map()");
    expect(app).toContain("pendingLogRenders: new Map()");
    expect(app).toContain("logPrefetchingFlowIds: new Set()");
    expect(app).toContain("logPrefetchFailedFlowIds: new Set()");
    expect(app).toContain("logPrefetchTimer: 0");
    expect(app).toContain("pendingShellOutputRenders: new Set()");
    expect(app).toContain("state.logIds.set(flowId, new Set((state.logs.get(flowId) || []).map((entry) => Number(entry.id))));");
    expect(app).toContain("if (ids.has(id)) return false;");
    expect(app).toContain("const insertAt = list.findIndex((item) => Number(item.id) > id);");
    expect(app).toContain("ids.add(id);");
    expect(app).toContain("if (state.logBackfilledFlowIds.has(flowId))");
    expect(app).toContain("appendLogEntry(log);");
    expect(app).toContain("const appendedLogs = [];");
    expect(app).toContain("if (appendLogEntry(log)) appendedLogs.push(log);");
    expect(app).toContain("function isShellOnlyRenderLog(log)");
    expect(app).toContain("appendedLogs.length && appendedLogs.every(isShellOnlyRenderLog)");
    expect(app).toContain("renderLogs(id, { ...options, suppressIncoming: Boolean(options.suppressIncoming || shouldLoadRecent) });");
    expect(app).toContain("function scheduleTicketLogPrefetch()");
    expect(app).toContain("async function prefetchTicketLogs()");
    expect(app).toContain("const LOG_PREFETCH_DELAY_MS = 750;");
    expect(app).toContain("const LOG_PREFETCH_BATCH_SIZE = 1;");
    expect(app).toContain("const LOG_PREFETCH_MAX_FLOW_COUNT = 3;");
    expect(app).toContain("logPrefetchedFlowCount: 0");
    expect(app).toContain("if (state.logPrefetchedFlowCount >= LOG_PREFETCH_MAX_FLOW_COUNT) return;");
    expect(app).toContain("if (flowIds.length >= LOG_PREFETCH_BATCH_SIZE) break;");
    expect(app).toContain("state.logPrefetchedFlowCount += 1;");
    expect(app).toContain("await loadLogs(flowId, { suppressIncoming: true });");
    expect(app).toContain("Background prefetch should never interrupt foreground ticket work.");
    expect(app).toContain("const log = {\n        id: message.payload.id,");
    expect(app).toContain("function scheduleLogRender(id, options = {})");
    expect(app).toContain("state.logRenderFrame = requestAnimationFrame(() => {");
    expect(app).toContain("function scheduleShellOutputRender(flowId)");
    expect(app).toContain("state.shellOutputRenderFrame = requestAnimationFrame(() => {");
    expect(app).toContain("if (isShellOnlyRenderLog(log)) {\n        scheduleShellOutputRender(log.flowId);\n        return;\n      }");
    expect(app).toContain("shouldScrollToSubmittedPrompt");
    expect(app).toContain('log.flowId === state.selectedFlowId &&\n        state.messageSubmitting &&');
    expect(app).toContain("log.flowId === state.messageSubmittingFlowId");
    expect(app).toContain('scheduleLogRender(log.flowId, shouldScrollToSubmittedPrompt ? { scrollToLatest: true } : {});');
    expect(app).toContain("const LOG_PAGE_SIZE = 1000;");
    expect(app).toContain("const AGENT_TRACE_INITIAL_TURN_COUNT = 6;");
    expect(app).toContain("const AGENT_TRACE_TURN_PAGE_SIZE = 6;");
    expect(app).toContain("while (true)");
    expect(app).toContain("if (options.resetCursor) resetFlowLogWindow(id);");
    expect(app).toContain("const shouldLoadRecent = !state.logBackfilledFlowIds.has(id);");
    expect(app).toContain("let cursor = state.lastLogId.get(id) || 0;");
    expect(app).toContain("const before = state.firstLogId.get(id) || Number.MAX_SAFE_INTEGER;");
    expect(app).toContain('wsRequest("logs", { flowId: id, before, limit: LOG_PAGE_SIZE })');
    expect(app).toContain('wsRequest("logs", { flowId: id, after: cursor, limit: LOG_PAGE_SIZE })');
    expect(app).toContain("if (terminalTurnCount(groups) >= AGENT_TRACE_INITIAL_TURN_COUNT) break;");
    expect(app).toContain("if (!data.logs.length) break;");
    expect(app).toContain("if (data.logs.length < LOG_PAGE_SIZE) break;");
    expect(app).toContain("cursor = Math.max(cursor, highestLogId);");
    expect(app).toContain("state.lastLogId.set(id, Math.max(state.lastLogId.get(id) || 0, cursor));");
    expect(app).toContain("state.logBackfilledFlowIds.add(id);");
    expect(app).toContain("state.terminalVisibleTurnCounts.set(id, AGENT_TRACE_INITIAL_TURN_COUNT);");
    expect(app).toContain("async function loadOlderTerminalTraceMessages(options = {})");
    expect(app).toContain("state.terminalVisibleTurnCounts.set(flowId, visibleCount + AGENT_TRACE_TURN_PAGE_SIZE);");
    expect(app).toContain('wsRequest("logs", { flowId, before, limit: LOG_PAGE_SIZE })');
    expect(app).toContain("function visibleTerminalGroups(flowId, groups)");
    expect(app).toContain("function terminalVisibleTurnStartIndex(flowId, groups)");
    expect(app).toContain("if (!isSubmittedUserLogSource(groups[index].source)) continue;");
    expect(app).toContain("if (seenTurns >= visibleTurns) return index;");
    expect(app).toContain("function terminalTurnCount(groups)");
    expect(app).toContain("function terminalTraceCanLoadMore(flowId, groups)");
    expect(app).toContain("if (canLoadMore) appendTerminalLoadMoreButton(fragment, { loading: loadMoreLoading });");
    expect(app).toContain("function appendTerminalLoadMoreButton(fragment, options = {})");
    expect(app).toContain('button.className = "terminal-load-more";');
    expect(app).toContain('button.textContent = options.loading ? "loading..." : "load more";');
    expect(app).toContain('event.target.closest(".terminal-load-more")');
    expect(app).toContain("if (!loadMore.disabled) void loadOlderTerminalTraceMessages({ preserveScrollTop: true });");
    expect(app).toContain("terminal.scrollTop = scrollTopBeforeRender + (terminal.scrollHeight - scrollHeightBeforeRender);");
    expect(app).not.toContain("terminal.scrollTop <= 12 && !terminalAtLatest(terminal)");
    expect(css).toContain(".terminal-load-more-row");
    expect(css).toContain(".terminal-load-more {");
    expect(app).toContain("function loadSelectedFlowLogs(issueId, flowId, options = {})");
    expect(app).toContain("return loadLogs(flowId, options).catch((error) => {");
    expect(app).toContain("void loadSelectedFlowLogs(issueId, flowId, { scrollToLatest: true, suppressIncoming: true })");
    expect(app).toContain('ws.addEventListener("open", () => {');
    expect(app).toContain("requestFlowSnapshot(flow.id);");
    expect(app).toContain("if (data.flow) upsertFlow(data.flow);");
    expect(app).toContain("void loadLogs(flow.id, { resetCursor: true });");
  });

  test("keeps ticket switching scoped to the selected panes", () => {
    expect(app).toContain("function updateTicketCardsForIdentifiers(...identifiers)");
    expect(app).toContain("function updateTicketSelectionCards(previousIssueId, nextIssueId)");
    expect(app).toContain("function scheduleSelectedFlowPaneRender(issueId, flowId = \"\")");
    expect(app).toContain("function animateTicketSwitch()");
    expect(app).toContain("updateTicketSelectionCards(previousIssueId, flow.linearIssueId);");
    expect(app).toContain("updateTicketSelectionCards(previousIssueId, ticket.identifier);");
    const selectFlowBody = app.slice(app.indexOf("async function selectFlow(id)"), app.indexOf("async function openTicketInFlowPane(ticket)"));
    const openTicketBody = app.slice(app.indexOf("async function openTicketInFlowPane(ticket)"), app.indexOf("function renderFlowPane(options = {})"));
    expect(selectFlowBody).not.toContain("render();");
    expect(openTicketBody).not.toContain("render();");
    expect(selectFlowBody).toContain("renderFlowPane({ light: true });");
    expect(openTicketBody).toContain("renderFlowPane({ light: true });");
    expect(selectFlowBody).toContain("if (previousIssueId !== flow.linearIssueId) animateTicketSwitch();");
    expect(openTicketBody).toContain("if (previousIssueId !== ticket.identifier) animateTicketSwitch();");
    expect(selectFlowBody).toContain("scheduleSelectedFlowPaneRender(flow.linearIssueId, id);");
    expect(openTicketBody).toContain('scheduleSelectedFlowPaneRender(ticket.identifier, flow?.id || "");');
    expect(app).toContain("state.selectionRenderFrame = requestAnimationFrame(() => {\n    state.selectionRenderFrame = requestAnimationFrame(() => {");
    expect(app).toContain("const hasCachedLogs = flowId && ((state.logs.get(flowId) || []).length || state.logBackfilledFlowIds.has(flowId));");
    expect(app).toContain("if (flowId && !hasCachedLogs) {");
    expect(app).toContain("if (issueId === state.selectedLinearIssueId && flowId === state.selectedFlowId) renderFlowPane();");
    expect(app).toContain('if (!options.light) void loadLinearDetail(issueId);');
    expect(app).toContain('terminal.textContent = "Loading agent session.";');
    expect(app).not.toContain("const cachedLogs = state.logs.get(flow.id) || [];");
    expect(selectFlowBody).not.toContain("await loadLogs(id, { scrollToLatest: true, suppressIncoming: true });");
    expect(openTicketBody).not.toContain("await loadLogs(flow.id, { scrollToLatest: true, suppressIncoming: true });");
    expect(selectFlowBody).not.toContain("void loadFlowDiff(id);");
    expect(openTicketBody).not.toContain("void loadFlowDiff(flow.id);");
    expect(app).toContain("void loadSelectedFlowLogs(issueId, flowId, { scrollToLatest: true, suppressIncoming: true });");
    expect(app).toContain("void loadFlowDiff(flowId, { force: true });");
    expect(app).toContain("if (identifier === state.selectedLinearIssueId) renderFlowPane();");
    expect(css).toContain(".flow-content.ticket-switch-fade-in {\n  animation: ticket-switch-fade-in 100ms ease-out both;\n}");
    expect(css).toContain("@keyframes ticket-switch-fade-in");
    expect(css).toContain(".flow-content.ticket-switch-fade-in {\n    animation-duration: 1ms;");
  });

  test("renders logs in chronological order in the Agent pane", () => {
    expect(app).toContain("function scrollTerminalToLatest(terminal)");
    expect(app).toContain("requestAnimationFrame(() =>");
    expect(app).toContain("terminal.scrollTop = terminal.scrollHeight;");
    expect(app).toContain("function formatTerminalTimestamp(value)");
    expect(app).toContain("function formatTerminalElapsed(startValue, endValue)");
    expect(app).toContain('if (hours) return `${hours}h ${minutes}m ${seconds}s`;');
    expect(app).toContain('if (minutes) return `${minutes}m ${seconds}s`;');
    expect(app).toContain('return `${seconds}s`;');
    expect(app).toContain("function terminalDateLabel(date, now = new Date())");
    expect(app).toContain("return `${days}d ago`;");
    expect(app).toContain('return dateLabel ? `${time} (${dateLabel})` : time;');
    expect(app).toContain("time.textContent = formatTerminalTimestamp(group.createdAt);");
    expect(css).toContain(".terminal-entry-time {\n  margin-left: auto;\n  color: #94a3b8;\n  font-size: 11px;");
    expect(app).toContain("function terminalDistanceFromBottom(terminal)");
    expect(app).toContain("return terminal.scrollHeight - terminal.clientHeight - terminal.scrollTop;");
    expect(app).toContain("function terminalAtLatest(terminal)");
    expect(app).toContain("terminalFollowPaused: false");
    expect(app).toContain("function pauseTerminalFollow()");
    expect(app).toContain("function resumeTerminalFollow()");
    expect(app).toContain("state.terminalFollowPaused || !terminalAtLatest(terminal)");
    expect(app).toContain('els.flowPane.querySelector(".terminal").addEventListener(\n  "wheel"');
    expect(app).toContain("function deferTerminalLogRender(terminal, flowId, options = {})");
    expect(app).toContain("terminal._flowLogPending = flowId;");
    expect(app).toContain("terminal._flowLogPendingOptions = mergeLogRenderOptions(terminal._flowLogPendingOptions || {}, options);");
    expect(app).toContain("deferTerminalLogRender(terminal, id, options);");
    expect(app).toContain("terminal._flowLogPending = \"\";");
    expect(app).toContain("terminal._flowLogPendingOptions = null;");
    expect(app).toContain("function flushDeferredTerminalLogRender(terminal, options = {})");
    expect(app).toContain('els.flowPane.querySelector(".terminal").addEventListener("scroll"');
    expect(app).toContain("for (const group of groups) appendTerminalBlock(fragment, group);");
    expect(app).toContain("function terminalGroupsSignature(groups)");
    expect(app).toContain("function terminalGroupSignaturePart(group)");
    expect(app).toContain('if (group.source === "agent:trace-group" && group.traceKey && !isTerminalTraceGroupOpen(group))');
    expect(app).toContain("function syncClosedTerminalTraceChildren(terminal, groups)");
    expect(app).toContain("syncClosedTerminalTraceChildren(terminal, groups);");
    expect(app).toContain("function terminalGroupRenderKey(group)");
    expect(app).toContain("function latestTerminalContentGroup(groups)");
    expect(app).toContain("if (!flowAgentRunning(flow) || state.messageSubmittingFlowId === flow?.id) return groups;");
    expect(app).toContain("const group = latestTerminalContentGroup(groups);");
    expect(app).not.toContain("for (let index = groups.length - 1; index >= 0; index -= 1) {\n    const group = groups[index];\n    if (isLiveAgentTextSource(group.source))");
    expect(app).toContain('if (options.incoming) block.classList.add("terminal-entry-incoming");');
    expect(app).toContain("const previousKeys = terminal._flowLogFlowId === id ? terminal._flowLogRenderedKeys : null;");
    expect(app).toContain("const nextKeys = new Set(groups.map((group) => terminalGroupRenderKey(group)));");
    expect(app).toContain("const animateIncoming = !options.suppressIncoming && !agentWorking;");
    expect(app).toContain("appendTerminalBlock(fragment, group, { incoming: Boolean(animateIncoming && previousKeys && !previousKeys.has(renderKey)) });");
    expect(app).toContain("terminal._flowLogRenderedKeys = nextKeys;");
    expect(css).toContain(".terminal > .terminal-entry-incoming {\n  animation: terminal-entry-fade-in 200ms ease-out both;\n}");
    expect(css).toContain("@keyframes terminal-entry-fade-in");
    expect(css).toContain(".terminal > .terminal-entry-incoming {\n    animation-duration: 1ms;");
    expect(app).not.toContain("[...groups].reverse()");
    expect(app).not.toContain("activeFlowTab");
    expect(app).toContain("terminal._flowLogFlowId === id && terminal._flowLogSignature === signature && !options.force");
  });

  test("creates trace bundles only after turn end while keeping thoughts in the trace", () => {
    expect(server).toContain('kind === "shell" ? "shell:trace-group" : "agent:trace-group"');
    expect(server).toContain("const latestUserLogBeforeStmt = db.query(");
    expect(server).toContain("create index if not exists logs_flow_id_id_idx on logs(flowId, id);");
    expect(server).toContain("create index if not exists logs_flow_id_source_id_idx on logs(flowId, source, id);");
    expect(server).toContain("create index if not exists flows_linear_issue_id_idx on flows(linearIssueId);");
    expect(server).toContain('function createTraceGroupBetweenLogs(flowId: string, afterId: number, beforeId: number, kind = "")');
    expect(server).toContain("if (traceCount <= 1) return;");
    expect(server).toContain('createTraceGroupBetweenLogs(flow.id, commandLogId, resultLogId + 1, "shell");');
    expect(server).toContain("function isUserLogSource(source: string)");
    expect(server).toContain('source === "user" || source === "user:queued"');
    expect(server).toContain("function isAgentMessageBoundarySource(source: string)");
    expect(server).toContain('return source === "agent:message-boundary";');
    expect(server).toContain("function isTraceCountSource(source: string)");
    expect(server).toContain('return !isAgentMessageBoundarySource(source) && source !== "user:queued";');
    expect(server).toContain("function createCompletedTurnTraceGroup(flowId: string, beforeId: number)");
    expect(server).toContain("const prompt = latestUserLogBeforeStmt.get(flowId, beforeId)");
    expect(server).toContain("createTraceGroupBetweenLogs(flowId, prompt.id, beforeId);");
    expect(server).toContain("function createCompletedTurnTraceGroupAfterLog(flowId: string, afterId: number | undefined, beforeId: number)");
    expect(server).toContain("createTraceGroupBetweenLogs(flowId, afterId, beforeId);");
    expect(server).not.toContain("function createAgentTraceGroupBeforeFinalResponse");
    expect(server).not.toContain("function createTraceGroupAfterPrompt");
    expect(server).not.toContain("function createTurnTraceGroup");
    expect(server).not.toContain("createAgentTraceGroupsBetweenLogs");
    expect(server).not.toContain("finalMessageStartIndex");
    expect(server).toContain('const turnStatusLogId = insertLog(runtime.flowId, "agent:status", `turn ${turn?.status ?? "completed"}`);');
    expect(server).toContain("createCompletedTurnTraceGroupAfterLog(runtime.flowId, activeTurnTraceAfterLogId, turnStatusLogId + 1);");
    expect(server).toContain('if (item.type === "agentMessage")');
    expect(server).toContain('return { source: "agent:message-boundary", message: "" };');
    expect(server).toContain("(log) => log.id < beforeId && isTraceCountSource(log.source)");
    expect(server).not.toContain("const isSteerMessage = Boolean(message && existingRuntime?.activeTurnId);");
    expect(server).not.toContain("if (isSteerMessage) createTurnTraceGroup(flow.id, userLogId);");
    expect(server).toContain('insertLog(flow.id, "agent:error", "agent runtime disappeared while status was running\\n");');
    expect(server).not.toContain("createTurnTraceGroup(flow.id, errorLogId);");
    expect(app).toContain('"agent:trace-group": { label: "trace"');
    expect(app).toContain("function parseTraceGroup(log)");
    expect(app).toContain("function isAgentMessageSource(source)");
    expect(app).toContain("function isAgentMessageBoundarySource(source)");
    expect(app).toContain('return source === "agent:message-boundary";');
    expect(app).not.toContain("function isAgentResponseSource(source)");
    expect(app).toContain("function isUserLogSource(source)");
    expect(app).toContain('source === "user" || source === "user:queued"');
    expect(app).toContain("function isSubmittedUserLogSource(source)");
    expect(app).toContain('return source === "user";');
    expect(app).not.toContain("function syntheticRuntimeDisappearedTraceRanges(logs, existingRanges)");
    expect(app).not.toContain("function syntheticShellTraceRanges(logs, existingRanges)");
    expect(app).not.toContain("function latestInputLogId(logs)");
    expect(app).toContain('if (log.source !== "agent:trace-group" && log.source !== "shell:trace-group") return null;');
    expect(app).toContain("if (count <= 1) return null;");
    expect(app).toContain('defaultOpen: false');
    expect(app).toContain("group.defaultOpen || (group.flowId && group.traceKey");
    expect(app).not.toContain("function syntheticSteerTraceRanges(logs, existingRanges)");
    expect(app).toContain("function syntheticCompletedTurnTraceRanges(logs, existingRanges)");
    expect(app).not.toContain("function isTurnCompletedLog(log)");
    expect(app).toContain("function isShellExitLog(log)");
    expect(app).toContain("function isRoutineShellExitLog(log)");
    expect(app).toContain("function sanitizeTraceRanges(logs, ranges)");
    expect(app).toContain("function traceRangeShouldIncludeUserLogs(range, rangeLogs)");
    expect(app).toContain('if (range.kind === "compact") return true;');
    expect(app).toContain(
      "return rangeLogs.some((log) => isSubmittedUserLogSource(log.source)) && rangeLogs.some((log) => isAgentTurnEndedLog(log));",
    );
    expect(app).toContain("const includeUserLogs = traceRangeShouldIncludeUserLogs(range, rangeLogs);");
    expect(app).toContain("addSanitizedTraceRange(result, seen, logs, { ...range, includeUserLogs }, range.afterId, beforeId, countRange);");
    expect(app).toContain("function isTraceGroupSource(source)");
    expect(app).toContain("function isStructuralTerminalSource(source)");
    expect(app).toContain("return isTraceGroupSource(source) || isAgentMessageBoundarySource(source);");
    expect(app).toContain("function isTraceContentLog(log)");
    expect(app).toContain('return !isStructuralTerminalSource(log.source) && log.source !== "user:queued";');
    expect(app).toContain("function finalResponseStartIdForTrace(range, rangeLogs)");
    expect(app).toContain("function lowerBound(values, target)");
    expect(app).toContain("function upperBound(values, target)");
    expect(app).toContain("function traceRangeLogCounter(logs)");
    expect(app).toContain("function sortedTraceRanges(ranges)");
    expect(app).toContain("function addSanitizedTraceRange(result, seen, logs, range, afterId, beforeId, countRange = traceRangeLogCounter(logs))");
    expect(app).toContain("if (!isSubmittedUserLogSource(log.source)) continue;");
    expect(app).toContain("addSanitizedTraceRange(result, seen, logs, range, segmentAfterId, log.id, countRange);");
    expect(app).toContain("addSanitizedTraceRange(result, seen, logs, range, segmentAfterId, beforeId, countRange);");
    expect(app).not.toContain("function isFinalResponseLogForTrace(log, traceRange, logs)");
    expect(app).not.toContain('String(log.message || "").trim() === "agent runtime disappeared while status was running"');
    expect(app).not.toContain("const runtimeDisappearedTraceRanges = syntheticRuntimeDisappearedTraceRanges(normalizedLogs, persistedTraceRanges);");
    expect(app).toContain("const agentTurnEndIds = normalizedLogs");
    expect(app).toContain("function logIdsHaveRangeEntry(ids, afterId, beforeId)");
    expect(app).toContain('range && range.kind !== "shell" && (range.kind || logIdsHaveRangeEntry(agentTurnEndIds, range.afterId, range.beforeId))');
    expect(app).toContain("const completedTurnTraceRanges = syntheticCompletedTurnTraceRanges(normalizedLogs, persistedTraceRanges);");
    expect(app).toContain("if (count <= 1) continue;");
    expect(app).toContain(".filter(isTraceContentLog)");
    expect(app).toContain('range.kind !== "shell"');
    expect(server).toContain("createCompletedTurnTraceGroupAfterLog(runtime.flowId, activeTurnTraceAfterLogId, turnStatusLogId + 1);");
    expect(app).toContain("function isShellTraceGroupLog(log)");
    expect(app).toContain("if (isShellTraceGroupLog(log)) continue;");
    expect(app).toContain('kind: typeof payload.kind === "string" ? payload.kind : ""');
    expect(app).not.toContain('ranges.push({ afterId, beforeId, count, key, kind: "shell" });');
    expect(app).toContain("if (isHiddenTerminalLog(log)) continue;");
    expect(app).toContain("if (isAgentMessageBoundarySource(log.source)) return true;");
    expect(app).toContain(
      'if (traceRange && log.source !== "user:queued" && (!isSubmittedUserLogSource(log.source) || traceRange.includeUserLogs)) {',
    );
    expect(app).toContain("const traceRanges = sortedTraceRanges(");
    expect(app).toContain("sanitizeTraceRanges(normalizedLogs, [");
    expect(app).toContain("let traceRangeIndex = 0;");
    expect(app).toContain("while (traceRanges[traceRangeIndex] && traceRanges[traceRangeIndex].beforeId <= logId) traceRangeIndex += 1;");
    expect(app).toContain("...completedTurnTraceRanges,");
    expect(app).not.toContain("...runtimeDisappearedTraceRanges,");
    expect(app).not.toContain("...shellTraceRanges,");
    expect(app).not.toContain("...syntheticSteerTraceRanges(normalizedLogs, [");
    expect(app).toContain("function appendTerminalTraceGroup(fragment, group, options = {})");
    expect(app).toContain("function flattenSingleChildTraceGroups(groups)");
    expect(app).toContain('if (group.source === "agent:trace-group") {');
    expect(app).toContain("if (children.length <= 1) {");
    expect(app).toContain("function mergeAdjacentStreamingGroups(groups)");
    expect(app).toContain("previous.message += group.message;");
    expect(app).toContain("!group.boundaryBefore");
    expect(app).toContain("let forceTerminalGroupBoundary = false;");
    expect(app).toContain("forceTerminalGroupBoundary = true;");
    expect(app).toContain("appendTerminalGroup(traceGroup.children, log, { forceNew: forceTerminalGroupBoundary, traceContent: true });");
    expect(app).toContain("appendTerminalGroup(groups, log, { forceNew: forceTerminalGroupBoundary });");
    expect(app).toContain("boundaryBefore: Boolean(options.forceNew)");
    expect(app).toContain("function markLiveTerminalGroup(groups, flow)");
    expect(app).toContain("const visibleGroups = mergeAdjacentStreamingGroups(flattenSingleChildTraceGroups(groups));");
    expect(app).toContain("markPendingTerminalTurnGroups(visibleGroups, flow);");
    expect(app).toContain("markLiveTerminalGroup(visibleGroups, flow);");
    expect(app).toContain("return visibleGroups;");
    expect(app).toContain("openTraceGroups: new Map()");
    expect(app).toContain("traceKey: traceRange.key,");
    expect(app).toContain("flowId: log.flowId,");
    expect(app).toContain('const elapsed = formatTerminalElapsed(group.createdAt, group.lastAt);');
    expect(app).toContain('label.textContent = elapsed ? `${meta.label} (${elapsed})` : meta.label;');
    expect(app).toContain('details.dataset.traceKey = group.traceKey || "";');
    expect(app).toContain("details._traceChildren = group.children || [];");
    expect(app).toContain("if (isTerminalTraceGroupOpen(group)) {");
    expect(app).toContain("function setTerminalTraceGroupOpen(details, open)");
    expect(app).toContain("setTerminalTraceGroupOpen(details, shouldOpen);");
    expect(app).toContain("details.replaceChildren(summary);");
    expect(app).toContain("function materializeTerminalTraceGroup(details)");
    expect(app).toContain("details.open = true;\n    materializeTerminalTraceGroup(details);");
    expect(app).toContain("const TERMINAL_TRACE_INITIAL_RENDER_COUNT = 18;");
    expect(app).toContain("const TERMINAL_TRACE_RENDER_BATCH_SIZE = 48;");
    expect(app).toContain("const TERMINAL_TRACE_OPEN_DURATION_MS = 90;");
    expect(app).toContain("const TERMINAL_TRACE_CLOSE_DURATION_MS = 80;");
    expect(app).toContain("function scheduleTerminalTraceRender(details, options = {})");
    expect(app).toContain("function renderTerminalTraceChunk(details, options = {})");
    expect(app).toContain("if ((details._traceRenderIndex || 0) > 0) scheduleTerminalTraceRender(details);");
    expect(app).toContain('collapseButton.className = "terminal-trace-collapse-line";');
    expect(app).toContain('collapseButton.setAttribute("aria-label", "Collapse trace");');
    expect(app).toContain("event.stopPropagation();");
    expect(app).toContain('content.className = "terminal-trace-body-content";');
    expect(app).toContain("details._traceBodyContent = content;");
    expect(app).toContain("body.replaceChildren(collapseButton, content);");
    expect(app).toContain("if (!children.length) {");
    expect(app).toContain("details._traceRenderIndex = 0;");
    expect(app).toContain("details._traceRenderIndex = children.length;");
    expect(app).toContain("renderTerminalTraceChunk(details, { maxItems: TERMINAL_TRACE_INITIAL_RENDER_COUNT, scheduleNext: false });");
    expect(app).toContain("scheduleTerminalTraceRender(details, { delay: TERMINAL_TRACE_OPEN_DURATION_MS });");
    expect(app).toContain("if (!Number.isFinite(details._traceRenderIndex)) details._traceRenderIndex = children.length;");
    expect(app).toContain("if (details._traceRenderIndex <= 0) return;");
    expect(app).toContain("details._traceRenderFrame = requestAnimationFrame(() => renderTerminalTraceChunk(details));");
    expect(app).toContain("if (options.delay) {");
    expect(app).toContain("details._traceRenderTimer = window.setTimeout(run, options.delay);");
    expect(app).toContain("if (!body || !content || !details.open) return;");
    expect(app).toContain("const endIndex = Number.isFinite(details._traceRenderIndex) ? details._traceRenderIndex : children.length;");
    expect(app).toContain("const maxItems = Math.max(1, options.maxItems || TERMINAL_TRACE_RENDER_BATCH_SIZE);");
    expect(app).toContain("const startIndex = Math.max(0, endIndex - maxItems);");
    expect(app).toContain("for (let index = endIndex - 1; index >= startIndex; index -= 1) {");
    expect(app).toContain("content.appendChild(fragment);");
    expect(app).toContain("for (const entry of appended) applyTerminalMessageClamps(entry);");
    expect(app).toContain("if (startIndex > 0 && options.scheduleNext !== false) scheduleTerminalTraceRender(details);");
    expect(app).toContain("function cancelTerminalTraceRender(details)");
    expect(app).toContain("cancelTerminalTraceRender(details);");
    expect(app).toContain("if (details._traceRenderTimer) window.clearTimeout(details._traceRenderTimer);");
    expect(app).toContain('message: "",');
    expect(app).not.toContain('event${traceRange.count === 1 ? "" : "s"}');
    expect(app).not.toContain('message: `${traceRange.count || 0} trace event');
    expect(app).toContain('details.className = "terminal-trace-group";');
    expect(app).toContain("void details.offsetHeight;");
    expect(app).not.toContain("function setTerminalTraceBodyHeight(details)");
    expect(app).not.toContain('body.style.setProperty("--trace-body-height"');
    expect(app).not.toContain("function traceFoldDelay(index)");
    expect(app).not.toContain("--trace-open-delay");
    expect(app).not.toContain("--trace-close-delay");
    expect(app).toContain('event.preventDefault();');
    expect(app).toContain("toggleTerminalTraceGroup(details);");
    expect(app).toContain("function toggleTerminalTraceGroup(details)");
    expect(app).toContain('const terminal = details.closest(".terminal");');
    expect(app).toContain("const shouldFollowLatest = terminal && !state.terminalFollowPaused && terminalAtLatest(terminal);");
    expect(app).toContain("if (shouldFollowLatest) followTerminalToLatestDuringLayout(terminal, duration + 40);");
    expect(app).toContain("function terminalTraceInteractionActive(terminal)");
    expect(app).toContain('return Boolean(terminal?.querySelector(".terminal-trace-animating"));');
    expect(app).toContain("terminalTraceInteractionActive(terminal) && !options.fromTraceFlush");
    expect(app).toContain("function mergeLogRenderOptions(existing = {}, incoming = {})");
    expect(app).toContain("fromTraceFlush: Boolean(existing.fromTraceFlush || incoming.fromTraceFlush)");
    expect(app).toContain("flushDeferredTerminalLogRender(terminal, {\n      fromTraceFlush: true,");
    expect(app).toContain("details._traceBody?.remove();");
    expect(app).toContain("details._traceBody = null;");
    expect(app).toContain("details._traceBodyContent = null;");
    expect(app).toContain("details._traceRenderIndex = null;");
    expect(app).toContain("if (shouldFollowLatest) scrollTerminalToLatestNow(terminal);");
    expect(app).toContain('body.className = "terminal-trace-body";');
    expect(css).toContain(".terminal-trace-group");
    expect(css).toContain(".terminal > .terminal-trace-group:last-child:not([open]) {\n  padding-bottom: 10px;\n}");
    expect(css).toContain("user-select: none;");
    expect(css).toContain(".terminal-trace-group:not([open]) > .terminal-trace-summary .terminal-entry-time");
    expect(css).toContain(".terminal-trace-body");
    expect(css).toContain(".terminal-trace-summary {\n  order: 2;\n}");
    expect(css).toContain(".terminal-trace-body {\n  order: 1;");
    expect(css).toContain(".terminal-trace-body-content {\n  display: flex;\n  flex-direction: column-reverse;");
    expect(css).toContain(".terminal-trace-collapse-line {\n  position: absolute;");
    expect(css).toContain("inset: 2px auto 8px 7px;");
    expect(css).toContain(".terminal-trace-collapse-line::before");
    expect(css).toContain(".terminal-trace-collapse-line:hover::before,\n.terminal-trace-collapse-line:focus-visible::before");
    expect(css).toContain("padding: 2px 0 8px 23px;");
    expect(css).toContain("transform-origin: bottom;");
    expect(css).toContain(".terminal-trace-animating > .terminal-trace-body {\n  will-change: opacity, transform;\n}");
    expect(css).toContain(".terminal-trace-opening > .terminal-trace-body {\n  animation: terminal-trace-fold-in 90ms cubic-bezier(0.2, 0, 0, 1) both;\n}");
    expect(css).toContain(".terminal-trace-closing > .terminal-trace-body {\n  animation: terminal-trace-fold-out 80ms cubic-bezier(0.4, 0, 1, 1) both;\n}");
    expect(css).toContain("transform: translateY(4px);");
    expect(css).not.toContain("height: var(--trace-body-height, 0px);");
    expect(css).not.toContain("animation-delay: var(--trace-open-delay, 0ms);");
    expect(css).not.toContain("animation-delay: var(--trace-close-delay, 0ms);");
  });

  test("repaints agent logs after navigating through a ticket without a flow", () => {
    expect(app).toContain('terminal._flowLogFlowId = "";');
    expect(app).toContain('terminal._flowLogSignature = "";');
    expect(app).toContain("terminal._flowLogRenderedKeys = null;");
    expect(app).toContain('terminal.textContent = "No agent session yet.";');
    expect(app).toContain("terminal._flowLogFlowId === id && terminal._flowLogSignature === signature && !options.force");
    expect(app).toContain("terminal._flowLogFlowId = id;");
  });

  test("logs the user prompt before starting a new Codex runtime", () => {
    const promptLog = 'const userLogId = message ? insertLog(flow.id, "user", `${userMessage}\\n`) : 0;';
    const startAgentBody = server.slice(server.indexOf("async function startAgent("));
    expect(startAgentBody).toContain(promptLog);
    expect(startAgentBody.indexOf(promptLog)).toBeLessThan(
      startAgentBody.indexOf("runtime = existingRuntime;"),
    );
  });

  test("supports slash command suggestions and dispatch", () => {
    expect(app).toContain('const SLASH_COMMANDS = [');
    expect(app).toContain('name: "/clear"');
    expect(app).toContain('name: "/compact"');
    expect(app).toContain('name: "/effort"');
    expect(app).toContain('name: "/fast"');
    expect(app).toContain('name: "/model"');
    expect(app).toContain('name: "/permissions"');
    expect(app).toContain('name: "/plugins"');
    expect(app).toContain('name: "/review"');
    expect(app).toContain('"/effort": ["xhigh", "high", "medium", "low"].map((effort) => ({');
    expect(app).toContain("const AGENT_MODELS = [");
    expect(app).toContain('"claude-haiku-4-5",');
    expect(app).toContain('"/model": AGENT_MODELS.map((model) => ({');
    expect(app).toContain('"/permissions": [');
    expect(app).toContain('{ name: "/permissions read-only", description: "Read files only" }');
    expect(app).toContain('{ name: "/permissions workspace-write", description: "Write inside the workspace" }');
    expect(app).toContain('{ name: "/permissions full-access", description: "Full filesystem access", icon: DEFAULT_FAVICON_HREF }');
    expect(app).toContain('icon.className = "slash-command-icon";');
    expect(css).toContain(".slash-command-icon");
    expect(app).toContain('"/review": [');
    expect(app).toContain('{ name: "/review base", description: "Review against a base branch (PR style)" }');
    expect(app).toContain('{ name: "/review uncommitted", description: "Review uncommitted changes" }');
    expect(app).toContain('{ name: "/review commit", description: "Review a commit" }');
    expect(app).toContain('{ name: "/review custom", description: "Custom review instructions" }');
    expect(app).toContain('{ name: "/status", description: "Show current flow status" }');
    expect(app).toContain('const SLASH_COMMANDS_WITH_ARGUMENTS = ["/review base", "/review commit", "/review custom"];');
    expect(app).toContain('description: "",');
    expect(app).toContain("function slashCommandExpansionMatches(query)");
    expect(app).toContain("function slashCommandHasExpansions(commandName)");
    expect(app).toContain("function slashCommandAllowsArguments(commandName)");
    expect(app).toContain("function validSlashCommand(value)");
    expect(app).toContain("slashCommandAllowsArguments(command.name) && commandName.startsWith(`${command.name} `)");
    expect(app).toContain("if (expansions.length) return expansions;");
    expect(app).toContain("function renderSlashMenu()");
    expect(app).toContain("function selectSlashCommand");
    expect(app).toContain("function resizeMessageInput()");
    expect(app).toContain("const currentHeight = input.getBoundingClientRect().height;");
    expect(app).toContain("const targetHeight = input.scrollHeight;");
    expect(app).toContain('input.style.height = `${currentHeight}px`;');
    expect(app).toContain('input.style.height = `${targetHeight}px`;');
    expect(app).toContain('const terminal = els.flowPane.querySelector(".terminal");');
    expect(app).toContain("const shouldFollowLatest = !state.terminalFollowPaused && terminalAtLatest(terminal);");
    expect(app).toContain('input.style.height = "auto";');
    expect(app).toContain("if (!currentHeight || Math.abs(currentHeight - targetHeight) < 1) {");
    expect(app).toContain("if (shouldFollowLatest) followTerminalToLatestDuringLayout(terminal, 160);");
    expect(app).toContain('if (!query.startsWith("/")) return [];');
    expect(app).toContain(".sort((a, b) => a.name.localeCompare(b.name))");
    expect(app).not.toContain("SLASH_COMMAND_EXPANSIONS[command] = sortSlashCommands(expansions);");
    expect(app).toContain('promptInput().addEventListener("keydown"');
    expect(app).toContain('if (event.key === "Enter" && !event.shiftKey && !event.isComposing)');
    expect(app).toContain("input.form?.requestSubmit();");
    expect(app).toContain("resizeMessageInput();");
    expect(app).toContain("if (input.value.trim() !== command.name) {");
    expect(app).toContain("if (slashCommandHasExpansions(command.name))");
    expect(app).toContain('if (input.value.trim().startsWith("/") && !validSlashCommand(input.value)) return;');
    expect(app).toContain('els.flowPane.querySelector(".slash-menu").addEventListener("mouseover"');
    expect(app).toContain('els.flowPane.querySelector(".slash-menu").addEventListener("mousedown"');
    expect(server).toContain("function parseSlashCommand(message: string)");
    expect(server).toContain('"thread/compact/start"');
    expect(server).toContain('codexThreadParams(flow, "clear")');
    expect(server).toContain('sessionStartSource,');
    expect(server).toContain('insertLog(flow.id, "agent:status", "context cleared")');
    expect(server).toContain('insertLog(flow.id, "agent:status", "compact requested")');
    expect(server).toContain("runtime.compacting = true;");
    expect(server).toContain("runtime.compactingStartedAt = Date.now();");
    expect(server).toContain("runtime.compactionPromptLogId = promptLogId;");
    expect(server).toContain("runtime.compacting = false;");
    expect(server).toContain("runtime.compactingStartedAt = undefined;");
    expect(server).toContain("runtime.compactionPromptLogId = undefined;");
    const turnStartedHandler = server.slice(
      server.indexOf('if (method === "turn/started")'),
      server.indexOf('if (method === "turn/completed")'),
    );
    expect(turnStartedHandler).not.toContain("runtime.compacting = false;");
    const turnCompletedHandler = server.slice(
      server.indexOf('if (method === "turn/completed")'),
      server.indexOf('if (method === "thread/compacted")'),
    );
    expect(server).toContain("function finishCodexCompaction(runtime: RuntimeProcess");
    expect(server).toContain("updateRuntimeThreadFromParams(runtime, params);");
    expect(server).toContain("const compactionPromptLogId = runtime.compactionPromptLogId;");
    expect(server).toContain('const contextCompactedLogId = insertLog(runtime.flowId, "agent:status", "context compacted");');
    expect(server).toContain("deleteQueuedAgentMessagePlaceholders(runtime);");
    expect(server).toContain(
      'if (compactionPromptLogId) createTraceGroupBetweenLogs(runtime.flowId, compactionPromptLogId, contextCompactedLogId + 1, "compact");',
    );
    expect(turnCompletedHandler).toContain('if (runtime.compacting && turn?.status !== "failed") {');
    expect(turnCompletedHandler).toContain("finishCodexCompaction(runtime, params);");
    expect(turnCompletedHandler.indexOf("finishCodexCompaction(runtime, params);")).toBeLessThan(
      turnCompletedHandler.indexOf('insertLog(runtime.flowId, "agent:status", `turn ${turn?.status ?? "completed"}`);'),
    );
    const threadCompactedHandler = server.slice(
      server.indexOf('if (method === "thread/compacted")'),
      server.indexOf('if (method === "model/rerouted")'),
    );
    expect(threadCompactedHandler).toContain("if (runtime.compacting) finishCodexCompaction(runtime, params);");
    expect(threadCompactedHandler).toContain("else updateRuntimeThreadFromParams(runtime, params);");
    expect(server).toContain('insertLog(flowId, "agent:status", "compact interrupted")');
    expect(server).toContain("runtime.queuedAgentMessages = [];");
    expect(server).toContain("queuedAgentMessages?: Array<{ message: string; queuedLogId: number }>");
    expect(server).toContain("activeTurnTraceAfterLogId?: number;");
    expect(server).toContain("compactionPromptLogId?: number;");
    expect(server).toContain("runtime.activeTurnTraceAfterLogId = userLogId || undefined;");
    expect(server).toContain("createCompletedTurnTraceGroupAfterLog(runtime.flowId, activeTurnTraceAfterLogId, turnStatusLogId + 1);");
    expect(server).toContain("function startNextQueuedAgentMessage(runtime: RuntimeProcess)");
    expect(server).toContain("create table if not exists queued_prompts");
    expect(server).toContain("function queuedPromptForFlow(flowId: string)");
    expect(server).toContain("function setQueuedPrompt(flowId: string, message: string)");
    expect(server).toContain("const message = String(value ?? \"\");");
    expect(server).toContain("if (!message.trim()) throw new Error(\"Queued message cannot be blank.\");");
    expect(server).toContain("async function startQueuedPromptIfReady(flowId: string)");
    expect(server).toContain("async function steerQueuedPrompt(flow: Flow)");
    expect(server).toContain("function isCompactSlashCommand(message: string)");
    expect(server).toContain('if (isCompactSlashCommand(queued.message)) throw new Error("Queued /compact cannot be steered.");');
    expect(server).toContain('parts[3] === "queued-prompt"');
    expect(server).toContain('parts[4] === "steer"');
    expect(server).toContain('parts[4] === "flush"');
    expect(server).toContain("function deleteQueuedUserLog(flowId: string, id: number)");
    expect(server).toContain("function deleteQueuedAgentMessagePlaceholders(runtime: RuntimeProcess)");
    expect(server).toContain('log.source !== "user:queued"');
    expect(server).toContain("deleteLogByIdStmt.run(id);");
    expect(server).toContain('broadcast("logs-deleted", { flowId, ids: [id] });');
    expect(server).toContain("deleteQueuedUserLog(flow.id, queued.queuedLogId);");
    expect(server).toContain('const userLogId = insertLog(flow.id, "user", `${queued.message}\\n`);');
    const startAgentBody = server.slice(
      server.indexOf("async function startAgent("),
      server.indexOf("function startShellCommand("),
    );
    expect(startAgentBody.indexOf("existingRuntime?.compacting")).toBeLessThan(
      startAgentBody.indexOf("handleSlashCommand(flow, userMessage)"),
    );
    expect(server).toContain('throw new Error("A message is already queued until context compaction finishes.");');
    expect(server).toContain('insertLog(flow.id, "agent:status", "message queued until context compaction finishes")');
    expect(server).toContain('const queuedLogId = insertLog(flow.id, "user:queued", `${userMessage}\\n`);');
    expect(server).toContain("existingRuntime.queuedAgentMessages.push({ message: userMessage, queuedLogId });");
    expect(server).toContain("await compactCodexThread(runtime, flow, userLogId);");
    expect(server).toContain("function formatCodexPluginList(stdout: string)");
    expect(server).toContain('cmd: ["codex", "plugin", "list", "--json"]');
    expect(server).toContain('"| Plugin | Status | Version |"');
    expect(server).toContain('if (stdout) insertLog(flow.id, "agent:message", `${formatCodexPluginList(stdout)}\\n`);');
    expect(server).toContain('if (command === "/plugins")');
    expect(server).toContain("await listCodexPlugins(flow);");
    expect(server).toContain("async function flowStatusMessage(runtime: RuntimeProcess)");
    expect(server).toContain('if (command === "/status")');
    expect(server).toContain('sendCodexRequest(runtime, "account/read", { refreshToken: false })');
    expect(server).toContain('sendCodexRequest(runtime, "account/rateLimits/read")');
    expect(server).toContain('const runtime = await ensureCodexRuntime(flow);');
    expect(server).toContain('insertLog(flow.id, "agent:message", `${await flowStatusMessage(runtime)}\\n`);');
    expect(server).toContain('return ["| Field | Value |", "| --- | --- |"');
    expect(server).toContain('["5h left", statusPercentLeft(rateLimits.primary)]');
    expect(server).toContain('["Weekly left", statusPercentLeft(rateLimits.secondary)]');
    expect(server).toContain('["Account", accountStatusValue(account)]');
    expect(server).not.toContain("| Section |");
    expect(server).not.toContain('["Turbopump",');
    expect(server).not.toContain('["Codex", "Auth"');
    expect(server).not.toContain('["Codex", "Model"');
    expect(server).not.toContain('["Codex", "Thread"');
    expect(server).toContain('type AgentSandbox = "read-only" | "workspace-write" | "danger-full-access";');
    expect(server).toContain('const agentSandboxes = new Set<AgentSandbox>(["read-only", "workspace-write", "danger-full-access"]);');
    expect(server).toContain("function codexSandboxMode(flow: Flow): AgentSandbox");
    expect(server).toContain("function codexSandboxPolicy(flow: Flow)");
    expect(server).toContain('if (sandbox === "read-only") return { type: "readOnly", networkAccess: true };');
    expect(server).toContain('if (sandbox === "workspace-write")');
    expect(server).toContain('return { type: "dangerFullAccess" };');
    expect(server).toContain('if (command === "/permissions")');
    expect(server).toContain("function agentSandboxForSlashCommand(value: string): AgentSandbox | \"\"");
    expect(server).toContain('return sandbox === "full-access" ? "danger-full-access"');
    expect(server).toContain('if (!agentSandbox) throw new Error("Usage: /permissions read-only|workspace-write|full-access");');
    expect(server).toContain('updateFlow(flow.id, { agentSandbox });');
    expect(server).toContain('insertLog(flow.id, "agent:status", `permissions set to ${agentSandbox}`);');
    expect(server).toContain("void startNextQueuedAgentMessage(runtime);");
    expect(server).toContain('updateFlow(flow.id, { agentStatus: "running" });');
    expect(server).toContain("const reviewFindingInstructions =");
    expect(server).toContain("function reviewPromptForSlashCommand(message: string)");
    expect(server).toContain('if (command === "/review")');
    expect(server).toContain("await startAgentTurnAfterUserLog(flow, userLogId, reviewPromptForSlashCommand(message));");
    expect(server).toContain('"Usage: /review base [branch]|uncommitted|commit [ref]|custom [instructions]"');
    expect(app).toContain("function canSubmitPromptMessage()");
    expect(app).not.toContain("const agentCanAcceptMessage =");
    expect(app).toContain("!flowAgentRunning(flow) &&\n      !promptQueuedForFlow(flow)");
    expect(app).toContain("if (flowAgentRunning(flow)) {");
    expect(app).toContain("if (flowAgentQueuedMessage(flow)) {");
    expect(app).toContain("flashBlockedInput(input);\n      return;");
    expect(app).toContain("scheduleQueuedPromptFlush();");
    expect(app).toContain('promptInput().addEventListener("beforeinput"');
    expect(app).toContain("if (handleQueuedPromptKeydown(event)) return;");
    expect(app).toContain("promptQueuedCanSteer() &&");
    expect(app).toContain("if (!event.repeat) void submitQueuedPromptSteer();");
    expect(app).toContain('if (source === "user:queued") return { label: userLabel, marker: "o", tone: "user" };');
    expect(app).toContain('["user", "user:queued", "agent", "agent:message", "agent:thinking", "agent:reasoning"].includes(source)');
    expect(app).toContain("if (data.flow) upsertFlow(data.flow);");
    expect(app).toContain("function canSubmitShellCommand()");
    expect(app).not.toContain("agentInterrupt.disabled");
  });

  test("focuses the message input when typing from non-editable chrome", () => {
    expect(app).toContain("function isEditableKeyTarget(target)");
    expect(app).toContain('target.closest("input, textarea, select")');
    expect(app).toContain("target.closest(\"[contenteditable]:not([contenteditable='false'])\")");
    expect(app).toContain("function shouldFocusMessageInputForKey(event)");
    expect(app).toContain("if (event.defaultPrevented || event.isComposing) return false;");
    expect(app).toContain("if (event.metaKey || event.ctrlKey || event.altKey) return false;");
    expect(app).toContain("if (event.key.length !== 1) return false;");
    expect(app).toContain("if (isEditableKeyTarget(event.target)) return false;");
    expect(app).toContain("return Boolean(input && document.activeElement !== input);");
    expect(app).toContain("function focusMessageInputForKey(event)");
    expect(app).toContain("function focusShellInputPaneForShortcut()");
    expect(app).toContain("if (state.shellPaneHidden) {\n    revealShellPaneForInputFocus();\n    return true;\n  }\n  return focusInputPane(\"shell\");");
    expect(app).toContain('if (event.key === "$" && focusShellInputPaneForShortcut()) return true;');
    const focusMessageInput = app.slice(app.indexOf("function focusMessageInputForKey(event)"));
    expect(focusMessageInput.indexOf('if (event.key === "$" && focusShellInputPaneForShortcut()) return true;')).toBeLessThan(
      focusMessageInput.indexOf("if (promptQueuedForSelectedFlow())"),
    );
    expect(app).toContain("input.setRangeText(event.key, start, end, \"end\");");
    expect(app).toContain('input.dispatchEvent(new Event("input", { bubbles: true }));');
    expect(app).toContain("if (focusMessageInputForKey(event)) return;");
  });

  test("toggles the theme with Cmd+E", () => {
    expect(html).toContain("<kbd>Cmd + E</kbd>");
    expect(html).toContain("<dd>Toggle theme</dd>");
    expect(app).toContain("function handleCommandE(event)");
    expect(app).toContain('event.key.toLowerCase() !== "e"');
    expect(app).toContain("event.preventDefault();");
    expect(app).toContain("event.stopImmediatePropagation();");
    expect(app).toContain("toggleTheme();");
    expect(app).toContain('document.addEventListener("keydown", handleCommandE, true);');
  });

  test("clears the shell input pane with Cmd+K", () => {
    expect(app).toContain("function handleCommandK(event)");
    expect(app).toContain('event.key.toLowerCase() !== "k"');
    expect(app).toContain('if (focusedInputPaneKind() !== "shell") return false;');
    expect(app).toContain('if (!event.repeat) void submitShellCommand("clear");');
    expect(app).toContain('document.addEventListener("keydown", handleCommandK, true);');
  });

  test("keeps Shift+Enter in the shell input from submitting the agent form", () => {
    expect(app).toContain('if (event.key === "Enter" && !event.isComposing) {\n    event.preventDefault();\n    if (event.shiftKey) return;\n    void submitShellCommand(input.value);\n  }');
  });

  test("toggles the shell pane with Cmd+Backslash", () => {
    expect(html).toContain('class="pane-rail shell-pane-rail"');
    expect(html).toContain('aria-label="Expand Shell pane"');
    expect(app).toContain('const SHELL_PANE_HIDDEN_KEY = "flow.shellPaneHidden";');
    expect(app).toContain("shellPaneHidden: initialBooleanSetting(SHELL_PANE_HIDDEN_KEY)");
    expect(app).toContain("function setShellPaneHidden(hidden)");
    expect(app).toContain("const restoreTerminalBottom = terminalBottomRestorer();");
    expect(app).toContain('document.body.classList.toggle("shell-pane-hidden", hidden);');
    expect(app).toContain('const shellPanel = els.flowPane.querySelector(".shell-command-panel");');
    expect(app).toContain('const shellRail = els.flowPane.querySelector(".shell-pane-rail");');
    expect(app).toContain('if (shellPanel) shellPanel.setAttribute("aria-hidden", String(hidden));');
    expect(app).toContain('if (shellRail) shellRail.setAttribute("aria-expanded", String(!hidden));');
    expect(app).toContain('if (hidden && focusedInputPaneKind() === "shell") focusInputPane("prompt");');
    expect(app).toContain("renderShellOutputPane(state.selectedFlowId);");
    expect(app).toContain("restoreTerminalBottom();");
    expect(app).toContain("function toggleShellPaneHidden()");
    expect(app).toContain("function revealShellPaneForInputFocus()");
    expect(app).toContain("setShellPaneHidden(false);\n  focusInputPane(\"shell\");");
    expect(app).not.toContain("shell-pane-instant-reveal");
    expect(app).not.toContain("scrollRestoreDurationMs: 0");
    expect(app).toContain("function handlePaneVisibilityShortcuts(event)");
    expect(app).toContain('event.code === "Backslash" || key === "\\\\"');
    expect(app).toContain('document.addEventListener("keydown", handlePaneVisibilityShortcuts, true);');
    expect(app).toContain("const hasGroups = Boolean(groups.length);");
    expect(app).not.toContain("function preserveTerminalScrollTopDuringLayout");
    expect(app).not.toContain("function freezeTerminalLayoutForPaneAnimation");
    expect(app).not.toContain("function terminalScrollTopPreserverForPaneLayout");
    expect(app).toContain("function terminalBottomRestorer()");
    expect(app).toContain("const distanceFromBottom = terminalDistanceFromBottom(terminal);");
    expect(app).toContain("if (terminalAtLatest(terminal))");
    expect(app).toContain("terminal.scrollTop = terminal.scrollHeight;");
    expect(app).toContain("const anchor = terminalBottomTextAnchor(terminal);");
    expect(app).toContain("function terminalBottomTextAnchor(terminal)");
    expect(app).toContain("document.createTreeWalker(terminal, NodeFilter.SHOW_TEXT");
    expect(app).toContain("function terminalTextAnchorBottom(anchor)");
    expect(app).toContain("terminal.scrollTop += anchorBottom - anchor.bottom;");
    expect(app).toContain("terminal.scrollTop = terminal.scrollHeight - terminal.clientHeight - distanceFromBottom;");
    expect(app).not.toContain("[shell-pane-layout]");
    expect(app).toContain('if (kind === "shell" && state.shellPaneHidden) return false;');
    expect(app).toContain("if (state.shellPaneHidden) promptInput()?.focus();\n    else shellInput()?.focus();");
    expect(css).toContain("body.shell-pane-hidden .prompt-input-pane {\n  padding-right: 14px;\n}");
    expect(css).toContain(".terminal-panel {\n  --shell-resizer-size: 8px;");
    expect(css).toContain("transition:\n    --shell-pane-size var(--motion-pane),\n    --shell-resizer-size var(--motion-pane);");
    expect(css).toContain("body.shell-pane-hidden .shell-command-panel {\n  opacity: 0;");
    expect(css).toContain("--shell-pane-size: 0px !important;");
    expect(css).toContain("--shell-resizer-size: 0px;");
    expect(css).toContain("body.shell-pane-hidden .terminal-panel.shell-output-visible");
    expect(css).toContain("body.shell-pane-hidden .terminal-panel.shell-output-visible .shell-output-resizer,\nbody.shell-pane-hidden .shell-output-pane {\n  opacity: 0;");
    expect(css).toContain("body.shell-pane-hidden .shell-pane-rail");
    expect(css).toContain("body.shell-pane-hidden .terminal {\n  padding-right: 26px;\n}");
    expect(css).toContain("body.shell-pane-hidden .message-form {\n  padding-right: 12px;\n}");
    expect(app).toContain('els.flowPane.querySelector(".shell-output-resizer").addEventListener("dblclick", toggleShellPaneHidden);');
    expect(app).toContain('els.flowPane.querySelector(".shell-pane-rail").addEventListener("click", () => setShellPaneHidden(false));');
    expect(css).not.toContain("terminal-layout-frozen");
    expect(css).toContain("overflow-anchor: none;");
    expect(css).not.toContain("shell-pane-instant-reveal");
  });

  test("supports separate reverse search histories for prompts and shell commands", () => {
    expect(app).toContain('const PROMPT_HISTORY_KEY = "flow.promptHistory";');
    expect(app).toContain('const SHELL_HISTORY_KEY = "flow.shellHistory";');
    expect(app).toContain("const MAX_INPUT_HISTORY_ITEMS = 200;");
    expect(app).toContain("function initialInputHistory(key)");
    expect(app).toContain("promptHistory: initialInputHistory(PROMPT_HISTORY_KEY)");
    expect(app).toContain("shellHistory: initialInputHistory(SHELL_HISTORY_KEY)");
    expect(app).toContain("promptHistoryOrder: new Map()");
    expect(app).toContain("shellHistoryOrder: new Map()");
    expect(app).toContain("ticketInputStates: new Map()");
    expect(app).toContain('activeInputIssueId: ""');
    expect(app).toContain("historySearch: null");
    expect(app).toContain("historyNavigation: null");
    expect(app).toContain("function emptyTicketInputState()");
    expect(app).toContain('promptValue: ""');
    expect(app).toContain('shellValue: ""');
    expect(app).toContain("function inputHistoryMode(input = document.activeElement)");
    expect(app).toContain('return input?.classList?.contains("shell-input") ? "shell" : "prompt";');
    expect(app).toContain("function rememberInputHistory(value, mode = inputHistoryMode(), orderValue = Date.now())");
    expect(app).toContain("function shouldRememberInputHistoryItem(item, mode)");
    expect(app).toContain('return !(mode === "prompt" && item.startsWith("/"));');
    expect(app).toContain("if (!shouldRememberInputHistoryItem(item, mode)) return;");
    expect(app).toContain("function rememberLogHistory(log)");
    expect(app).toContain("function inputHistoryOrder(mode = inputHistoryMode())");
    expect(app).toContain("function inputHistoryLogOrder(log)");
    expect(app).toContain('const timestamp = Date.parse(log.createdAt || "");');
    expect(app).toContain("return timestamp + id / 1_000_000;");
    expect(app).toContain("function sortInputHistory(mode = inputHistoryMode())");
    expect(app).toContain("history.sort((a, b) => (order.get(b) ?? 0) - (order.get(a) ?? 0));");
    expect(app).toContain("if ((order.get(item) ?? -Infinity) <= orderValue) order.set(item, orderValue);");
    expect(app).toContain("sortInputHistory(mode);");
    expect(app).toContain("const order = inputHistoryLogOrder(log);");
    expect(app).toContain('if (normalized.source === "user") rememberInputHistory(normalized.message, "prompt", order);');
    expect(app).toContain('if (normalized.source === "shell:command") rememberInputHistory(normalized.message, "shell", order);');
    expect(app).toContain("rememberLogHistory(log);");
    expect(app).toContain("const existingIndex = history.indexOf(item);");
    expect(app).toContain("if (existingIndex < 0) history.push(item);");
    expect(app).toContain("history.splice(MAX_INPUT_HISTORY_ITEMS);");
    expect(app).toContain("function matchingInputHistory(query, mode = inputHistoryMode())");
    expect(app).toContain("item.toLowerCase().includes(needle)");
    expect(app).toContain("function cloneHistorySearch(search)");
    expect(app).toContain("matches: [...(search.matches || [])]");
    expect(app).toContain("function cloneHistoryNavigation(navigation)");
    expect(app).toContain("function ticketInputState(issueId)");
    expect(app).toContain("state.ticketInputStates.set(issueId, emptyTicketInputState())");
    expect(app).toContain("function saveActiveTicketInputState()");
    expect(app).toContain("inputState.promptValue = messageInput.value;");
    expect(app).toContain("inputState.shellValue = commandInput.value;");
    expect(app).toContain("inputState.historySearch = cloneHistorySearch(state.historySearch);");
    expect(app).toContain("inputState.historyNavigation = cloneHistoryNavigation(state.historyNavigation);");
    expect(app).toContain("function restoreTicketInputState(issueId)");
    expect(app).toContain("messageInput.value = inputState.promptValue;");
    expect(app).toContain("commandInput.value = inputState.shellValue;");
    expect(app).toContain("state.historySearch = cloneHistorySearch(inputState.historySearch);");
    expect(app).toContain("state.historyNavigation = cloneHistoryNavigation(inputState.historyNavigation);");
    expect(app).toContain("function clearQueuedPromptDraftState(flow, message = \"\")");
    expect(app).toContain("function queuedPromptDraftMatches(value, message = \"\")");
    expect(app).toContain("function clearPromptDraftForIssue(issueId, message = \"\")");
    expect(app).toContain("function syncTicketInputState(issueId)");
    expect(app).toContain("if (state.activeInputIssueId === nextIssueId) return;");
    expect(app).toContain("saveActiveTicketInputState();\n  state.activeInputIssueId = nextIssueId;\n  restoreTicketInputState(nextIssueId);");
    expect(app).toContain("syncTicketInputState(issueId);");
    expect(app).toContain("function renderHistorySearchIndicator()");
    expect(app).toContain('form?.classList.toggle("history-searching", Boolean(search));');
    expect(app).toContain('indicator.setAttribute("aria-hidden", String(!search));');
    expect(app).toContain("const shouldFollowLatest = !state.terminalFollowPaused && terminalAtLatest(terminal);");
    expect(app).toContain("applyFlowSplitSize();");
    expect(app).toContain("if (shouldFollowLatest) followTerminalToLatestDuringLayout(terminal, 160);");
    expect(app).toContain('indicator.dataset.mode = "";');
    expect(app).toContain("indicator.dataset.mode = search.mode;");
    expect(app).toContain('indicator.innerHTML = `<span class="history-search-label">i-search:</span> ${escapeHtml(search.query)}_${resultText}`;');
    expect(css).toContain('.history-search-indicator[data-mode="prompt"] .history-search-label {\n  color: #6d28d9;\n}');
    expect(css).toContain('.history-search-indicator[data-mode="shell"] .history-search-label {\n  color: #22c55e;\n}');
    expect(css).toContain('body.theme-dark .history-search-indicator[data-mode="prompt"] .history-search-label {\n  color: #c084fc;\n}');
    expect(app).not.toContain("bck-i-search");
    expect(app).toContain("function scrollTerminalToLatestNow(terminal)");
    expect(app).toContain("function followTerminalToLatestDuringLayout(terminal, durationMs)");
    expect(app).toContain("applyFlowSplitSize();");
    expect(app).toContain("if (now - startedAt < durationMs) requestAnimationFrame(follow);");
    expect(app).toContain("function startOrAdvanceHistorySearch(input)");
    expect(app).toContain('query: "",');
    expect(app).toContain("draft: input.value");
    expect(app).toContain("matches: []");
    expect(app).toContain("search.matches = search.query ? matchingInputHistory(search.query, search.mode) : [];");
    expect(app).toContain("state.historySearch.index = Math.min(state.historySearch.index + 1, state.historySearch.matches.length - 1);");
    expect(app).toContain("function moveHistorySearchForward(input)");
    expect(app).toContain("search.index = Math.max(search.index - 1, 0);");
    expect(app).toContain("function resetInputHistoryNavigation()");
    expect(app).toContain("state.historyNavigation = null;");
    expect(app).toContain("function updateInputHistoryNavigation(input, direction)");
    expect(app).toContain("const history = inputHistory(mode);");
    expect(app).toContain("if (direction < 0) return false;");
    expect(app).toContain("draft: input.value");
    expect(app).toContain("const value = nextIndex === -1 ? state.historyNavigation.draft : history[nextIndex];");
    expect(app).toContain("hideSlashMenu();");
    expect(app).toContain("function inputCaretOnFirstLine(input)");
    expect(app).toContain('return !input.value.slice(0, start).includes("\\n");');
    expect(app).toContain("function inputCaretOnLastLine(input)");
    expect(app).toContain('return !input.value.slice(end).includes("\\n");');
    expect(app).toContain("function handleInputHistoryNavigationKeydown(event)");
    expect(app).toContain('if (event.key !== "ArrowUp" && event.key !== "ArrowDown") return false;');
    expect(app).toContain('const direction = event.key === "ArrowUp" ? 1 : -1;');
    expect(app).toContain("if (direction > 0 && !inputCaretOnFirstLine(input)) return false;");
    expect(app).toContain("if (direction < 0 && !state.historyNavigation && !inputCaretOnLastLine(input)) return false;");
    expect(app).toContain("if (!updateInputHistoryNavigation(input, direction)) return false;");
    expect(app).toContain("function handleHistorySearchKeydown(event)");
    expect(app).toContain("function setInputMode(mode)");
    expect(app).toContain('focusInputPane(mode === "command" || mode === "shell" ? "shell" : "prompt");');
    expect(app).toContain("input.focus({ preventScroll: true });");
    expect(app).toContain("cancelHistorySearch();\n  resetInputHistoryNavigation();\n  saveActiveTicketInputState();\n  input.focus({ preventScroll: true });");
    expect(app).toContain("focusInputPane(\"prompt\");");
    expect(app).toContain("function canSwitchInputPane()");
    expect(app).toContain("const ticket = selectedTicket();");
    expect(app).not.toContain("selectedLinearTicket");
    expect(app).toContain("function handleInputPaneTabKeydown(event)");
    expect(app).toContain("if (event.defaultPrevented) return false;");
    expect(app).toContain('if (event.key !== "Tab" || event.metaKey || event.ctrlKey || event.altKey || event.isComposing) return false;');
    expect(app).toContain("if (!canSwitchInputPane()) return false;");
    expect(app).toContain("event.preventDefault();\n  if (event.repeat) return true;");
    expect(app).toContain('if (focused !== "prompt") return focusInputPane("prompt");');
    expect(app).toContain('if (focusedInputPaneKind() === "prompt" && state.shellPaneHidden) {');
    expect(app).toContain("revealShellPaneForInputFocus();");
    expect(app).toContain("toggleFocusedInputPane();");
    expect(app).toContain('shellInput().addEventListener("keydown"');
    const messageInputKeydown = app.slice(app.indexOf('promptInput().addEventListener("keydown"'));
    expect(messageInputKeydown).toContain("if (handleInputHistoryNavigationKeydown(event)) return;");
    expect(messageInputKeydown).toContain("if (handleInputPaneTabKeydown(event)) return;");
    expect(messageInputKeydown.indexOf("selectSlashCommand();")).toBeLessThan(
      messageInputKeydown.indexOf("handleInputHistoryNavigationKeydown(event)"),
    );
    expect(messageInputKeydown.indexOf("handleInputPaneTabKeydown(event)")).toBeLessThan(
      messageInputKeydown.indexOf("handleInputHistoryNavigationKeydown(event)"),
    );
    const globalKeydown = app.slice(app.indexOf('document.addEventListener("keydown"'));
    expect(globalKeydown).toContain("if (handleInputPaneTabKeydown(event)) return;");
    expect(app).toContain('event.ctrlKey && event.key.toLowerCase() === "r"');
    expect(app).toContain('event.ctrlKey && event.key.toLowerCase() === "z"');
    expect(app).toContain("if (!state.historySearch) return false;");
    expect(app).toContain('if (event.key === "ArrowLeft" || event.key === "ArrowRight") {\n    cancelHistorySearch();\n    return false;\n  }');
    expect(app).toContain('if (event.key === "Escape") {\n    cancelHistorySearch();\n    return true;\n  }');
    expect(app).toContain('if (event.key === "Tab") {\n    event.preventDefault();\n    cancelHistorySearch();\n    return true;\n  }');
    expect(app).toContain("updateHistorySearchMatches(input, state.historySearch.query + event.key);");
    expect(app).toContain("function handleGlobalHistorySearchKeydown(event)");
    expect(app).toContain("if (isEditableKeyTarget(event.target)) return false;");
    expect(app).toContain("if (handleGlobalHistorySearchKeydown(event)) return;");
    expect(app).toContain("if (handleHistorySearchKeydown(event)) return;");
    expect(app).toContain("renderSlashMenu();\n  saveActiveTicketInputState();");
    expect(app).toContain("resetInputHistoryNavigation();\n  saveActiveTicketInputState();");
    expect(app).toContain('rememberInputHistory(command, "shell");');
    expect(app).toContain('rememberInputHistory(message, "prompt");');
    expect(app).toContain("cancelHistorySearch();");
  });

  test("supports fast slash command as a toggle", () => {
    expect(server).toContain('type ReasoningEffort = "low" | "medium" | "high" | "xhigh";');
    expect(server).toContain('type ServiceTier = "fast" | "flex";');
    expect(server).toContain('type AgentSandbox = "read-only" | "workspace-write" | "danger-full-access";');
    expect(server).toContain("const reasoningEfforts = new Set<ReasoningEffort>");
    expect(server).toContain("const serviceTiers = new Set<ServiceTier>");
    expect(server).toContain("const agentSandboxes = new Set<AgentSandbox>");
    expect(server).toContain("function codexThreadOverrides(flow: Flow)");
    expect(server).toContain("function codexTurnOverrides(flow: Flow)");
    expect(server).toContain("function configuredAgentMetadata(flow: Flow): Partial<Flow>");
    expect(server).toContain("...codexThreadOverrides(flow),");
    expect(server).toContain("...codexTurnOverrides(flow),");
    expect(server).toContain("function slashCommandArgs(message: string)");
    expect(server).toContain('if (command === "/effort")');
    expect(server).toContain('if (command === "/model")');
    expect(server).toContain('if (command === "/permissions")');
    expect(server).toContain('updateFlow(flow.id, { agentReasoningEffort: reasoningEffort });');
    expect(server).toContain('updateFlow(flow.id, { agentModel: model });');
    expect(server).toContain('updateFlow(flow.id, { agentSandbox });');
    expect(server).toContain('if (command === "/fast")');
    expect(server).toContain('const serviceTier = flow.agentServiceTier === "fast" ? "" : "fast";');
    expect(server).toContain("updateFlow(flow.id, { agentServiceTier: serviceTier });");
    expect(server).toContain('insertLog(flow.id, "agent:status", serviceTier ? "fast mode enabled" : "fast mode disabled");');
    expect(server).not.toContain('Unknown fast mode');
    expect(server).not.toContain('mode === "flex"');
    expect(server).not.toContain('mode === "off"');
  });

  test("auto-approves sandboxed agent approvals", () => {
    expect(server).toContain('approvalPolicy: "on-failure"');
    expect(server).toContain('approvalsReviewer: "auto_review"');
    expect(server).toContain("sandbox: codexSandboxMode(flow)");
    expect(server).toContain("sandboxPolicy: codexSandboxPolicy(flow)");
    expect(server).toContain('decision: "acceptForSession"');
    expect(server).toContain('decision: "approved_for_session"');
    expect(server).toContain('scope: "session"');
    expect(server).not.toContain('approvalPolicy: "never"');
    expect(server).not.toContain("does not expose approval UI yet");
  });
});
